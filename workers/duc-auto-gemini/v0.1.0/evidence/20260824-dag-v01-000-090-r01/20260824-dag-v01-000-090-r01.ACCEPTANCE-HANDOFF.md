# Acceptance Handoff — Review-Ready 90%

- Run ID: `20260824-dag-v01-000-090-r01`
- Candidate state: `GPT_BOUNDED_FIX_VERIFIED / GPT_REAUDIT_PENDING`
- Explicit boundary: `LIVE_RUNTIME_UNVERIFIED / OWNER_PILOT_PENDING`
- Historical local auditor verdict: `PASS — REVIEW-READY 90%` on `de504d41e15e298dbb88268480a3374d626714ab`
- GPT Web superseding verdict: `REVISE — BOUNDED FIX REQUIRED` on handoff commit `90d837f1fca2b2e048615a213ec8a362fb3cd70e`; GPT-DAG-001/002 are implemented and verified, with GPT re-audit pending

## Delivered

Independent Gemini-only MV3 package; single-job and pending-queue Side Panel controls; XLSX queue; 0/1/multi-reference validation; image output download; Result XLSX checkpoints; durable attempt/tab binding; audit JSONL; exact-once attempt state; blocker detection; deterministic fixtures; live empty-surface mapping; owner pilot runbook.

## Not claimed

No prompt was typed or submitted, no file uploaded, and no generated Gemini image downloaded during the 90% run. Live response selectors, full-size image URL behavior, account/model/locale variation and image quality require the owner pilot.

## Semantic acceptance answers

1. **Why no automatic resend after `SUBMITTED`?** The provider state machine permits retry only while the attempt is `PRE_SUBMIT`/`FAILED_PRE_SUBMIT`. `retryDecision` returns `SUBMITTED_BOUNDARY` for every submitted or later phase. Timeout/ambiguity transitions the same persisted attempt to `OWNER_REVIEW`/`INTERRUPTED`, and checkpoint restore excludes it from pending selection.
2. **Why does output belong to this attempt?** The content adapter freezes URL/image keys and model-response container identities immediately before Send. A result must use a source key absent from that boundary, appear in a newly observed model-response container, remain visible/usable, not be an input, and be unique. “Latest image” or a changed source inside an old container is never sufficient.
3. **How is DOM drift handled safely?** Selectors are layered across ARIA, role, test ID and structure. Unknown composer/send/upload/output state returns an explicit readiness or attribution failure. The adapter never falls back to coordinate/generic first-button clicks; new observed structures must be added with a fixture.

## Revision loop 1 closure claims

- Attempt bindings persist `run_id/job_id/attempt_id → tab_id/window_id` and all wait/advance/abort operations validate that binding on exact Gemini Images.
- Every attempt-stage response is identity-checked; a prior job response produces `ATTEMPT_ID_MISMATCH`, never success for the current job.
- `SUBMITTED` is written to durable local storage before Send. Reopening the Side Panel converts unresolved submitted/output stages to non-runnable `OWNER_REVIEW` and preserves a monotonic attempt serial.
- Queue radios plus **Run Selected** support the numbered one-job owner pilot without editing the workbook.
- Behavioral tests cover tab binding, cross-job leakage, restart recovery, retry budget and response-container attribution.

## Revision loop 2 closure claims

- `batch-core.js` owns active-queue continuation policy. Ambiguity/owner review, interruption, security/quota/policy, and attempt/tab identity failures always emit `BATCH_HARD_STOP` and leave later jobs pending, even when `continue_on_error=true`.
- Ordinary exhausted pre-submit failures may continue only when `continue_on_error=true`.
- `content-decision-core.js` executes lazy input exposure, preview readiness, Send usability and blocker-before-click decisions through injected ports; behavioral tests assert exact click counts and zero Send clicks after blockers.
- Static guards remain supplemental; the decisive loop 2 acceptance evidence is executable behavior.

## Final bounded revision claim

- File-input exposure recovers only from the exact `FILE_INPUT_NOT_EXPOSED` timeout. Security/quota/policy, `ABORTED_BY_OPERATOR`, and unknown wait errors are never converted to `null`.
- Both the upload trigger and upload menu click run behind an immediate blocker/abort guard.
- The executed injected-wait matrix proves blocker-before-exposure = 0 clicks; blocker/abort during first wait = 1 trigger click and 0 menu clicks; normal first timeout may reach the menu; blocker during second wait propagates unchanged.

## GPT bounded-fix claim pending independent re-audit

- The actual Send click now re-snapshots `abortRequested` after durable `SUBMITTED` persistence; an operator Stop at this boundary prevents the click with `ABORTED_BY_OPERATOR`.
- A background-owned extension-wide lease is persisted in `chrome.storage.session` before routing; a simultaneous second `DAG_ROUTE_RUN`, including after MV3 service-worker restart, fails closed with `GLOBAL_ACTIVE_ATTEMPT_EXISTS` before selecting or entering another Gemini tab.
- Focused behavioral coverage plus the full suites pass, but these claims remain `GPT_REAUDIT_PENDING` until GPT independently inspects the published SHA.

## Remaining owner action

Run the numbered controlled sequence in `Duc-Auto-Gemini.PILOT-RUNBOOK.md` using benign test inputs, stopping immediately on any hard-stop condition.
