import { readFile } from "node:fs/promises"; import { assert,pass } from "./test-helpers.mjs"; const body=await readFile(new URL("../background.js",import.meta.url),"utf8"); assert.ok(body.includes('const GEMINI_URLS = ["https://gemini.google.com/*"]')); assert.ok(body.includes('const TERMINAL_KEY = "dag.terminal_attempts.v1"')); assert.ok(body.includes('conflictAction: "uniquify"')); assert.ok(body.includes("chrome.storage.session")); assert.ok(!body.includes("chatgpt")); assert.ok(!body.includes("force")); pass("background: receiver probe, terminal retention and collision-safe download");
