import { readFile } from "node:fs/promises"; import { assert, pass } from "./test-helpers.mjs"; const html=await readFile(new URL("../sidepanel.html",import.meta.url),"utf8"); const css=await readFile(new URL("../sidepanel.css",import.meta.url),"utf8"); const js=await readFile(new URL("../sidepanel.js",import.meta.url),"utf8"); for(const id of ["currentGrid","currentPrompt","currentReferenceGallery","queueList","checkBtn","runBtn","stopBtn"]) assert.ok(html.includes(`id="${id}"`)); assert.ok(css.includes("grid-template-columns:minmax(0,1fr) 130px")); assert.ok(js.includes('classList.toggle("no-refs", !item.references.length)')); assert.ok(js.includes("dag.active_checkpoint.v1")); assert.ok(js.includes("sourceSha256"));
pass("side panel: Current Job, thumbnails, checkpoint and source provenance");
