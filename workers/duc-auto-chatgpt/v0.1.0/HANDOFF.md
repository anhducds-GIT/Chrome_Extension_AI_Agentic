# Coordinator / Auditor handoff

Roles, golden rules, and the #01/#02 templates below now live in [AGENTS.md](AGENTS.md) as the canonical copy — this file keeps them here too so existing links don't break, but AGENTS.md is authoritative if they ever diverge. See also [decisions.md](decisions.md) for major decisions extracted from the Log below.

## #01 — Claude Coordinator review

```text
#01

PROJECT: Duc Auto ChatGPT V0
ROLE: Claude = Coordinator / Architecture Reviewer
IMPLEMENTER: GPT Web
CODE PACKAGE: duc-auto-chatgpt-v0

SCOPE LOCK:
- Chrome Manifest V3 personal extension
- local-only Text Batch Automation on chatgpt.com
- no separate login
- no backend/server
- no extension quota
- no image/file automation
- no multi-tab concurrency
- no bypass of ChatGPT/account limits
- clean-room implementation; do not copy proprietary extension source

TASK:
1. Read README.md, AUDIT.md, manifest.json, background.js, sidepanel.js, content.js.
2. Audit architecture and state machine before proposing changes.
3. Focus on DOM robustness, queue sequencing, stop/pause semantics, Chrome MV3 permissions, and failure recovery.
4. Identify only material issues for V0. Do not expand scope.
5. Return PASS / CONDITIONAL PASS / FAIL with ranked findings.
6. For each blocking finding, provide an exact acceptance criterion for GPT Web to repair.

GUARDRAIL:
Do not implement code unless explicitly authorized. Coordinator/auditor only.
```

## #02 — Codex code audit

```text
#02

PROJECT: Duc Auto ChatGPT V0
ROLE: Codex = Independent Code Auditor
IMPLEMENTER: GPT Web

AUDIT TARGET:
- manifest.json
- background.js
- sidepanel.html
- sidepanel.css
- sidepanel.js
- content.js

V0 CONTRACT:
Sequential text prompts only. Side Panel -> content script -> ChatGPT DOM -> wait for completion -> next prompt.
No server, no login, no quota logic, no image/file automation, no concurrency, no paywall/rate-limit bypass.

AUDIT:
1. Static correctness / JS errors.
2. MV3/API correctness and least-privilege permissions.
3. Race conditions in Start/Pause/Stop and message passing.
4. Duplicate-send risk.
5. False completion / timeout risk.
6. Composer input compatibility (textarea/contenteditable/ProseMirror).
7. Persistence behavior if side panel closes/reopens.
8. Security/privacy: confirm no external network/exfiltration.

OUTPUT:
RESULT: PASS | CONDITIONAL PASS | FAIL
BLOCKERS: numbered list
NON_BLOCKERS: max 5
REPAIR_INSTRUCTIONS: exact and bounded
TESTS_REQUIRED: concrete manual/static checks

Do not rewrite the extension wholesale. Preserve V0 scope.
```

## Log

- 2026-08-22 · Claude · Independent audit of HEAD `54fff5d`. Verdict REVISE. Protected invariants (exact-once, attribution, readiness, retry, security hard-stop) all confirmed intact. Four artifact-truthfulness defects found, all reproduced in the committed Pilot-05 evidence.
- 2026-08-22 · Claude · Đức assigned implementation. Fixed in one pass, no commit:
  - `xlsx-codec.js` `updateConfigSnapshot` appended duplicate config rows reusing the same `@r` numbers (Pilot-05 v005: 64 rows / 44 distinct, 40 duplicate cell refs), freezing a stale first copy of every key. Rows are now pushed back into the captured `rows` array.
  - `DacRunnerCore.basename()` (a lossy image-reference helper) was lower-casing artifact filenames into recorded provenance. New `DacOutputLocation.artifactLeaf()` preserves case; 9 call sites moved over.
  - `saveAuditLog` never cleared `state.auditEvents` after a verified append, so a second flush re-emitted persisted events (`AUDIT_CHAIN_GAP` written twice). Buffer now cleared after the verified directory write only.
  - Four `innerHTML` sinks fed chatgpt.com-sourced image URLs and workbook text into the privileged side panel. All renderers now build nodes; image sources are restricted to `https:` / `data:image/`.
  - Also: persisted byte count is verified against the written blob (directory) and against `fileSize`/`exists` (Downloads); `overwrite` policy no longer reports a first write as `overwritten`; `safeRelativeFolder` no longer admits `*` and `"`; `PERSISTENCE_VERIFICATION_FAILED` added to `FAILURE_TYPES`; a verified recreate whose continuation is blocked no longer displays "RECREATE BLOCKED".
  - Tests: 33 pass (was 31). New `xlsx-config-snapshot-smoke`, `artifact-integrity-smoke`; `pilot-05-checkpoint-fixture` no longer shells out to `tar`. `npm test` added at repo root.
  - Pilot-03 / Pilot-05 artifacts deliberately untouched; they remain the operator evidence for these defects.
- **Next:** Đức runs the live acceptance pass (see README "Running the tests" for the deterministic suite). Live-only items: recreate banner wording, queue/output list rendering after the DOM rewrite, and a fresh continued run producing a v00N checkpoint whose `config` sheet has no duplicate keys.
- 2026-08-24 · Codex Orchestrator · **AGENT BRIDGE V1 WP-2/WP-3/WP-4 IMPLEMENTED — INDEPENDENT DETERMINISTIC PASS, UNCOMMITTED.** Added the owner-reviewed quarantined proposal/checkpoint flow, authenticated `127.0.0.1` Node host, MV3 router/Port transport, pairing UI, Windows install/uninstall scripts, and installed CLI. Removed the legacy unauthenticated `externally_connectable` / `onMessageExternal` Worker API, its job map/keepalive/session retention, and both browser test launchers only after the Option B path passed independent acceptance; preserved the private `DAC_DOWNLOAD_IMAGE` handler. Closed-panel executor calls fail with retryable `EXECUTOR_UNAVAILABLE`; router health/capability calls remain available. Token rotation invalidates the prior caller; uninstall defaults to removing pairing and `-KeepPairing` now removes host/codec/CLI while preserving only pairing under a re-applied current-user ACL. `queue.propose` only stages; owner approval adds rows and verifies audit + Result checkpoint, but never starts a Run or submits to ChatGPT. Final independent audit: **PASS**, `npm.cmd test` **63/63** plus observer smoke, JS/MJS/PowerShell/manifest parse and `git diff --check` PASS, protected Pilot tracked diff `0`. **LIVE OWNER-PROFILE UNVERIFIED.** **Next:** reload at `chrome://extensions`, install/pair the host, then perform one owner-profile sequence: CLI ping/list/propose/poll, exact approval/checkpoint with Run still manual, close/reopen panel without replay, no-ChatGPT-tab and host-death semantics, then rotation/uninstall/reinstall/`-KeepPairing`. No Agent Bridge commit was created.
- 2026-08-22 · Claude · Pilot-06 live acceptance run 1 PASSED. Verified from the artifacts, not from the UI: config sheet 38 keys / 38 distinct, 39 rows / 39 distinct `@r`, 78 cells / 78 distinct (valid OOXML — Pilot-05 v005 was 64 rows / 44 distinct with 40 duplicate cell refs); audit 26 events with zero duplicate lines; `write_outcome=written` on all three fresh images; audit filename kept its capital-D case. Fixes #1, #3, #4 proven live.
- 2026-08-22 · Claude · Checkpoint naming convention changed to two digits (`v01`) per Đức. `formatVersion` pads to 2 and `parse` accepts `\d{2,}`, so three-digit checkpoints written under the old convention still parse and resume — a legacy `v001` continues as `v02`. Versions past 99 widen rather than truncate.
- 2026-08-22 · Claude · Added `scripts/add-jobs-to-ledger.mjs` and used it to append P06-D and P06-E to the Pilot-06 `v001` ledger, so the continuation has real work (a finished run is locked forever by design and cannot produce a second checkpoint on its own). Simulated against the real cores first: A/B/C `SAFE_COMPLETE`, D/E `SAFE_PENDING`, 0 blockers, next checkpoint `v02`. Editing a verified checkpoint by hand is acceptable for a test fixture only; the runner cannot detect it.
- **Next:** Đức runs Pilot-06 continuation (checks F–I in `pilot-06/README-PILOT-06.txt`) to prove the resume path — the exact path where Pilot-05 corrupted `previous_checkpoint_filename`. Then commit.
- 2026-08-22 · Claude · Pilot-06 continuation PASSED on the substance. Checks G/H/I verified from `v002`: `previous_checkpoint_filename = Duc-Auto-ChatGPT-Pilot-06__results__v001.xlsx` with its capital-D case intact — this is the exact field Pilot-05 corrupted to lowercase, so fix #2 is now proven live on the resume path. Config sheet 39 keys / 39 distinct, 40 rows / 40 distinct `@r`, 80 cells / 80 distinct across a resume (where snapshots run most often). Audit 44 events, zero duplicate lines, zero `AUDIT_CHAIN_GAP` — Pilot-05 recorded a spurious gap and wrote it twice. Exact-once held: every one of the five jobs shows exactly one `PROMPT_SUBMITTED`, and A/B/C were not resubmitted during the continuation.
- 2026-08-22 · Claude · Check F (two-digit name) not yet met: the run produced `v002`, not `v02`. Not a code defect — `checkpoint-core.js` was edited at 15:29 and the run was at 15:41 without a Chrome extension reload, so the side panel still held the cached module. Verified the on-disk module renders `v02`. Appended P06-F to the `v002` ledger; after a reload the next continuation must write `v03`, which both closes F and confirms the reload took effect.
- 2026-08-22 · Claude · Check F PASSED after the extension reload: run 3 wrote `Duc-Auto-ChatGPT-Pilot-06__results__v02.xlsx` from a `v001` ledger, confirming both the two-digit convention and backward compatibility. `write_outcome` correctly reported `overwritten` for P06-D/E this time because the files already existed.
- 2026-08-22 · Claude · **New P1 found, introduced by the naming change.** Run 3 continued from `v001` rather than `v002`, so the folder now holds `v002` and `v02` — both parse to version 2. `highest()` tie-breaks on filename and returns `v002` (written 08:42) over `v02` (08:50), i.e. a silent fall back to an older checkpoint, the exact failure the protocol exists to prevent. Root cause: `assertCheckpointVersionAvailable` compared only the exact filename, never the version; `hasVersionConflict()` already existed in `checkpoint-core.js` but was never called. Fixed: added `versionCollisions()`, wired the pre-write check to compare versions, and made the resume scan block with `RESUME_CHECKPOINT_VERSION_AMBIGUOUS` instead of choosing. Regression tests added in `checkpoint-protocol-smoke` and `artifact-integrity-smoke`.
- 2026-08-22 · Claude · Codex ran the SETUP UI work (`codex exec`, workspace-write) on brief: promote the six SETUP actions to 32px+ targets, keep Run dominant at 38px, and attach an inline fix button to each actionable Check Plan finding. Result accepted after review, with two corrections: (a) Codex had dropped the `severity !== "OK"` filter so passing checks rendered as extra rows, which lengthens the panel and works against the operator's scrolling complaint — filter restored, sort kept; (b) Codex rewrote `operator-glossary-core.js` from Vietnamese to English section headers and terms, entirely outside the brief and worse for the operator — reverted. No markup sink was reintroduced; `artifact-integrity-smoke` guards that.
- **Next:** operator decision on the ambiguous `pilot-06` folder (rename `...__results__v002.xlsx` aside so `v02` is unambiguously latest), then visual acceptance of the SETUP screen, then commit.
- 2026-08-22 · Claude · Added the folder-pick dialog (`#folderPickDialog`). Chrome's directory picker cannot be pre-filled or pre-navigated, and the expected-folder path plus its Copy control lived in a card the operator had to scroll to find. Choosing a folder — from the SETUP button, the Result-folder button, or the new inline Check Plan action — now shows the recorded `output_folder_hint` with a Copy button first, then opens the picker from a second click. `showDirectoryPicker()` stays inside that user gesture: `confirmFolderPick` closes the dialog synchronously and awaits nothing before the picker call, which `artifact-integrity-smoke` asserts against comment-stripped source. With no recorded hint the dialog is skipped so no dead click is added. The path is `user-select: all` so Ctrl+C still works if the clipboard API is unavailable.
- 2026-08-22 · Claude · Operator diagnostics are now Vietnamese and actionable. Added `operator-messages-core.js`: a code → `{label, guidance}` map covering all 30 codes the panel can render. The finding CODE stays English everywhere it functions as an identifier (audit JSONL, Result ledger, log, tests); only what the operator reads is translated, and it lives outside the diagnostic cores so those keep one job and no test asserts on prose. Each row now shows a Vietnamese title, Vietnamese guidance, and the original message as smaller technical detail — that message carries the filenames and job IDs the operator needs. An untranslated code falls back to its English text rather than rendering blank. `renderResumePlan` previously concatenated every finding into one run-on English line; it now renders the same row format as the Check Plan panel. Inline action buttons extended to the resume codes and relabelled in Vietnamese.
- 2026-08-22 · Claude · `RESUME_CHECKPOINT_VERSION_AMBIGUOUS` deliberately gets a **Chép tên file** action rather than an automatic rename. Either colliding file may still be the authoritative checkpoint and the runner cannot tell which, so the operator is handed the exact names and makes the call. The finding now carries a `files` array so the action copies the names instead of re-parsing them out of prose.
- 2026-08-22 · Claude · A safety test was found asserting on a button caption (`label: "Choose folder"`), which broke the moment captions were translated. Rewritten to assert the handler wiring instead. Operator-facing text must never be what a safety test depends on.
- **Still English:** the Recreate confirmation dialog and its Resume Plan prompt. `tests/recreate-core-smoke.mjs` asserts on that exact wording because it encodes the reviewed duplicate-risk decision, so translating it should be a deliberate change with the test updated alongside — not folded into a UI pass.
- 2026-08-22 · Claude · Two defects reported from live testing of the Vietnamese diagnostics, both real: (1) the blocker rendered **twice** because `validate()` copies every resume finding into `state.diagnostics` and both the Check Plan panel and the Resume Plan panel then drew it — the Resume Plan panel now yields those rows once Check Plan has run and keeps only what is unique to it; (2) the row had **no action button** because `addResumeFinding(code, message, guidance)` reconstructed a bare finding and discarded the `files` array the action depends on — it now forwards the remaining fields.
- 2026-08-22 · Claude · Replaced the "Chép tên file" placeholder with a real resolution: **Xử lý file trùng** opens a dialog that reads each colliding checkpoint and shows its own recorded `checkpoint_created_at` and job counts, preselects the newest, and renames the file the operator does not keep to `<name>__superseded.xlsx` via `FileSystemFileHandle.move()`. Nothing is deleted and nothing is overwritten — `supersededName()` probes for a free name first. Resume state is re-derived from disk afterwards rather than trusting the plan built while the folder was ambiguous. Tests assert the no-delete and no-overwrite properties.
- 2026-08-22 · Claude · Validating that dialog against the real pilot-06 collision proved the earlier decision not to auto-resolve was right: `v002` (15:41) holds 5/6 jobs with P06-F still pending, while `v02` (15:50) holds 5/5 and has no P06-F. The newer file is **not** the safe default — keeping it silently drops a pending job. The dialog now states the pending count and warns "Bỏ file này là mất N job đang chờ." on any option that carries unfinished work.
- 2026-08-22 · Claude · Fixed the collision dialog rendering outside its box. `.recreate-confirm-dialog` declared only width, border and shadow — no padding, background, height bound or wrap rule. The new dialog carries a list of long checkpoint filenames, so it grew past the short side panel and its content was clipped, while an unbroken filename pushed text past the right edge. Added explicit padding and background, `max-height: calc(100vh - 32px)` with `overflow-y: auto`, `overflow-wrap: anywhere`, and a sticky action row so the buttons stay reachable when the list is long. This also hardens the existing Recreate and audit-gap dialogs, which shared the same rule.
- 2026-08-22 · Claude · Added the operator-chosen rename style Đức asked for. The dialog now offers `__superseded` (marks the file as the older record) or ` (1)` (neutral — just makes the name different). Both stop the name matching the checkpoint pattern, which is what actually clears the ambiguity; a test asserts that by parsing each generated name back through `DacCheckpointCore.parse` and requiring null. Repeat runs count up (` (2)`, ` (3)` / `__superseded-01`) and every candidate is checked free before use, so no rename ever overwrites.
- 2026-08-22 · Claude · The in-app preview pane cannot verify this UI: it serves pages as `data:` snapshots with `script-src 'none'` and drops the linked stylesheet (`document.styleSheets.length === 0`), so both computed styles and scripted checks are meaningless there. Visual acceptance of the dialogs has to happen in the real side panel.
- 2026-08-22 · Claude · Collision dialog confirmed working end to end on the live folder: Đức chose the ` (1)` rename style and `...__results__v002.xlsx` became `...__results__v002 (1).xlsx` at 22:29. That name no longer parses as a checkpoint, so `versionCollisions()` is now empty and `highest()` resolves cleanly to `v02`.
- 2026-08-22 · Claude · **Bug reproduced: Run says READY but does nothing.** With `v02` as the ledger all five jobs are `SAFE_COMPLETE`, so `selectQueue(queue, "all")` returns an empty array, yet `controls()` enables Run from `workbook && prepared && outputSettings && validated` without ever asking whether a job is eligible. `run("all")` then returns `{ok:false, reason:"No all jobs are eligible."}`. The message does appear, but after a green READY TO RUN button it reads as a silent no-op. Fix is to make the control reflect real eligibility, not to make Run act when there is nothing to act on.
- 2026-08-22 · Claude · Verified for the next session: `rerun_done=true` is honoured by `runner-core.prepare()` but then overridden by `resume-core.applyToQueue()`, which forces every `SAFE_COMPLETE` job to `skipped`/`protected_checkpoint`. **There is currently no path to re-run a completed job while resuming** — by design ("completed verified outputs stay skipped forever"). Any override feature must go through an explicit per-job approval, not by weakening that rule. Also verified: `selectQueue` already supports `pending` and `selected` modes, but only `all` and `failed` have buttons wired.
- **Next:** see `NEXT-SESSION-BRIEF.md` in this folder — three items (Run-button eligibility bug, deliberate per-job override with preserved history, and job selection in SETUP), with the recommended order and the constraints that must hold. Tree is at 34/34 tests passing and **uncommitted**.
- 2026-08-22 · Codex · **OWNER DECISION — DO NOT REVERT:** Đức explicitly requested the operator glossary to use English section headings and English App terminology while keeping the explanatory text in Vietnamese. This supersedes the glossary-revert rationale recorded above on 2026-08-22. The required sections are `SETUP`, `RUN`, `OUTPUT`, `STATUS / FAILURE`, and `TIMING`; the required term titles are `Timeout`, `Retries`, `Safety cooldown`, `Inter-job delay`, `Waiting for ChatGPT ready`, `Response / output detection`, `Saving`, `Reconciling`, `Persistence verified`, `Detected not downloaded`, `Halted`, `Exact-once`, `Security hard stop`, `Job elapsed`, and `Next action`. The drawer chrome (`Hướng dẫn vận hành`, `Đóng`) and all term explanations remain Vietnamese unless Đức requests otherwise.
- 2026-08-22 · Codex · **OWNER UX/TIMING REQUEST:** Đức requested three connected changes: (1) a Run control inside the Run tab using the same validation and real queue-eligibility gate as Setup; (2) one bidirectional `↔` draggable separator that resizes both Run-dashboard column pairs, supports keyboard adjustment/double-click reset, and persists its ratio; and (3) randomized defaults `delay_min_sec=12`, `delay_max_sec=24`, and `safety_cooldown_sec=6-9`. `safety_cooldown_sec` remains the same field and is backward compatible with a fixed integer; a range selects one inclusive random integer per readiness gate. Inter-job delay starts only after the previous job has persisted output, passed idle readiness, and reached `SUCCESS`; after the delay, the next job still must pass its own readiness gate. Safety cooldown is not a promise to submit: after READY is first observed, the extension waits the selected cooldown and requires READY again. Explicit XLSX values remain authoritative and are not silently migrated; the new Run Settings controls provide deliberate local overrides.
- 2026-08-23 · Codex · **OWNER OUTPUT-DENSITY REQUEST:** Keep only `Image filename pattern` and `When filename already exists` visible in the primary NAMING area. Move `Result checkpoint pattern`, `Audit JSONL filename`, `Run ID`, `Latest checkpoint`, and `Checkpoint file` into the collapsed `Output details & advanced naming` section. The destination preview in that section is read-only: it explains the effective image folder and the exact Result XLSX / Audit JSONL paths derived from the active output profile and naming configuration.
- 2026-08-23 · Codex · **OWNER NAMING UI REQUEST:** Đức requested the visible Naming UI in Vietnamese with larger, clearer typography and controls. Keep canonical configuration values and placeholders unchanged. The advanced `Audit JSONL filename` control must align to the top of the same row as the result-checkpoint control even though only the latter has help text.
- 2026-08-23 · Codex · **OWNER RUN-SETTINGS DENSITY REQUEST:** Run Settings are short numeric values and must not stretch across all available card width. Each field uses a compact 116–120px grid track and displays its unit as part of the control: `giây` for Timeout, Inter-job min/max, and Safety cooldown; `lần` for Retries. The underlying XLSX/config keys and numeric/range values remain unchanged.
- 2026-08-23 · Codex · **OWNER QUEUE PROMPT PREVIEW REQUEST:** Every Run Queue row must show a short, single-line prompt excerpt so the operator can identify the intended submission before running it. Use an explicit `Xem đầy đủ` / `Thu gọn` toggle rather than hover. Expanded prompt text preserves line breaks, remains selectable for copying, and is rendered only through `textContent`; selection checkboxes and prompt expansion stay separate actions.
- 2026-08-22 · Claude · Implemented all three items from `NEXT-SESSION-BRIEF.md` in one pass, per the brief's suggested order (C, A, B), with regression tests for each:
  - **Issue A (Run says READY, does nothing):** `controls()` now computes `eligibleAll = DacRunnerCore.selectQueue(queue, "all").length` and disables Run when it is zero even though `ready` is true, showing a Vietnamese hint ("Mọi job trong ledger đã hoàn tất — chọn job muốn chạy lại bên dưới…") instead of a green button that does nothing when pressed.
  - **Issue C (choose which jobs to run):** `selectQueue(queue, "selected", ids)` now also accepts an array or Set of job ids (duck-typed on `.has`, not `instanceof Set`, so it still works when the module runs in a different vm/realm than its caller — the test suite does this). Existing single-id string callers are unchanged. Queue rows in the RUN screen got a selection checkbox (`state.runSelection`, separate from the existing row-expand `state.selectedJobId`); a new "▶ Chạy job đã chọn" button runs `run("selected")` against the full set, and the selection clears itself once that run finishes.
  - **Issue B (deliberate rerun of a completed job):** reused the existing Recreate approval/checkpoint mechanism per the brief rather than building a parallel one. `DacRecreateCore.approval()` now also accepts `recoveryState: "SAFE_COMPLETE"` (previously only `AMBIGUOUS_SUBMITTED`), and `snapshot()`/`approval()` capture the prior `result_file` / `persistence_verified` / `write_outcome` / `output_saved_at` into `recreate_origin_*` fields and the forensic history JSON — the ambiguous-recreate case never had a verified prior file so this is additive and does not change `recreate-core-smoke.mjs`'s existing assertions (verified, still passing). No change was needed in `resume-core.js`: the moment approval is persisted the job's own `status` field becomes `RECREATE_APPROVED`, so `resume.classify()` naturally reclassifies it as `AMBIGUOUS_SUBMITTED`/`RESUME_RECREATE_INCOMPLETE` on the next `plan()`, and the existing `operator_recreate` branch in `applyToQueue()` already handles the rest — the same protection that guards a normal completed job now guards this approved-but-not-yet-reverified one. New UI: a "↻ Chạy lại {id}" button in the queue-row detail panel for `SUCCESS`/`persistence_verified` jobs opens `rerunConfirmDialog`, which asks the operator to choose "Giữ ảnh cũ" (uniquify, default) or "Đè hẳn lên ảnh cũ" (overwrite) for that one job only — recorded as `rerun_collision_policy` on the job row and read by `saveGeneratedImage()` in preference to the run's global collision-policy setting, so one operator's per-job choice can never be silently defeated by (or silently defeat) the run's default. Unlike `confirmRecreate`, `confirmRerun` never auto-continues into `run("all")` afterward — it does exactly the one deliberate attempt the operator asked for. The completion audit (`RECREATE_JOB_SUCCESS`) now names the superseded file when there was one, so the audit trail records which attempt replaced which file and when, instead of the new `result_file` silently erasing that history.
  - Tests: 37 pass (was 34). New `run-eligibility-static.mjs`, `queue-selection-core-smoke.mjs`, `deliberate-rerun-smoke.mjs`. `runner-core.js`'s `selectQueue` and `recreate-core.js`'s `snapshot`/`approval` changed; `resume-core.js` and `sidepanel.js`'s existing recreate/reconciliation code paths did not need to change.
  - Not touched: pilot-03 / pilot-05 / pilot-06 evidence.
- **Next:** Đức reloads the extension (`chrome://extensions`) — every change above is in `.js` files — and does a live acceptance pass: (1) load a ledger where every job is `SAFE_COMPLETE` and confirm Run is disabled with the Vietnamese hint instead of doing nothing; (2) tick a couple of pending/failed jobs in the QUEUE list and confirm "Chạy job đã chọn" runs only those; (3) on a completed job, try "Chạy lại", confirm the dialog names the job and its saved file, pick "Giữ ảnh cũ" and verify the old image survives alongside a new `__attempt-01` file, then repeat once choosing "Đè hẳn lên ảnh cũ" and verify the old file is actually replaced. Tree is at 37/37 tests passing and **uncommitted** — ask before committing.
- 2026-08-22 · Claude · Codex made concurrent edits while the above was in progress (not this session's work, noted for context): `runFromRunTabBtn` (a Run control inside the Run tab sharing Setup's eligibility gate), a draggable `↔` column-resize separator, and default pacing changed to `delay_min_sec=12`, `delay_max_sec=24`, `safety_cooldown_sec=6-9` (now an inclusive random-range string, backward compatible with a fixed integer). Verified with `npm test` (37/37) before and after picking this session back up; no conflict with the work below.
- 2026-08-22 · Claude · Three items reported live, investigated and fixed in one pass:
  1. **Bug: Output tab "Images" row showed jobs as undetected/"0 verified" even though the files existed on disk and the loaded ledger already recorded them as `persistence_verified` with a `result_file`.** Root cause: `renderOutputScreen()`'s Images count read `state.verifiedImageFiles.length`, an array populated only by writes made during *this session's own* run (reset on every workbook/ledger load). Loading or resuming any existing ledger without running anything new always showed 0, regardless of the ledger's own recorded proof — this is why pilot-06's P06-D/E looked "not detected" after a fresh load. Fixed: the count now reads `queue.filter(item => item.persistence_verified && item.result_file).length`, which is correct whether the proof came from this session's writes or from a loaded ledger (Result XLSX and Audit JSONL rows already worked this way; only Images was the exception). Test: `output-images-verified-static.mjs`.
  2. **Bug report: images silently save-overwrite with no option offered.** Traced to data, not code: pilot-06's own Result XLSX config sheet has `collision_policy=overwrite` baked in (row `A14`/`B14` in both `v001.xlsx` and `v02.xlsx`), set during an earlier test of the `overwritten` write-outcome path (see the 2026-08-22 entry about Check F). The three-option control (`overwrite` / `uniquify` — "Keep both, add number" / `fail`) already exists in SETUP → Naming, defaulting to `uniquify` for any workbook that doesn't set `collision_policy` itself; pilot-06 simply carries a leftover explicit `overwrite` from that earlier test and nobody reset it. **Did not touch pilot-06's files — they are protected evidence.** Real gap found and fixed instead: the active policy was only three lowercase words inside the Naming row's detail text with a plain green "OK", so a workbook-carried `overwrite` is easy to run straight past. Added `OUTPUT_COLLISION_OVERWRITE_ACTIVE` (`plan-diagnostics-core.js`, scope `output`, severity `WARNING` — Run stays enabled, this does not block, overwrite is a legitimate choice) with a Vietnamese entry in `operator-messages-core.js`, and bumped the Naming checklist row (`orchestrator-review-core.js`) to `WARNING` with an explicit "sẽ ĐÈ LÊN file ảnh cũ trùng tên" detail whenever `collisionPolicy === "overwrite"`. Test: `collision-overwrite-warning-smoke.mjs`. Đức: if you want pilot-06's fixture back to `uniquify` for cleanliness, that's a manual edit outside this tooling — I won't touch it.
  3. **Feature: Pause Run**, prompted by wondering whether Continue Run implies a pause mechanism too. Deliberately built as distinct from Stop rather than a rename of it: exact-once submission means an in-flight attempt can never be safely suspended mid-generation, so Pause only ever holds the queue at the one boundary that is already safe — after the current job reaches a terminal state (`SUCCESS`/`FAILED`/etc.) and before the next one is gated/submitted, checked at exactly two points in `run()`'s loop (never mid-attempt). Stop still abandons the run entirely (no auto-resume; press Run again); Pause holds the *same in-memory run* so "Tiếp tục" continues it with no re-checkpoint and no re-gate — nothing about the ledger, resume plan, or checkpoint protocol changes. New `pauseResumeBtn` truthfully names all three states ("⏸ Tạm dừng" / "⏸ Đang dừng sau job hiện tại…" / "▶ Tiếp tục") rather than a two-state toggle that could claim "paused" while the current job is still finishing; clicking again while pending cancels the pending pause. Stop still immediately wins over a pause (the pause wait loop checks `state.stopRequested` every 250ms). `RUN_PAUSED`/`RUN_RESUMED` audit events added for the trail. Test: `pause-resume-static.mjs`.
  - `npm test`: 40 pass (was 37).
  - Not touched: pilot-03 / pilot-05 / pilot-06 evidence files.
- **Next:** Đức reloads the extension and does a live acceptance pass: (1) reopen pilot-06 (either checkpoint) and confirm the Output tab's Images row now shows the real verified count instead of 0; (2) Check Plan on that same ledger and confirm a visible WARNING now appears calling out the active overwrite policy, both in the Naming checklist row and as its own guidance row with an inline action; (3) start a run with 2+ eligible jobs, click "⏸ Tạm dừng" mid-job and confirm it waits for the current job to finish before actually holding (button should say "Đang dừng sau job hiện tại…" in between), then click "▶ Tiếp tục" and confirm the next job starts without needing to reload anything; also confirm Stop still works immediately while paused. Tree is at 40/40 tests passing and **uncommitted** — ask before committing.
- 2026-08-23 · Claude · Pilot-06 wrapped up. Đức asked why P06-D/E showed Pending after loading `v001.xlsx` despite the folder already having all 5 images, guessing a missing job ID. Opened the raw xlsx (zip) directly to check rather than guessing: both jobs have a valid `id`/`prompt` in `v001.xlsx` — their `status` is simply blank there, because `v001.xlsx` is the pre-continuation ledger (D/E were appended to it as PENDING fixtures, then *completed in the later continuation, which wrote a separate file*, `v02.xlsx`). Confirmed `v02.xlsx` has both as `SUCCESS` with a `result_file`. Not a bug: a ledger only knows its own recorded history, by design (exact-once), so it can never be "aware" of a same-named file that another checkpoint produced. Guidance given: load `v02.xlsx` (or Continue Existing Run, which auto-discovers the latest checkpoint) instead of `v001.xlsx`.
- 2026-08-23 · Claude · SETUP/RUN screen simplification, per Đức feedback that the UI felt overwhelming for a non-technical operator. Confirmed scope with Đức first ("Vừa phải" / moderate) before touching ~4 regions, since one candidate move would have undone a documented prior decision. Moved behind collapsed `<details>` blocks (nothing removed, only re-parented — every id/handler still exists and works once expanded):
  - Run Settings' entire numeric grid (timeout, retries, inter-job min/max, safety cooldown) merged into the same collapsed block as the existing Max input references / Continue on error / Rerun DONE — workbook config or defaults already cover these for almost every run.
  - `saveAuditJsonlInput` ("Save Audit JSONL (technical log)") moved out of the always-visible save-toggles row into the existing advanced output/naming block, next to its own filename field.
  - "Copy for AI Review" (a reviewer/dev tool, not an operator control) wrapped in its own advanced toggle in the Plan Check card.
  - RUN screen's six-field runtime-information grid collapsed into "Chi tiết vận hành" — the pipeline stepper, timer badge, and one-line timing status already give at-a-glance progress without it.
  - **Deliberately NOT hidden:** the image filename pattern and collision policy stay in the always-visible Naming section. `v1-ui-ux-closure-static.mjs` already protects this as a past decision ("Naming is visible and no longer hidden inside advanced output details"), and collision policy specifically was just made *more* prominent last session (a Check Plan warning) after the silent-overwrite bug report — folding it back into a collapsed block would have undone that fix in the same breath as fixing it. Caught this by reading the existing test before touching the HTML, not by memory.
  - Test: `setup-simplification-static.mjs`. `npm test`: 41 pass (was 40). No JS logic changed — sidepanel.js only ever looks elements up by id, never by DOM position, so nothing needed updating there.
- **Next:** Đức reloads and confirms the SETUP screen reads as simpler (Workbook, References, Output destination + save toggles, Naming with collision policy, Check Plan + Run all visible by default; everything else one click away under "Tuỳ chỉnh nâng cao" / "Nâng cao" / "Chi tiết vận hành"). Then start Pilot-07 as a live acceptance pass over everything accumulated since Pilot-06 wrapped — none of it has been exercised in the real side panel yet: selective job running (checkbox + "Chạy job đã chọn"), the completed-job rerun dialog with its keep/overwrite choice, Pause/Resume, the new pacing defaults (delay 12–24s, cooldown 6–9s), Codex's Run-tab button and column resizer, and this UI simplification itself. Tree is at 41/41 tests passing and **uncommitted** — ask before committing.
- 2026-08-23 · Claude · Đức reported that preparing an XLSX for Pilot-07 felt like it required filling in far more than his real workflow ("id + prompt, paste in; only truly mandatory extras I'll fill"), and that the extension should auto-fill everything else rather than asking the operator to. Diagnosed from `Pilot-07/Duc-Auto-ChatGPT-Pilot-07.xlsx` directly (opened the zip, not guessed): **not a code defect.** `xlsx-codec.js` `parseWorkbook()` has only ever required `id`/`prompt` on the `jobs` sheet (`required = ["id", "prompt"]`); `config` is optional and every key defaults sensibly (`xlsx-run-plan-core.js`). The actual Pilot-07 file is a **Pilot-06 Result checkpoint reused as the input workbook** — 38 columns including `attempt_id`, `detection_diagnostics` (multi-KB JSON blobs), `status=SUCCESS` rows already carrying `P06-A`/`P06-B` data — which is why it looked like it demanded a lot of fields. The README already documented `id`/`prompt` as the only required columns but never told the operator not to start from a Result file.
  - Added `templates/Duc-Auto-ChatGPT-Template.xlsx`: `jobs` sheet with only `id`/`prompt` (yellow header fill + Vietnamese cell-comment marking them mandatory) and `reference_images` (grey fill, marked optional, `|`-separated), plus an empty `config` sheet (grey fill, comment explaining it's optional and what it's for). Deliberately **no pre-filled id rows** — a row counts as a job the moment column A has a value even with a blank prompt (`jobs.filter(item => item.id || item.prompt)`), so pre-numbering unused rows would have silently queued blank prompts; Đức pastes id+prompt together for exactly as many rows as he has, matching his actual workflow.
  - Verified compatibility for real, not assumed: ran the template through the actual `xlsx-codec.js` `open()` path in Node (`DecompressionStream`/`FakeDOMParser` from `tests/xlsx-test-utils.mjs`, not a synthetic fixture) — empty template correctly throws "jobs has no job rows" (expected/safe), and a copy with 2 filled rows parses to the exact `{id, prompt, reference_images}` shape the runner expects, with `config` correctly empty so all defaults apply.
  - `README.md` "Workbook contract" now tells the operator to start every new pilot from the template and never from a prior Result XLSX, naming why (Result checkpoints carry 30+ runtime-only columns).
  - Not built (deliberately, to avoid overbuilding on one turn): an in-app "download blank template" button. The static template file fully solves the reported problem with zero code/regression risk; offered as a follow-up only if Đức wants one-click access inside the side panel too.
  - No `.js`/`.html`/`.css` changed — nothing to reload. `npm test` not re-run (no runtime code touched).
- 2026-08-23 · Claude · **New feature: "Nhập prompt nhanh" (Quick Prompt).** Đức's real workflow (GPT's own Orchestrator skill generates ideas/concepts; his entire manual step per image is typing "OK, render") doesn't fit authoring an Excel workbook for every image. Confirmed the one open design question with Đức first: each new prompt appends into the SAME session ("nối tiếp trong 1 phiên"), not a fresh file per render.
  - `xlsx-codec.js`: extracted `open()`'s post-unzip parsing into a shared `parseWorkbook(fileName, entries)`, then added `createWorkbook(fileName, jobs)` — builds a complete, valid XLSX (Content_Types, rels, workbook.xml, jobs + config worksheets) entirely in memory, no uploaded file involved — and `addJob(workbook, values)`, which appends one new row into an already-open workbook (mirrors `updateJob()`'s header-growing behaviour for any column beyond id/prompt). Both return the exact same shape `open()` does, so every other module (runner-core, resume-core, the checkpoint writer, collision policy) treats a quick-prompt session identically to a workbook opened from disk — nothing else needed to change.
  - `sidepanel.js`: a textarea + one button (`submitQuickPrompt()`). First prompt in a session calls `createWorkbook()` and runs the same post-load setup as `openWorkbook()` (`applyWorkbookConfig`, profile resolution if configured); every later prompt calls `addJob()` on the existing `state.workbook`. Either way it then reuses the "selected" run mode built for the QUEUE checkboxes (`state.runSelection = new Set([job.id])`, `run("selected")`) so it only ever runs the just-added job — never silently sweeps in an older pending job from earlier in the same session. Job ids are auto-generated (`Q001`, `Q002`, …) and checked against the session's existing ids first.
  - Placed above the Excel workbook card in SETUP since it's the primary path for this workflow now, not a buried extra.
  - Testing needed a small addition since Node has no built-in `DOMParser`/`XMLSerializer`: added a dependency-free `parseXmlDocument()`/`FakeDOMParser` to the existing test-only `xlsx-test-utils.mjs` (same spirit as its existing fake DOM — only handles the exact XML subset xlsx-codec.js produces/consumes, not general XML). Used it to verify a full round trip: build a session, append a job, `updateJob`/`updateConfigSnapshot` it, then re-parse the *persisted bytes* from scratch and confirm the data survives, not just the in-memory objects.
  - Tests: 43 pass (was 41). New `xlsx-quick-prompt-smoke.mjs` (core round trip) and `quick-prompt-static.mjs` (UI wiring). Caught and fixed one bug during a `replace_all` edit that leaked `state.quickPromptCounter = 0` into `run()`'s end-of-run cleanup outside its intended `if` — found by re-grepping after the edit rather than trusting it, per the "verify what you observe now" rule.
  - Not touched: pilot-03 / pilot-05 / pilot-06 evidence.
- **Next:** this is new, untested-live surface — Đức should try it before anything else in Pilot-07: type a prompt with no workbook loaded (confirm a session starts and the image saves to the default Downloads/Duc Auto ChatGPT location), then type a second prompt in the same sitting (confirm it appends — QUEUE should show 2 jobs, not a reset) and confirm nothing about the first job's status changes when the second appends. Tree is at 43/43 tests passing and **uncommitted** — ask before committing.
- 2026-08-23 · Claude · Two small follow-ups on `templates/Duc-Auto-ChatGPT-Template.xlsx` from Đức, both data/template-only (no `.js` touched):
  1. **id naming:** Đức wants numeric ids (1, 2, 3…) instead of the letter style (`P06-A`) seen in the old pilot fixtures, and asked whether that's a system-level thing. Traced it: it isn't. `image_filename_pattern` defaults to the literal `{job_id}` token ([output-location-core.js:144](output-location-core.js#L144)) with zero transformation, and job execution order comes from worksheet row order, not from sorting `id` (`xlsx-codec.js` builds `jobs` from `jobsSheet.rows.slice(1)` in document order). The `P06-A`/`P06-B` style was only ever operator/fixture-authored data (`scripts/create-pilot-06.mjs` and friends), never generated by the extension. Told Đức: no code change needed, no zero-padding needed either — plain `1, 2, 3…` in the `id` column is enough, since row order (not id value) drives run order.
  2. **Added an `stt` column** as the new first column (A), purely because Đức wants a leading sequence-number column as a habit for referring to rows out loud — separate from the `id` column the extension actually reads. Verified before adding, not assumed: grepped the whole extension source for `stt`, zero hits anywhere — confirming an extra header the codec doesn't recognize just becomes an unused property on the job object (`headers.forEach((name, index) => { if (name) item[name] = values[index] || ""; })` in `xlsx-codec.js`), never read by `runner-core.js`/`output-location-core.js`/anything else. This also means `stt` is safe to **pre-number 1..30** in the template (unlike `id`/`prompt`, which stay blank) — a row only becomes a queued job when `id` or `prompt` is non-empty (`jobs.filter(item => item.id || item.prompt)`), and `stt` alone is neither, so pre-filling it can never create a phantom blank-prompt job. Proved this rather than trusting the read: built a copy of the template with `stt` pre-numbered 1–30 and `id`/`prompt` filled on only 2 rows, ran it through the real `xlsx-codec.js` `open()` path in Node, and confirmed exactly 2 jobs parsed (not 30). `jobs` sheet is now `stt | id | prompt | reference_images`; `id`'s cell comment now mentions it can mirror the `stt` number for convenience.
  - Not built: no in-app "renumber STT" helper or auto-sync between `stt` and `id` — they're independent columns by design (extension never reads `stt`), so keeping them in sync is on Đức, same as any other manual spreadsheet column.
- 2026-08-23 · Claude · Revised "Nhập prompt nhanh" per live feedback: Đức tried it and couldn't tell (a) whether the box took one prompt or several, and (b) whether the button ran immediately or just staged a preview. Two changes:
  1. **Multi-prompt input.** `splitQuickPromptText()` splits the textarea on a blank-line gap (`\r?\n[ \t]*\r?\n+`), matching the reference extension's convention Đức pointed to — a single `\n` inside one prompt never splits it, only an actual blank line starts a new prompt. `checkQuickPrompt()` loops over every parsed prompt and stages one job each (`createWorkbook` for the first ever, `addJob` for the rest, same as before).
  2. **Stage, don't run.** The button (`quickPromptCheckBtn`, was `quickPromptSubmitBtn`) no longer calls `run()` at all — it only adds jobs, pre-selects all of them (`state.runSelection = new Set(addedIds)`), expands the full queue, and switches to the RUN tab so they're immediately visible as real queue rows. Starting the run is now a fully separate, deliberate step through the existing Run controls (Start Run / "Chạy job đã chọn"), same as any workbook-sourced queue — removes the exact ambiguity Đức flagged.
  3. Added an "ⓘ" hover icon next to the card title (native `title` attribute, same lightweight pattern already used on Safety cooldown) explaining both the blank-line syntax and the stage-vs-run split, so the two points of confusion are answered in place instead of only in HANDOFF.
  - Tests: `quick-prompt-static.mjs` rewritten for the new behavior (8 new split-parsing cases: mid-prompt `\n` doesn't split, blank line does, multiple/whitespace-only blank lines do, trimming, leading/trailing blank sections don't create empty jobs) plus assertions that `checkQuickPrompt()` never calls `run()`. Hit the same cross-realm `assert.deepStrictEqual` gotcha as before (vm-context arrays fail identity checks even with identical contents) — fixed by spreading into plain arrays before comparing, not by weakening the assertions. `npm test`: still 43 pass (replaced, not added, since this is a revision of the same feature).
  - Not touched: pilot-03 / pilot-05 / pilot-06 evidence.
- **Next:** still needs a live pass (this revision hasn't been clicked in the real side panel any more than the first version had) — paste 2+ prompts separated by a blank line, click "Kiểm tra & thêm vào hàng đợi", confirm it lands on the RUN tab with that many new rows already ticked and NOTHING has started running yet, then confirm "Chạy job đã chọn" is what actually starts them. Tree is at 43/43 tests passing and **uncommitted** — ask before committing.
- 2026-08-23 · Claude · Two Pause/Resume reports investigated: (1) pressing "⏸ Tạm dừng" never visibly turned into a Resume button; (2) resuming appeared to "restart from the beginning of the queue" instead of continuing from the latest completed job.
  - **(1) confirmed and fixed — a real design defect, not a rendering bug.** The label was `state.paused ? "▶ Tiếp tục" : state.pauseRequested ? "⏸ Đang dừng sau job hiện tại…" : "⏸ Tạm dừng"` — three states, and it only reached "Tiếp tục" once `state.paused` became true, which happens only *after the in-flight job finishes* (by design: pause never interrupts an attempt already sent — exact-once). If that job takes minutes, the button sits on "Đang dừng sau job hiện tại…" the whole time with no further feedback, reading as broken. Fixed by making the label follow `state.pauseRequested` (the operator's intent) immediately, not `state.paused` (whether the hold has physically started yet) — a plain two-state toggle, ⏸ Tạm dừng ↔ ▶ Tiếp tục, flips the instant the button is clicked. `state.paused` still exists and still gates the actual hold in `waitWhilePaused()`; only the button's *label* changed. Added a `title` tooltip on the button spelling out both that the in-flight job still finishes normally and that a pause is a same-session hold, not durable across closing the side panel (see next point).
  - **(2) could not be reproduced as a code defect after a careful re-read of `run()`'s loop** — `waitWhilePaused()` is checked at exactly two safe boundaries (before a job is gated, and right after a job's terminal status is recorded), the enclosing `for` loop's `runIndex` is untouched by a pause, and `selectQueue()` already excludes any job already `SUCCESS` from every run mode, so continuing the *same* `run()` call after `waitWhilePaused()` returns cannot resubmit an already-completed job. Working theory instead: with (1) unfixed, the button gave no visible confirmation, so the natural next move was to press **Stop** (which *does* interrupt the in-flight job, marking it `STOPPED`/interrupted rather than `SUCCESS`) or reload rather than wait for the real Resume — and starting a fresh run afterward correctly reruns only that interrupted job onward, which reads as "back to the beginning" if it happened to be an early job. Deliberately did **not** add a checkpoint-on-pause to make a hold durable across closing the panel: `saveLedger()` replaces `state.workbook` with a freshly re-parsed object, and the in-flight `run()` loop's `runQueue` array holds `item.job` references into the *old* workbook — checkpointing mid-loop without re-deriving `runQueue` from a fresh `prepare()` afterward would silently corrupt every subsequent `updateJob()` call for the rest of that run. Fixing that properly means restructuring the loop to re-fetch its remaining queue after every checkpoint, which is real surgery on the run loop and wasn't worth the risk for a symptom that has a simpler, now-fixed explanation. The tooltip says plainly instead: a pause is a same-session hold — nothing is lost if the panel stays open, but closing it while paused loses whatever this run hasn't saved yet, same as Stop.
  - Test: `pause-resume-static.mjs` updated for the two-state label; added an assertion that the button's title explains the same-session-only scope.
  - Not touched: pilot-03 / pilot-05 / pilot-06 evidence.
- **Next:** Đức retests with the fixed button: pause mid-run, confirm the label reads "▶ Tiếp tục" immediately on click (not after the current job finishes), then click it and confirm the run continues with the *next* job, not job 1. If a genuine restart-from-job-1 still happens with the label now correctly flipped and Resume (not Stop) actually clicked, that would mean (2) is a real separate defect after all — in that case note exactly which job was active when Pause was pressed, since that's the detail needed to keep digging. Tree is at 43/43 tests passing and **uncommitted** — ask before committing.
- 2026-08-23 · Claude · **New: capture GPT/Gemini-style daily image-generation quota limits and halt the batch there, per Đức's request** ("để dừng đúng lúc, không bị override hoặc là bị gửi prompt đè lên"). Modelled directly on the existing `securityBlockerText()` / `SECURITY_HARD_STOP` mechanism, but kept as its own distinct classification throughout rather than folded into "security" — the operator should be able to tell a quota wall from an actual security block.
  - `content.js`: `matchesGenerationLimit(text)` / `generationLimitText()` — deliberately scoped to a *specific* assistant message, not the whole page like `securityBlockerText()`. A CAPTCHA interstitial isn't tied to one chat turn, so page-wide scanning is right for it; a quota message IS one specific response, and scanning the whole page would risk false-firing on the operator's own prompt text if it happened to contain the same common words ("draw someone waiting for their daily limit to reset") — the same false-positive class of bug the security blocker had before. Wired into three places: (1) `waitForCompletion()`'s poll loop, checked only once the current attempt's own response has finished streaming, never mid-generation; (2) `waitForChatReady()`'s pre-submit gate, on both the initial read and the final re-check before READY is trusted — this is the actual "don't submit the next prompt on top of it" gate; (3) `DAC_PING`, so a limit already in effect is caught by Check Plan / `authoritativeValidate()` before a run even starts.
  - `chat-readiness-core.js`: `evaluate()` now treats `generationLimitBlocker` as a `HARD_STOP` alongside `securityBlocker`.
  - `runner-core.js`: new `GENERATION_LIMIT_REACHED` failure type, classified from the `LIMIT_STOP:` prefix (parallel to `HARD_STOP:`). Never auto-retried (`PRE_SUBMIT_RETRYABLE_FAILURES` excludes it) and excluded from `INTERRUPTED` in `interruptedStatus()`, same as `SECURITY_HARD_STOP`/`USER_STOP`.
  - `sidepanel.js`: added to the run-loop's halt condition (`halted = true`, same tier as a security hard stop and `RECEIVER_LOST`) and to `authoritativeValidate()`'s pre-run ping check. Check Plan gets its own finding, `CHATGPT_GENERATION_LIMIT`, with a Vietnamese entry in `operator-messages-core.js` ("ChatGPT đã hết lượt tạo ảnh hôm nay").
  - **Honesty note, same as every other detector built on scanned page text:** the phrase list in `matchesGenerationLimit()` is a best-effort starting set based on common patterns, not confirmed against a real rate-limited ChatGPT session — that can only be verified live, by actually hitting the limit. If Đức hits a real quota wall and the batch does **not** halt, capture ChatGPT's exact wording and it gets added, the same evidence-based way the CAPTCHA phrase list was built.
  - Test: `generation-limit-smoke.mjs` (phrase matching including negative cases so an unrelated prompt/response doesn't false-fire; readiness/classification/halt wiring across all four files). `npm test`: 44 pass (was 43).
  - Not touched: pilot-03 / pilot-05 / pilot-06 evidence.
- **Next:** this cannot be verified without an actual quota-limited ChatGPT session — Đức is the only one who can produce that condition. When it happens live: confirm the batch halts immediately (no further job's prompt gets submitted), the halted banner/log shows `GENERATION_LIMIT_REACHED` or `CHATGPT_GENERATION_LIMIT`, and Check Plan reports the same before a fresh run is even attempted. If it does NOT halt, send the exact ChatGPT wording seen at the time. Tree is at 44/44 tests passing and **uncommitted** — ask before committing.
- 2026-08-23 · Codex · **OWNER QUEUE EDITING REQUEST — IMPLEMENTED:** Đức approved one unified Queue-management pass: Duplicate, safe Remove, deterministic Reorder, plus `Tick tất cả` / `Bỏ tick tất cả`. Each row now has a visible `⧉ Nhân bản` action; expanding the row exposes `↑ Lên`, `↓ Xuống`, and `Bỏ khỏi Queue`. Duplicate creates a genuinely new ID immediately after the source and copies only input fields (`prompt`, references, and per-job runtime overrides), with `duplicate_of` provenance; attempt/result/status/history are never copied. Reorder uses buttons rather than drag-and-drop and is limited to adjacent pre-submit jobs, so terminal/submitted rows remain fixed. Remove requires confirmation and is a ledger tombstone, not deletion: the physical XLSX row remains with `queue_removed=true` / `queue_removed_at`, while active execution order is persisted in `queue_position`. `runner-core`, Resume planning, and Plan Diagnostics consume `DacXlsx.activeJobs()` so removed jobs cannot run and logical order survives reopen; Resume identity and ledger uniqueness still inspect every physical row. All edit actions lock while Run/reconciliation/recreate/audit-gap work is active. Bulk tick operates on the full active Queue (including rows hidden by the six-row collapsed view) and selects only eligible pre-submit jobs. Regression coverage includes physical-row retention, persisted ordering, Runner exclusion, Resume ordering/counts, and UI wiring. Test suite: **44/44 PASS**, `git diff --check` clean; changes remain uncommitted.
- 2026-08-23 · Codex · **OWNER HALT INSTRUCTION REQUEST — IMPLEMENTED:** Đức asked how many Halt types exist and requested an in-app `ⓘ Halt` instruction. The runtime declares 16 canonical `Failure Types`, but the operator guide intentionally presents **10 Halt groups**: two codes are not root Halt causes (`USER_STOP` is an operator action; `INTERRUPTED` is a resulting status), while related codes such as `POST_SUBMIT_UNCERTAIN` / `TIMEOUT_AFTER_SUBMIT` and four pre-submit failures are grouped by one operator decision. Added `halt-instructions-core.js` as the single taxonomy source: every group keeps exact English App codes/titles, with Vietnamese meaning, Auto-retry policy, and next action. `OUTPUT PERSISTENCE FAILED` is displayed separately because it is a Run-level Result XLSX/Audit JSONL artifact error, not a job Failure Type, and may appear together with another Halt. The always-visible `ⓘ Halt` control sits beside Run progress and opens a bounded, scrollable dialog; Escape and Đóng both close it. A coverage test proves all 16 canonical Failure Types are accounted for exactly as a Halt cause or explicit non-Halt code. Test suite: **45/45 PASS**, `git diff --check` clean; changes remain uncommitted.
- 2026-08-23 · Codex · **OWNER QUEUE DRAG/DROP REQUEST — IMPLEMENTED:** Đức requested manual Queue placement because a duplicated job needs a new unique ID but its correct run position depends on context. Every editable pre-submit row now has a visible `⠿` drag handle and before/after drop indicators; the footer explains `⠿ Kéo để sắp xếp`. Dragging changes only persisted `queue_position`: IDs remain unique/incrementing and status, attempt, result, provenance, and execution history remain unchanged. A pre-submit job may be placed before or after any active Queue row, including across completed/submitted rows, while completed/submitted jobs themselves are never draggable; Run, reconciliation, recreate, and audit-gap locks still disable mutation. The existing `↑ Lên` / `↓ Xuống` actions remain as deterministic keyboard/click fallbacks. Core persistence coverage verifies both before/after placement after XLSX reparse, and live browser QA reproduced the reported case: duplicate produced `Q001 → Q003 → Q002`, then drag/drop persisted the visible order as `Q001 → Q002 → Q003`; browser console had 0 errors and 0 warnings. Test suite: **45/45 PASS**, `git diff --check` clean; changes remain uncommitted.
- 2026-08-23 · Claude · **OWNER RETRY/HALT POLICY REQUEST — IMPLEMENTED:** Đức reported hitting `POST_SUBMIT_UNCERTAIN` on Q003 and asked why it hard-stopped the whole Run instead of pausing/resuming, and separately why configured retries never seemed to fire. Root cause of both: `canRetry()` only ever allowed a retry when `item.phase === "PRE_SUBMIT"`, so any post-submit failure (which is most real failures — this app submits first, then has to detect/attribute output) always skipped straight to a full-batch halt, no matter `max_retries`. Đức's explicit new priority (cost is not a concern): don't stop, retry, run the whole queue to completion, skip a job that still fails after retries — and only truly stop for CAPTCHA / quota / a genuinely lost ChatGPT tab (confirmed via `AskUserQuestion` that `RECEIVER_LOST` should stay a hard stop, since retrying with no tab would just fail every remaining job in the queue back-to-back for nothing).
  - **runner-core.js:** replaced `PRE_SUBMIT_RETRYABLE_FAILURES` (3 codes, pre-submit only) with `HARD_STOP_FAILURE_TYPES = {SECURITY_HARD_STOP, GENERATION_LIMIT_REACHED, RECEIVER_LOST}`. `canRetry()` now allows a retry for any failure type outside that set (any phase) while `retry_count < max_retries`. `interruptedStatus()` flipped to match: only a hard stop settles as `INTERRUPTED` (blocks Resume until a human resolves it); everything else settles as `FAILED` (safe to skip). `selectQueue("all", …)` now also excludes `status === "INTERRUPTED"` — previously an unresolved hard-stop job had no checkpoint yet, so `!item.skipped` let a plain "▶ START RUN"/"▶ CONTINUE RUN" silently resubmit it outside Resume Mode; this was the second, previously-undocumented half of what made a halt look like "start over from scratch."
  - **sidepanel.js:** the four separate failure sites (`gateNextJob`'s readiness wait, the submit response's attempt-id check, `finishDetectedOutput`'s two save/verify catches, `reconcileSubmittedAttempt`'s three non-success branches) each used to call `markInterrupted(...)` and hard-halt unconditionally. All now return/report their failure to one new shared `resolveJobFailure(item, failureType, message, settings)`: hard stop → `markInterrupted` + halt (unchanged); retryable → resets the item to `PRE_SUBMIT`, waits the existing retry cooldown, and loops back through `gateNextJob` to submit a genuinely fresh prompt attempt; retries exhausted → marks `FAILED` and the run loop's `for` continues to the next queued job (`halted` stays false unless `continue_on_error` is off). `gateNextJob` and `reconcileSubmittedAttempt` no longer decide halt/retry themselves — they just report `{ok:false, failureType, message}` and let `resolveJobFailure` decide, so the policy lives in exactly one place.
  - **resume-core.js:** `classify()`'s `SAFE_FAILED_PRE_SUBMIT` state (pre-submit-only, `failure_type`-gated) generalized to `SAFE_FAILED` — any `status === "failed"` job is safe to skip on continuation regardless of phase, because the runner now only ever reaches `FAILED` after deliberately exhausting retries on a non-hard-stop failure. `AMBIGUOUS_SUBMITTED` (the Resume-blocking state) is now reached only by a genuinely `INTERRUPTED` job — i.e. only the three hard stops — so a full-queue Resume block now correctly means "the environment itself is blocked" (CAPTCHA/quota/no tab), not "one job's output was merely uncertain." `summary.failed_pre_submit` renamed `summary.failed` throughout (`plan()`, `applyToQueue()`, `summaryText()`).
  - **Known, deliberate simplification — told to Đức, not yet asked for:** every retry is a full fresh prompt resubmission, even for `READINESS_TIMEOUT_AFTER_SAVE` where an image was already saved before the failure. A successful retry there overwrites the job's *tracked* `result_file` with the new attempt's image; the first saved file is left on disk, just no longer the one referenced in the ledger. Retrying only the readiness-wait step (leaving a confirmed save alone) would be more surgical but needs the run loop to re-enter mid-`finishDetectedOutput` rather than from `gateNextJob`, which was out of scope for this pass.
  - **halt-instructions-core.js:** rewritten to English-first with a Vietnamese explanation/suggestion in parentheses after each sentence, per Đức's explicit request to learn the English terms while using the tool (previously Vietnamese-first, English only in codes/titles). Same 10 groups, same codes, same titles — only `retry`/`meaning`/`action` content changed, so `halt-instructions-core-smoke.mjs`'s structural assertions (group count, unique titles, `findInstruction("POST_SUBMIT_UNCERTAIN").title`) needed no changes.
  - Updated the two queue-row "Auto-retry: No" badges (`sidepanel.js`) to a new `retriesExhausted(item)` helper (`retry_count >= max_retries` or a hard-stop `failure_type`) instead of the stale `phase !== "PRE_SUBMIT"` check, so a post-submit job that's still mid-retry no longer shows a wrong "No" badge.
  - Tests updated for the new policy (not weakened — they now assert the new intended behavior): `p1-attempt-state-smoke.mjs`, `generation-limit-smoke.mjs`, `v03-operational-core-smoke.mjs`, `sidepanel-output-location-static.mjs`, `resume-core-smoke.mjs`. Full suite: **48/48 files pass** (`run-all.mjs` internal bundle 46/46 plus the two run separately), `node -e` syntax-checked all four touched core files. `git diff --check` not yet run this pass.
  - Not touched: pilot-03 / pilot-05 / pilot-06 evidence. `operator-messages-core.js`'s `RESUME_AMBIGUOUS_SUBMISSION` guidance text left as-is (still accurate: it now specifically means a hard-stop-interrupted job, a narrower case than before, but the guidance — don't resubmit, review manually, Recreate if needed — still holds).
- **Next:** Đức retests live: reproduce (or wait for) a `POST_SUBMIT_UNCERTAIN`/similar mid-queue and confirm the batch does NOT stop — the job retries a few times, then either succeeds or gets skipped as `FAILED`, and later jobs keep running. Separately confirm a real hard stop (CAPTCHA, or Setup a fake `RECEIVER_LOST` by closing the ChatGPT tab mid-run) still stops the whole batch and that reopening the tab + Check Plan + Continue Run picks it back up. Tree is at 48/48 tests passing and **uncommitted** — ask before committing.
- 2026-08-23 · Claude · **AGENT BRIDGE phase opened — WP-0 design + WP-1 core landed, WP-2..WP-4 handed to Codex.** Đức asked to research "code talking to the extension" (the OpenAI Codex-extension architecture study) and to have Codex do the building. Coordinated 2 Codex rounds. Verdict on the prior study: ALIGN_WITH_FIX. Native Messaging **rejected** as production transport — Chrome launches the host as its own stdio child, so a separate CLI cannot attach and would need a second IPC hop; chose a token-authenticated `127.0.0.1` loopback host instead. Two claims from the earlier reasoning were corrected by evidence: (a) a `FileSystemDirectoryHandle` *can* be used from an MV3 service worker and is IndexedDB-serializable (`output-profile-core.js:10-41` already stores one) — so "side panel = sole executor" is now recorded as a deliberate **product invariant** (picker needs a document + gesture; `xlsx-codec.js` needs DOM XML; splitting file writes from the ledger would split artifact truth), not an API limitation; (b) the legacy localhost API needs a *browser page* in Chrome, so "any local process can drive it" was slightly overstated — it is still unauthenticated ingress and WP-4 removes it. `run.start`/`run.pause`/`run.resume` deliberately omitted from v1: the bridge is ingress + observability, not remote execution. External code may only stage a **quarantined proposal**; Đức's click in the side panel is required before any queue mutation, and approval still does not start a run. Also overrode Codex's choice of a .NET host — no .NET SDK on this machine and the repo is deliberately dependency-free Node — recorded as a dated reversal in the design doc rather than a silent rewrite. Delivered: `drafts/AGENT-BRIDGE-DESIGN-V1.md` (761 lines, design of record), `bridge-core.js` (605 lines, verified pure — no `chrome.`/DOM/network/`node:` refs, loads in worker + panel + Node), 2 new tests. Independent adversarial review of WP-1 found one real defect, written up for the next round: `requireMethod()` reads `METHOD_REGISTRY[method]` on an `Object.fromEntries` object, so `constructor`/`toString`/`__proto__` resolve as registered methods — not reachable today because `parseEnvelope` requires a dotted name (`bridge-core.js:405`), but it becomes live in WP-2 when the executor Port adds a second entry path. Tests: **48/48 PASS** + observer smoke; nothing committed. **Next:** Codex implements WP-2 per `drafts/AGENT-BRIDGE-HANDOFF.md` §2 (defect fix first, then the Vietnamese "Đề xuất từ Agent" approval card and the approval/checkpoint transaction).
- 2026-08-24 · Codex · **AGENT BRIDGE TIER 1 — IMPLEMENTED, STATIC PASS:** Added the six direct Setup RPC methods (`jobs.add`, `jobs.update`, `jobs.remove`, `jobs.reorder`, `output.configure`, `run_settings.configure`) as executor writes with `approval: "none"`. All handlers reuse the Setup/data-layer mutation paths, enforce the existing mutation locks and PRE_SUBMIT boundary, and persist one distinguishable audit event plus one immutable Result checkpoint through `DacApprovalPersistence.execute()` per successful call. `jobs.add` can bootstrap an in-memory workbook; `output.configure` fails closed unless Đức already bound the output location. Added the fourth BRIDGE tab with connection/pairing status, a newest-first passive direct-mutation activity feed, and the unchanged `queue.propose` owner-approval card relocated into it. `queue.propose` remains owner-click gated; `run.start`/`run.pause`/`run.resume` remain absent and no handler invokes Run. Added six method smoke tests and one BRIDGE-tab static test; full suite **70/70 PASS** plus observer-engine smoke PASS, syntax checks PASS, and `git diff --check` clean. Protected pilot evidence was not touched by this pass; unrelated pre-existing working-tree changes remain. No commit. **Next:** reload the unpacked extension at `chrome://extensions`, then Claude performs the independent source audit before Đức's paired-extension live acceptance. Checkpoint-per-mutation is correct for immutable evidence but may create noticeable version churn during many small agent edits; any future batching needs a separate owner decision.
- 2026-08-24 · Claude · Catch-up verification after the Tier 1 audit session: tree unchanged and still **70/70 PASS** (re-ran `npm.cmd test`), still uncommitted. Bridge host was found not running and was restarted from the installed copy; CLI `ping` then showed extension online, executor available, ChatGPT READY, Pilot-09 workbook loaded. However `system.capabilities` still returns only the old 8-method registry — the six Tier 1 methods exist on disk (`bridge-core.js`) but **the running extension has not been reloaded yet**, so live CLI testing of `jobs.*`/`output.configure`/`run_settings.configure` is blocked on the `chrome://extensions` reload. Note for that test pass: the installed `bridge-cli.mjs` only has subcommands for the old surface (ping/capabilities/queue-list/run-status/ledger-read/proposal-get/propose) — the six new methods will be exercised with raw protocol envelopes over the same pairing, no reinstall needed.
- 2026-08-24 · Claude · **INDEPENDENT WHOLE-SYSTEM AUDIT COMPLETED** per Đức's request (effectiveness of interaction/read/signal/control, plus a roadmap toward autonomous agent operation). Method: 4 parallel independent code surveys (bridge transport, run-loop/DOM, evidence layer, test infrastructure), every finding evidenced at file:line, cross-checked before synthesis. Report: `drafts/AUDIT-SYSTEM-EFFECTIVENESS-2026-08-24.md`. Verdict: **effective and safely built for the current owner-operated mode; not yet ready for autonomous agent operation.** Top findings — (A) correctness: mid-run crash loses the whole run's ledger+audit and makes submitted jobs re-submittable (audit/ledger only flushed in `run()`'s `finally`); the 6 Tier-1 methods are non-idempotent while the protocol instructs retry (duplicate jobs); `securityBlockerText()` scans the whole page including the job's own prompt (false CAPTCHA halts — the quota detector was already fixed for exactly this class); `classifyFailure` order makes every RECEIVER_LOST report as SECURITY_HARD_STOP; `new_visible_fallback` can attribute an unrelated page image as verified output; lock-after-await races in `approveBridgeProposal`/`run()`; the WS handshake never authenticates the host to the extension (port-squat gets token + full Tier-1). (B) efficiency: one mutation = 2 workbook clones + 3 zips + full audit-file rewrite + directory scan + 1 new checkpoint (an agent editing session exhausts 2-digit versions); O(N²) sheet serialization caps practical scale ~100–200 jobs; STORED (uncompressed) ZIP; 45–60% of run wall-clock is fixed waiting incl. a double cooldown; failed jobs skip the inter-job delay entirely. (C) autonomy gaps: polling-only signaling with no revision token; agents cannot read back checkpoints/audit to verify their own writes; CLI lacks Tier-1 subcommands/`--request-id`/timeout; host unsupervised (found dead today); `RUN_ACTIVE` marked retryable for human-only config states; `content.js`+`sidepanel.js` at 0% execution coverage with a 10-item human live-test backlog as the real bottleneck; evidence chain is tamper-transparent (no seq/hash chain); 2 docs stale vs Tier-1 reality. Roadmap: 5 phases (0 chốt nền → 1 vá móng an toàn → 2 Bridge agent-grade → 3 tự validate qua Chrome-thật harness → 4 tối ưu quy mô/throughput → 5 vòng tự hành), with exactly **5 owner decisions** enumerated in the report (commit Tier-1; amend AGENTS.md rule 8 for a real-Chrome harness; checkpoint batching; checkpoint retention; agent commits to a work branch only). No code changed in this audit pass.
- 2026-08-24 · Claude · **ĐỨC CHỐT CẢ 5 ĐIỂM ROADMAP**, with one broadening: decision #5 becomes *full AI commit rights including main* (not just a work branch), under 4 safeguards recorded in `decisions.md` — tests green before any commit, never force-push/rewrite history, every commit gets a HANDOFF Log line, and file deletion / pilot evidence / the Run boundary still require Đức. Recorded all 5 decisions in `decisions.md` (new "Roadmap tự hành" section + a SUPERSEDES row on the old no-commit rule); amended AGENTS.md golden rules 6 (commit) and 8 (real-Chrome harness now allowed; in-app preview still banned). Explained "harness" to Đức in plain terms (real Chrome + real extension driven by code against a mock chatgpt.com page).
- 2026-08-24 · Claude · Post-reload live test of Tier 1 started: capabilities now serves all 14 methods (6 Tier-1 present, run.* still prohibited). First `jobs.add` correctly **failed closed** with `PERSISTENCE_VERIFICATION_FAILED` — Chrome revoked the output-folder permission on extension reload, and a mutation with no verified persistence refuses to apply. Live confirmation of two audit findings at once: fail-closed persistence works, and the error is mislabeled `retryable: true` (a human-only condition — Giai đoạn 2 fix).
- 2026-08-24 · Claude · **BRIDGE tab is now the single AI↔owner interaction hub, per Đức's direct request** ("mọi tương tác cần người làm cùng nằm riêng trong BRIDGE, khỏi chạy qua chạy lại"). Built: a "CẦN THAO TÁC CỦA ĐỨC" card in `bridgeScreen` + a red count badge inside the BRIDGE tab button visible from any screen. Every executor-handler failure now funnels through `bridgeAttentionFromError()` (hooked in `withBridgeErrors`), which maps four human-gesture block classes to actionable rows: `PERSISTENCE_VERIFICATION_FAILED` → "Chọn lại folder output" button reusing `openFolderPickDialog()` (the picker's user gesture originates from the BRIDGE tab click, so it is valid); `VALIDATION_FAILED`+`OUTPUT_PROFILE_UNBOUND` → first-time folder bind; `WORKBOOK_NOT_LOADED` → "Mở SETUP"; `RUN_ACTIVE` with the persistence lock reason → "Mở SETUP". Rows clear themselves: any verified direct-mutation commit clears all (success is proof the block is gone), `requireBridgeWorkbook()` success clears the workbook row, and each row has a manual "Ẩn". DOM built with `element()`/`replaceChildren` — no HTML sinks. New `tests/bridge-attention-static.mjs`; `bridge-tab-static.mjs`'s exact-markup regex relaxed to allow trailing children in the tab button (deliberate, documented in-test). Suite: **71/71 PASS**.
- 2026-08-24 · Claude · **186 MB extension-size question answered with measurement, split-into-two-extensions rejected.** Total folder = 185.9 MB, but actual loaded code (js/html/css/json) = **0.64 MB**; the rest is pilot evidence data living in the same directory (Pilot-08 alone 130.9 MB of images; pilot-03/05/06 + Pilot-09 ≈ 54 MB). Chrome loads only the declared code files — the images are inert bytes on disk, so there is no runtime/memory cost and no danger. Splitting Bridge into a second extension would not shrink anything (~0.1 MB of bridge code), would double maintenance/pairing, and would break the "side panel is the sole executor" invariant — a second extension cannot reach the first one's state. Correct hygiene instead: **from Pilot-10 onward, bind output folders OUTSIDE the extension folder** (e.g. `C:\WORKING ZONE\DucAuto-Output\<pilot>\`); existing pilot folders stay untouched (protected evidence).
- 2026-08-24 · Claude · **New design principle recorded (Đức): "AI là bộ não, người dùng là cánh tay."** Every human-gesture blocking point must arrive pre-chewed — the exact one-click action in place, paths with Copy buttons, one-line guidance, all centralized in the BRIDGE tab. Recorded in `decisions.md`. Applied immediately: (a) a pending proposal blocked on a human-only precondition now raises its attention row proactively from `renderBridgeProposals()` (Đức caught this gap live: a stale proposal sat blocked on "Chưa có workbook" while the attention hub stayed silent — the agent may never retry, so waiting for a failed call was wrong); (b) the WORKBOOK_NEEDED row now opens the real file pickers in place (`resumeWorkbookInput`/`workbookInput` `.click()` — the row's own click supplies the user gesture) instead of pointing at SETUP. Clarified while wiring: "Continue Existing Run" is a **file** input (Result XLSX) and does NOT grant folder write permission — folder re-auth is always its own gesture via the folder row. Suite: **71/71 PASS** (bridge-attention-static extended).
- 2026-08-24 · Claude · Per Đức's live feedback, attention rows now carry a **target-path suggestion with one-click Copy**: `raiseBridgeAttention(code, detail, suggestion)` renders the path as selectable `code` text + a Copy button (clipboard with Ctrl+C fallback), so the AI visibly names exactly which folder/file it is working against. Honest limitation handled: the File System Access API never exposes absolute paths, so the full path exists only when a workbook carried `output_folder_hint`; after a fresh reload with no workbook, `bridgeTargetFolderSuggestion()` falls back to the bound profile's folder NAME ("Tên folder đã bind: …"). Also answered Đức's question about the lingering "ĐỀ XUẤT TỪ AGENT" card: it is Claude's own stale connectivity-test proposal (`bridge-accept-001`), durable by design until the owner decides — reject it with "Từ chối". **Backlog for Giai đoạn 2: `queue.proposal.withdraw` so an agent can retract its own stale proposals.** Suite: **71/71 PASS**.
- 2026-08-24 · Claude · Two live bugs from Đức, both real, both fixed: (1) **the folder-reauth row never appeared** because attention rows were only raised by a *failed agent call* — after a reload no call has run yet, so the hub stayed silent about the revoked permission. New `probeBridgePersistence()` runs on BRIDGE-tab refresh and after every folder pick: it checks the bound directory's real permission via `DacOutputLocation.preflight` (queryPermission — promptless), raising or clearing `FOLDER_REAUTH_NEEDED` on its own. (2) **"Ẩn" deleted the row with no way back** — dismissal is now a `dismissed` flag, the badge counts only visible rows, and a "Hiện lại N việc đã ẩn" button restores them; a re-raise deliberately does NOT undo a dismissal (the proposal renderer re-raises every paint and would otherwise fight the user's click). Only `clearBridgeAttention()` (block actually resolved) removes rows. Suite: **71/71 PASS** (bridge-attention-static extended for probe + recoverable dismissal).
- 2026-08-24/25 · Claude · **Root cause of the "vẫn bị ẩn" reports found and fixed, with the new mandatory cross-check round proving itself on its first run.** Root cause: `state.outputSettings` is only built on workbook load, so after a reload with no workbook (a) `probeBridgePersistence()` had nothing to probe and stayed silent, (b) the folder-pick button would have crashed on a null deref, and (c) panel init never rendered the attention badge at all — the row could never appear no matter how many reloads. Fixes: probe falls back to the IndexedDB profile store (`DacOutputProfiles.list()` added to `output-profile-core.js`) and checks each stored profile's real permission; `choosePrimaryDestination()` runs the picker first (gesture safety), reuses the sole stored profile's id when no session context exists, and initializes `outputSettings` from defaults when null; init now calls `renderBridgeAttention()` + the probe. **Cross-check round (per the new decisions.md process):** Antigravity CLI (`agy`) cannot run headless here — non-interactive mode auto-denies its own tool permissions and the bypass flag is blocked by Claude Code's safety layer — so Codex is the working auditor; its own Windows sandbox is also broken (`apply deny-read ACLs`, pre-existing), worked around by inlining the git diff into the prompt. Codex round 1: **FAIL with 2 real findings** — (1) one authorized profile suppressed another profile's revocation (early-break bug), (2) the probe ran after `bridgeSystemPing()` so a rejected ping skipped it. Both fixed; Codex round 2: **RE-VERDICT PASS**, no new defects (its residual note about `resolve()` throwing is moot — `resolve()` catches internally and never throws). Suite: **71/71 PASS**; static test now locks probe-before-ping and no-early-break.
- 2026-08-25 · Claude · **One-click-copy REAL folder paths, double cross-audited.** Đức reported the suggestion copied only profile names ("pilot-05, pilot-06, …"), not a pasteable path. Chrome never reveals a handle's absolute path, so the fix persists the workbook-authored `output_folder_hint` onto the IndexedDB profile record (`last_known_folder_hint`, new `DacOutputProfiles.setHint`) at workbook resolution; post-reload the attention row then offers the real path per profile, one Copy button per path. Codex cross-audit round 1: **FAIL, 4 real findings, all fixed** — picker no longer stamps hints onto an arbitrarily picked folder (false-path risk); hint snapshotted before the resolve await (profile/hint affinity); `setHint` rewritten as one readwrite transaction (lost-update race); per-line copy buttons (a multi-path blob pastes as garbage). Two findings deliberately accepted: fire-and-forget persistence (best-effort by design) and empty-hint-means-leave-unchanged. **Antigravity re-audit (first successful headless run, with Đức's explicit approval of `--dangerously-skip-permissions` + review-only prompt + plan mode): RE-VERDICT PASS**, all 4 fixes confirmed, no new defects; its one defensive suggestion (optional chaining on `importedConfig?.effective?.output`) applied. Đức's standing instruction saved to Claude's long-term memory: always run the multi-AI audit loop (test → independent audit → fix → re-audit PASS) before handing anything to Đức; agy is now a usable second auditor. Suite: **71/71 PASS**. Note: real paths appear after each pilot's workbook has been loaded once (that's when the hint gets persisted); until then the row shows folder names with an explanatory fallback.
- 2026-08-25 · Claude · **Owner workflow definition recorded (Đức): "chuẩn bị task" = AI delivers a complete package.** When Đức asks an AI to prepare a task: read his input folder of complex prompt templates → break down into jobs → build the run XLSX (config carries `output_folder_hint` with the real absolute path) → create folders, name files, wire references → and the AI feeds every folder path into the Bridge itself — Đức never hunts for a path the AI just created. Saved to Claude's long-term memory. **This upgrades the Giai đoạn 2 backlog item from nice-to-have to required:** an agent-writable folder-hint channel over the Bridge (a `profiles.set_hint`-style method and/or `output_folder_hint` params on `jobs.add`), so real paths are available in the attention rows even before any workbook loads. To implement immediately after the pending Tier-1 commit lands — not folded into the current uncommitted scope.
- 2026-08-25 · Claude · **Commits landed under the new full-autonomy policy** (Đức reaffirmed: AI commits and pushes proactively): `c28600d` (BRIDGE attention hub, 9 files) and `bb16a92` (**`output.set_folder_hint`** — the agent-writable folder-path channel, 15th registry method, `approval: "none"`, display-metadata only: no workbook write, no checkpoint, deliberately NOT routed through `executeBridgeDirectMutation` so it works while folder permission is revoked). Validator enforces absolute Windows path shape (drive-rooted or UNC `\\server\share`) and rejects C0/C1 controls plus bidi/zero-width/invisible formatting chars (display-deception vector on a copy-paste field). Cross-audited per the one-auditor-per-round rule (Đức: never run both auditors — usage economy): Codex round 1 FAIL (handler missing from payload — extraction bug on Claude's side — plus 2 real validator gaps), round 2 handler PASS + the two validator blockers fixed with the auditor's own prescribed patterns, locked by `bridge-output-set-folder-hint-smoke.mjs`. Also survived this session: the Edit tool interprets `\uXXXX` in replacement text — it embedded a literal NUL into bridge-core.js once (git saw a binary file); all escape-bearing edits now go through Node scripts. Both pushed to `origin/main`. Suite: **72/72 PASS**.
- 2026-08-25 · Claude · **`output.set_folder_hint` proven live end-to-end:** after Đức's reload, Claude drove the whole flow over the Bridge with zero human steps — the deliberate `PROFILE_AMBIGUOUS` listing revealed the stored profile ids (`pilot-05`, `pilot-06`, `pilot-09`), and three calls injected the real absolute paths; the handler's probe refresh updated the attention rows in place, no reload needed. The earlier "copy still shows the fallback" report was sequencing, not a bug — Đức checked before the injection step ran.
- 2026-08-25 · Claude · **Đức's "conflicting proposal card" report uncovered a real platform-level bug: `el.hidden = true` silently did nothing on every button.** The global `button { display: inline-flex }` author rule overrides the UA's `[hidden] { display: none }` — author rules always beat UA rules regardless of specificity — so the Reject/Approve buttons stayed visible on an empty card (and the tab attention badge span, with its own display rule, could never be hidden either). Two older local `[hidden] { display:none !important }` rules (`destination-authorized`, `queue-row-details`) were piecemeal patches of this exact trap. Fixed globally with `[hidden] { display: none !important; }` at the stylesheet root, plus the static approval-explainer paragraph (`bridgeProposalNotice`) now hides in the empty state. Cross-audit (Codex, single round per the usage-economy rule): **PASS** — verified every `.hidden` assignment in the codebase means do-not-display, no conflicting display rules, native `<dialog>` (open attribute) unaffected. Commits `427fab5` (+ `bb16a92`, `c28600d` earlier) pushed to `origin/main`. Suite: **72/72 PASS**.
- 2026-08-25 · Claude · **TIER-1 LIVE ACCEPTANCE: ALL 6 DIRECT METHODS PASS ON THE REAL PAIRED EXTENSION.** Sequence on the Pilot-09 session (resumed from `v01`, audit gap acknowledged by Đức): `run_settings.configure`→v04, `output.configure`→v05 (used for a real purpose: reset `audit_filename` to the canonical name, clearing the legacy of the 2026-08-24 naming bug baked into v01's config), `jobs.add`→v06 (Q001), `jobs.update`→v07, `jobs.reorder`→v08, `jobs.remove`→v09 (tombstone). Every call returned `verified: true` with one new immutable checkpoint; disk shows `v02..v09` plus the audit JSONL carrying one distinguishable `BRIDGE_*` event per mutation. Also captured live along the way: (a) the earlier CHECKPOINT_VERSION_CONFLICT attempt's audit event **is** in the JSONL although its checkpoint rolled back — live confirmation of the evidence-layer finding that audit persistence isn't transactional with the checkpoint (Giai đoạn 1 scope); (b) a stray `Duc-Auto-ChatGPT-Pilot-09__audit.jsonl.xlsx` (2.2KB) exists — the audit-gap segment wrote to the buggy name from v01's config before `output.configure` fixed it; harmless, left in place (deleting is Đức's call); (c) the two INTERRUPTED jobs from 2026-08-24 correctly block resume-Run until per-job Recreate — Claude misstated this to Đức as "only blocks those jobs" and corrected it: they block the session's Run entirely, by design. **Tier 1 is now fully live-verified; Giai đoạn 0 of the roadmap is complete.**
- 2026-08-25 · Claude+Codex · **GIAI ĐOẠN 1 SHIPPED — all eight safety-foundation fixes (A1–A8), commit `468437c` pushed.** Codex implemented per `drafts/GIAI-DOAN-1-SAFETY-HANDOFF.md`; Claude verified independently (re-ran suite: **73/73 + observer PASS**; protected pilots untouched; deep-read the risky hunks). Highlights: A1 submission reservation + verified pre-send checkpoint (fail-closed: persist failure ⇒ prompt NOT sent) + `checkpoint_interval_jobs` (default 1) + `rebindQueueRows` after every mid-run checkpoint + incremental audit flush; A2 idempotency for all 7 Tier-1 mutations (replay cap 500); A3 CAPTCHA scan excludes user messages; A4 `RECEIVER_LOST:` typed prefix classified first; A5 attribution scoped to conversation root, unknown-role fallback removed; A6 `createQueueRunLock` latch — locks taken before any await, `run()` refuses during mutations; A7 mutual WS handshake (nonce → host HMAC proof → only then token) — **installed host at `%LOCALAPPDATA%` updated and restarted (pid 24740) to match; old ext↔new host and new ext↔old host both fail closed until Đức reloads**; A8 `.partial-vNN` quarantine + `CHECKPOINT_PARTIAL_ABANDONED` audit event. **Watch-items (non-blocking):** (1) A3 clones `document.body` every 300ms poll — perf on heavy chats + semantics widen to hidden text (a phantom `SECURITY_HARD_STOP` with no visible captcha would implicate this); (2) pre-send + interval checkpoints ≈ 2 checkpoint files per job — churn is the approved Giai đoạn 4 batching decision, not a defect. Duplicate host processes found and cleaned during the restart (two `bridge-host` nodes were alive — one from the installer shortcut, one from Claude's earlier manual start; manual starts must pass `--pairing`).
- 2026-08-25 · Claude · **Giai đoạn 1 live-verified after Đức's reload + folder re-auth:** A7 mutual handshake completes (new host pid 24740 ↔ new extension); A2 proven live (same request_id replay → cached response, altered payload → `REQUEST_ID_REUSED`); A4 proven live (inactive ChatGPT tab pings as `RECEIVER_LOST` with the reload-tab instruction, not as CAPTCHA). Pilot-09 hint re-injected for the fresh session.
- 2026-08-25 · Claude+Codex · **GIAI ĐOẠN 2A SHIPPED — commit `1fbd4ba` pushed, suite 80/80 + observer PASS (re-run by Claude).** All 7 items: (1) **`run.trial`** implements the dev trial-run decision — visible "Chế độ phát triển" toggle + DEV MODE badge (panel-only, storage-persisted, no bridge method can flip it), ≤2 explicitly selected jobs through the same `selectQueue("selected")` gate as the UI, ≥5 min between trials (timestamp persisted BEFORE settings mutate so a storage failure can't leave capped settings behind), timeout capped ≤90s with restore via `restoreRunOptions` on both failure and completion, accept-and-poll (`run.status` now reports `trial{trial_id, input_origin}`), `bridge_dev` origin stamped on every audit row, `run()` consumes a `prelocked` latch so the trial and a UI click can never double-enter; (2) persistence-missing lock → non-retryable `VALIDATION_FAILED`; (3) optional `if_ledger_etag` on Tier-1 writes; (4) batch update/remove/reorder (one checkpoint per call); (5) `queue.proposal.withdraw` (own-proposal only); (6) `profiles.remove` (IndexedDB record only, refuses bound profile); (7) CLI full subcommand surface + `--request-id`/`--client-id` + 40s timeout + exit codes 0/3/2, installed copy deployed. Claude's independent audit deep-read the `run.trial` handler + `run()` integration + restore paths — clean.
- 2026-08-25 · Claude · **Agent-supplied output location shipped (`da3ae83`) — the folder click is gone from the autonomous loop.** Đức relayed the Gemini-session discovery (the extension can't read local folders, but the AI can — so the AI feeds the location over the Bridge): `output.configure` now accepts `output_downloads_subfolder` (Downloads-relative only, via `chrome.downloads` — zero human gesture; `safeRelativeFolder` confines traversal/drive/UNC/homoglyph input, Antigravity review PASS per the new rotation rule); bootstrap `jobs.add` sessions default to Downloads mode (Quick Prompt shape) instead of laundering `preflight(null)` to INTERNAL_ERROR (`e5b7a32` + `da3ae83`). Absolute directories (e.g. Đức's named GPT base `C:\WORKING ZONE\DucAuto_GPT-Output`) still require the owner picker — Chrome physics. Decision recorded in `decisions.md`. Also amended per Đức: cross-checks only when warranted (security/run-loop/large implementations/real doubt), ONE auditor per round, ROTATE Codex↔Antigravity (memory `audit-before-handoff-workflow` updated).
- 2026-08-25 · Claude · **First `run.trial` fired for real (trial-bc46ef55): bootstrap + output retarget + trial acceptance all worked; the run halted fail-closed at the pre-send flush** — `run()` was resetting the session's artifact chain (auditFile/checkpointVersion/auditPersistedPayload) even though the bridge Setup mutations had already written v01/v02 + audit, so the flush collided with the session's own files (Chrome uniquified the audit name → verify failed → **prompt correctly NOT sent, zero quota spent**, Q001 INTERRUPTED, Q002 untouched). Root cause fixed in `41ab17b`: a session with existing artifacts continues its chain; only a genuinely fresh session resets. Also shipped this stretch: `b587246` own-download filename determiner (Chrome ignores blob filename suggestions on this machine — GUID names hit BOTH extensions all day; the determiner has final say; note the real Downloads root is `download.default_directory` = `Downloads\Phai sinh`); `e5b7a32`+`da3ae83` bootstrap Downloads defaults + agent-settable `output_downloads_subfolder` (Antigravity audit PASS); `ea1dc60` dev-gated `details.debug` diagnostics (found the real error in one shot after hours of laundering-blind debugging). **Owner clarification to encode (recorded, not yet implemented): trial pacing counts JOBS — one job per ~5 minutes — and one job may produce MULTIPLE images; the current engine attributes exactly ONE image per attempt, so multi-image jobs need an attribution upgrade (2B backlog) before that semantic is real.** Parallel Gemini session proved its closed loop 4/4 the same day.
- 2026-08-25 · Claude · **Đức called a temporary stop ("tạm thời stop") mid-flow.** Paused state: fix `41ab17b` committed+pushed but the extension has NOT been reloaded since, so the running panel still has the artifact-reset bug; the halted Pilot-10 bootstrap session (Q001 INTERRUPTED / Q002 PENDING, checkpoints v01–v03 + audit in `Downloads\Phai sinh\DucAuto_GPT-Output\Pilot-10_Trial-Tu-Hanh`) is disposable test debris — a fresh bootstrap session is cleaner than resuming it.
- 2026-08-25 · Claude · **SESSION CLOSE-OUT (Đức directed: write the handoff, start fresh in a new chat).** What happened after the pause was lifted:
  - **Trial round 2 blocked correctly by locks, then by a NEW discovery.** Đức ran a manual Quick session (`Quick-2026-08-25T07-31`, resumed to v05); my `jobs.add`/`output.configure` were refused with `RUN_ACTIVE` while his run was live — locks working as designed. His run then exposed the **A/B image poll**: ChatGPT sometimes answers ONE prompt with TWO images + "Which image do you like more?" (small **Skip** link top-right, two "Image N is better" buttons). Live consequences observed: Q001 → `OUTPUT_AMBIGUOUS`/FAILED (two fresh images, fail-closed held), Q002 → stuck `WAITING_READY` (an unanswered poll keeps the assistant turn open and **locks the composer** — Đức typed "1"+Enter and it would not send, disproving the answer-by-prompt idea live).
  - **Owner decision recorded (`decisions.md`):** the extension will AUTO-CLICK the poll — default random image 1/2 (config `click_1`/`click_2`/`skip`); poll-pending is a readiness blocker to clear automatically; both images of such a turn get saved as `__variant-01/02` under the owner's "one job may yield many images" semantic.
  - **Where the codebase actually stands:** commits through `41ab17b` are on `origin/main`, suite **81/81 + observer PASS**. The running extension has everything up to `b587246` (own-download filename determiner — proven live: Đức's Quick session artifacts have correct names) but **NOT `41ab17b`** (run-continues-artifact-chain fix) — a reload is required before any bridge-driven trial, or the first pre-send flush will halt again on a session that already has checkpoints.
  - Trial channel status: `run.trial` proven live to acceptance (`trial-bc46ef55`); a full pass has NOT yet completed. Owner trial semantics amended in shared memory meanwhile: ONE chain up to **30 jobs** (registry still caps `job_ids` at 1..2 — needs updating), pacing ≥5–6 min between trials, jobs may be multi-image.
- 2026-08-25 · Claude+Codex · **WAVE A/B-POLL SHIPPED — suite 84/84 + observer PASS, Codex audit PASS sau 6 vòng.** Toàn bộ mục 3 của Next trước:
  - **`ab-poll-core.js` (mới):** chính sách trả lời poll — neo theo CHỮ ("Which image do you like more?" / "Image N is better" / "Skip"), `ab_poll_action` = `random` (mặc định) / `click_1` / `click_2` / `skip`. Khi control cấu hình không có (ví dụ `skip` mà không có link Skip) thì **từ chối có tên** (`AB_POLL_UNANSWERED`) chứ không lặng lẽ bấm cái khác — bấm khác ý cấu hình sẽ làm audit nói dối.
  - **Click ở readiness gate, KHÔNG click lúc dò ảnh** (lệch có chủ đích so với brief, đã ghi `decisions.md`): lúc `waitForCompletion` chạy, ảnh của job vẫn còn trên màn và URL blob/CDN vẫn sống — bấm poll lúc đó có thể gỡ mất ảnh trước khi kịp ghi file. Gate chạy sau khi job đã xong bước output (đã lưu+verify, hoặc đã ghi `detected_not_downloaded`).
  - **An toàn đứng trên poll:** `pollInteractionBlocker()` chặn mọi click khi có CAPTCHA / hết quota / đang generate, kiểm tra 2 lần — 1 lần trước khi chọn đáp án, 1 lần ngay sát `.click()`. Poll bị từ chối vì blocker vẫn được audit (`hardStopError` mang `ab_poll` ra ngoài).
  - **Nhiều ảnh 1 job:** `max_images_per_job` (default 4). Chấp nhận nhiều ảnh **chỉ khi mọi ảnh mới thuộc CÙNG 1 lượt assistant VÀ đều là bằng chứng post-turn**. Ảnh rải 2 lượt, hoặc chỉ dựa vào `new_visible_fallback`, vẫn `OUTPUT_AMBIGUOUS` fail-closed. Lưu `__variant-01/02`, ledger thêm cột `result_files` + `image_count`, `write_outcome` ghi từng file; set lưu dở dang có `OUTPUT_PARTIAL` nên ledger không bao giờ nói "không ghi gì" trong khi file đã nằm trong folder.
  - **`run.trial` cap 1-2 → 1-30** theo sửa đổi của Đức.
  - **Codex audit (luân phiên đúng lượt): 6 vòng, tìm ra 6 lỗi thật + 5 lỗi phát sinh khi sửa, tất cả đã sửa, chốt PASS.** Đáng nhớ nhất: (1) click poll TRƯỚC khi kiểm tra hard-stop — bypass CAPTCHA gate; (2) readiness báo READY khi thấy câu hỏi poll mà không nhận ra nút → gửi prompt vào chat đang khoá; (3) đánh dấu "đã trả lời" trước khi ChatGPT thật sự nhận click → audit nói dối.
  - **2 đánh đổi đã chấp nhận, ghi rõ để không phải cãi lại:** (a) cửa sổ chờ ảnh khi ChatGPT còn hiện nút Stop là hữu hạn (4× cửa sổ cơ bản) chứ không chờ đến khi Stop biến mất — vì nút Stop treo là hiện tượng có thật trong codebase này, chờ nó sẽ biến ảnh đã có trên màn thành lỗi timeout. Hệ quả: ảnh đến RẤT muộn vẫn có thể bị bỏ sót, và điều đó được ghi vào `detection_diagnostics` (`image_set_stable_while_generating`). (b) Câu hỏi poll còn hiện nhưng không còn nút nào bấm được chỉ chặn readiness trong 10 giây rồi thôi — nếu chặn mãi thì Đức tự trả lời tay xong run vẫn đứng. An toàn vì nếu composer thật sự còn khoá thì gate pre-submit bắt được và **không prompt nào được gửi**.
  - **CHƯA CHẠY THẬT.** Toàn bộ wave này mới chỉ có test tĩnh + test lõi; chưa gặp poll A/B thật lần nào. Lần đầu gặp live, `detection_diagnostics.ab_poll` sẽ chụp lại thuộc tính THẬT của khối poll (testid, aria-label, class, text) — đó là thứ để neo lại selector ở vòng sau.
- **Next (new session, in order):**
  1. **Đức reload extension** ở `chrome://extensions` (bắt buộc: wave này sửa `content.js` + thêm `ab-poll-core.js` vào content scripts — không reload thì code mới không chạy). Sau đó cấp lại quyền folder nếu bị hỏi + F5 tab chatgpt.com. Dev toggle vẫn giữ nguyên.
  2. **Chạy 1 trial tự hành trọn vẹn** (mốc còn nợ từ phiên trước): bootstrap `jobs.add` 2 job → `output.configure {output_downloads_subfolder: "DucAuto_GPT-Output/Pilot-10_Trial-Tu-Hanh"}` → `run.trial` → poll `run.status` → kiểm tra ảnh + audit `bridge_dev` trong `Downloads\Phai sinh\DucAuto_GPT-Output\...`. Session Quick kẹt của Đức là rác test, bỏ.
  3. **Nếu gặp poll A/B live:** lấy `detection_diagnostics.ab_poll.diagnostics` trong ledger/audit ra, neo lại selector khối poll theo thuộc tính thật thay vì chỉ dựa vào chữ. Kiểm tra 2 ảnh có ra `__variant-01/02` và `result_files` có đủ 2 dòng không.
  4. Giai đoạn 2B backlog: revision token + `run.watch`, `bridge.activity.read`, sidecar JSON mỗi checkpoint, host thành Scheduled Task có giám sát + `/v1/health`.
  - Standing context every new session must honor: full commit/push autonomy under the 4 safeguards; audit-before-handoff with one rotated auditor only when warranted (**vòng vừa rồi là Codex — lượt sau tới Antigravity**); "AI là bộ não, người là cánh tay"; trial exception caps; Chrome physics (pickers need Đức; Downloads-relative is agent-settable; real Downloads root = `Downloads\Phai sinh`).
