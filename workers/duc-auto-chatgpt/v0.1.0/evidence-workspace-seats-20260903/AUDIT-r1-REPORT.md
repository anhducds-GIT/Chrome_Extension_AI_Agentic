RESULT: FAIL
FINDINGS:
1. HIGH — `bridge-transport-loopback.js:102-115, 404-430, 699-705`: pairing rollover can disclose the new token to a host that proved only the old token. Exact interleaving: keep `workspaceWork` occupied (for example, a `DAC_BRIDGE_WORKSPACES_GET` blocked in `tabs.get`); an existing workspace socket supplies a valid `auth_proof`, so line 405 starts verification with old token A and then awaits; `DAC_BRIDGE_PAIRING_SET` independently runs on `pairingWork`, assigns pairing/token B at line 702, and cannot close the workspace socket because `reconnectAllSeats` is merely queued behind the occupied workspace queue; the proof continuation resumes, passes the socket-only re-guard at line 423, and line 428 reads the now-global token B and sends it on the old A-authenticated socket. There is no pairing epoch/token equality re-check, so invariant 1 is broken.
2. HIGH — `bridge-workspace-core.js:45-70` and `bridge-transport-loopback.js:215-220, 554-570, 610-613`: the durable store binds a workspace only by numeric `tab_id`, but Chrome tab IDs are unique only within one browser session. Repro: assign workspace `finance` to tab 101, restart Chrome, and let an unrelated ChatGPT tab receive ID 101; startup normalization accepts the stale record, `workspaceTabUsable(101)` sees a provider URL, and reconciliation authenticates `finance` against that unrelated tab. No browser-session nonce or durable tab/conversation fingerprint is checked, so the name can drift to another tab in direct violation of invariant 3.
3. HIGH — `bridge-workspace-core.js:54-60, 122-133` and `bridge-transport-loopback.js:195-212`: normalized workspace IDs are checked only against other workspace IDs, never against the profile `instance_id`. Repro: seed `dac.bridge.workspaces.v1` with a live workspace whose `workspace_id` equals the value in `dac.bridge.instance.v1`; both the profile and workspace auth frames announce the same `instance_id`. The unchanged host keys sessions by that ID, so whichever socket authenticates second replaces the other; the evicted seat reconnects and can replace it back. Profile/workspace targeting therefore aliases or oscillates instead of remaining fail-closed. This is reachable from the hostile-storage case the brief explicitly requires defending.
4. HIGH — `bridge-transport-loopback.js:475-492, 786-818`: tab fail-closure omits `chrome.tabs.onReplaced`. Chrome fires that distinct event when prerendering/Instant swaps an old tab ID for a new one. Repro: authenticate a workspace on tab 101, then emit `onReplaced(202, 101)`; neither registered listener runs, the seat remains authenticated, and both the alarm and `connectHost()` skip tab validation because the socket is already authenticated/open. The host continues advertising and answering for a workspace whose bound tab ID no longer exists, breaking invariant 3.
5. MED — `bridge-transport-loopback.js:195-205, 420-421, 546-569`: first-run profile identity creation is no longer single-writer. Every seat calls `loadInstance()` independently during authentication. If the profile seat and a workspace seat both read an absent/invalid instance record before either `storage.local.set` lands, they generate different IDs; the profile can authenticate with ID A while the workspace seat's otherwise-unused base-instance write leaves ID B persisted. At the next profile reconnect its advertised identity changes from A to B. Memoizing/serializing instance initialization is required to preserve the pre-existing stable per-profile routing identity.
6. LOW — `bridge-workspace-seats-smoke.mjs:178-355`: the passing dynamic tests exercise ordinary startup, rename/remove, navigation, and handshake gates, but omit pairing rollover with an in-flight proof, browser-session tab-ID reuse, profile/workspace ID collision, `tabs.onReplaced`, and concurrent first-run instance creation. Consequently the test can pass while all HIGH findings above remain present; the adversarial cases need deterministic regression coverage.
7. LOW — `bridge-workspace-seats-smoke.mjs:38-40` and `DIFF.patch:1475-1478`: the test source contains literal control/NUL bytes, causing Git to encode the entire new test as `Binary files ... differ`. The supplied final file is inspectable, but the commit patch itself hides all test logic from normal line review and text-based audit tooling. Construct the hostile strings with escapes or `String.fromCharCode` so the test remains a textual diff.
