# TRIALS — những trang Scouter đã thử, và học được gì

> **Đây là một cuốn SỔ, không phải hàng rào.** Đức chốt 07/09: *"nhiễm cũng được, chỉ là 1 list
> các trial mà ta đã thử thôi, ko quá khắt khe đâu, trừ khi nó ảnh hưởng quá."*
>
> Viết cho Đức đọc. Mỗi dòng trả lời đúng một câu: **trang này đã thử tới đâu, và nó dạy seed
> được gì.**

## Ba luật của cuốn sổ này

1. **Mỗi dòng phải trỏ về một lượt đã CHẠY THẬT.** Không ghi dự định. Chưa chạy thì chưa có dòng.
2. **Cột "dạy seed được gì" là lý do cuốn sổ tồn tại.** Một trang thử xong mà không dạy được gì
   thì ghi thẳng *"không"* — đó cũng là thông tin, và nó thật hơn một câu chữ nghĩa.
3. **Thứ nào không riêng của trang nào thì đẩy lên seed**, rồi ghi lại ở đây là đã đẩy.
   Luật gốc: [ADR-0009](../../../../docs/adr/0007-scouter.md) mục ⑵.

## Đã thử

| Trang | Ngày | Thử gì | Kết quả | Dạy seed được gì |
|---|---|---|---|---|
| Trang tự dựng (thư mục tạm) | 06/09 | Cú bấm của máy có được trang coi là thật không | **ĐẠT** — `isTrusted: true` qua `chrome.debugger`; `element.click()` thì không | Đóng luôn hướng `element.click()`. Ba worker cũ đang dùng cách đó |
| Trang tự dựng (thư mục tạm) | 07/09 | Chính lõi hành động của Scouter, bấm–gõ trên trang thật | **ĐẠT 11/11** trên Chrome 152, kể cả ca phải cuộn 1288px hai chiều | Hai hệ toạ độ không lệch. **Chưa đo:** trang có khung lồng · trang đổi tỉ lệ hiển thị |
| `hnx.vn` — kết quả giao dịch phái sinh | 07/09 | Dữ liệu nằm ở đâu, gọi bằng tham số nào | **Đo xong hợp đồng trang.** Bảy tham số đọc thẳng từ mã trang; một ngày ~46 KB (KHÔNG phải 231 KB như ghi lần trước) | ⑴ Có trang **không cần bấm nút nào** → `scout.fetch` là năng lực chính, không phải bấm–gõ. ⑵ **200 OK không chứng minh gì**: ngày nghỉ cũng 200 + JSON hợp lệ, chỉ khác ở chỗ 0 ô dữ liệu (đo: 192 với 0). ⑶ Sai tham số ra **một trang HTML**, không ra lỗi |
| `hnx.vn` — tầng vận chuyển | 07/09 | Vì sao Node không với tới được | Máy chủ gửi **thiếu mắt xích chứng chỉ** (chỉ leaf). Chrome tự lấy qua AIA, Node ném `UNABLE_TO_VERIFY_LEAF_SIGNATURE` | Một lý do nữa để pilot chạy **qua Scouter chứ không qua script Node**. Và đừng bao giờ vá bằng cách tắt kiểm chứng chỉ — lấy đúng mắt xích thiếu thì kiểm vẫn tử tế |
| `hnx.vn` — điều hướng | 07/09 | Bộ tải tĩnh có tìm ra trang không | **Không.** Menu dựng bằng JavaScript; `cau-truc-website.html` trả 16 liên kết, không có cái nào dẫn tới trang kết quả | **Tìm ra trang** cần Chrome; **lấy dữ liệu** thì không. Hai bài toán khác nhau, và lẫn chúng là chọn sai công cụ |
| `vinfast.udinbv.com/optic` — Udin, công cụ thiết kế (**ca thử**, không phải đích) | 12/09 | Seed có đọc được một SPA React khác hẳn `hnx.vn` không, và **không sửa một dòng seed nào** | **ĐẠT** — `scout.targets` · `page` · `a11y` · `tree` · `shot` đều chạy. 15 phần tử tương tác, cây trợ năng đọc ra *"nút Agent"* / *"nút Manual Gen"* trong khi DOM chỉ có hai `button.create-mode-btn` **không phân biệt được** | ⑴ Câu **"năng lực chung"** thôi là lời khai: cùng seed, hai hình dạng trang. ⑵ Đây là trang đầu tiên **bắt PHẢI GÕ và BẤM mới ra dữ liệu** — đúng tiêu chí `ROADMAP.md` mục ① thêm ngày 08/09, mà ba ứng viên đề xuất hôm đó (`hsx.vn` · CafeF · Vietstock) **không cái nào** đáp ứng gọn bằng. ⑶ Ép lộ chỗ seed còn THIẾU: nó không có cách **đợi**, nên sinh ra `scout.wait`; và không **nghe** được mạng, nên sinh ra `scout.network`. Cả hai đã đẩy lên SEED, không nằm ở adapter nào |
| `vinfast.udinbv.com/optic` — lượt GHI đầu tiên trong việc thật | 12/09 | Gõ prompt → chờ nút sống lại → bấm Send → nghe mạng. **Lượt ghi đầu tiên của gói chạy để làm ra một kết quả, không phải để đo chính nó** | **Hỏng có ích.** Gõ ĐẠT (40 ký tự, bàn phím thật, React nhận — nút Send từ `disabled` sống lại). Bấm báo ĐẠT nhưng **không gửi được gì**. Nghe mạng sau khi bấm: **0**; nghe một lượt tải trang: **30 lượt**, lộ cả bộ máy sau lưng (Cognito · AppSync GraphQL · Lambda · `manage-tenant-session`) | **Bốn mục nợ, đều là của SEED chứ không của trang:** `S-17` bấm không kiểm điểm-bấm-thuộc-về-ai nên **báo thành công khi bấm trúng lớp che** (P1) · `S-18` `scout.wait` nhầm *có mặt* với *dùng được* — trả `satisfied` sau 36ms trong khi tấm chắn phủ kín ứng dụng · `S-19` `scout.navigate` không F5 được cùng URL · `S-20` gắn-rồi-nhả nên **không nghe được lưu lượng do chính mình gây ra**. Cả bốn chỉ lộ ra khi có một trang BẮT PHẢI BẤM — HNX không bao giờ ép được chúng |
| Trang tự dựng `127.0.0.1:38411` — ba nút và một lớp phủ bật/tắt được | 12/09 | Hai chốt mới (`S-17` bấm phải kiểm điểm bấm; `S-18` `wait` biết *dùng được*) có đúng ngoài đời không, hay chỉ đúng trước máy giả | **ĐẠT cả sáu ô.** Nút chữ thuần → `relation: "self"` · nút `<button><svg><path>` → `"descendant"` · có lớp phủ → `CLICK_OBSCURED`, **không một khung chuột nào rời đi**. `wait present` ĐẠT trong khi `wait usable` trả `usableCount: 0, blockedBy: "covered"` — **hai câu trả lời khác nhau trên cùng một trang** | ⑴ Trả lời một câu **không tra được trong tài liệu**: Chrome tự đi ngược từ nút văn bản lên phần tử cha, nên `<button>Gửi</button>` KHÔNG bị từ chối oan. Tôi đã định tin trí nhớ mình chỗ này; đo thì rẻ hơn. ⑵ Tái hiện `S-19` lần hai ngoài Udin — nó là khuyết tật của seed, không của trang. ⑶ Lộ `S-21`: một target thỉnh thoảng không trả lời được câu hỏi hình học, **chưa biết vì sao**. ⑷ Trang tự dựng làm được việc mà trang thật không làm được: **bật/tắt lớp phủ theo ý mình**. Đây cũng là bộ khung cho `T7` |
| Trang tự dựng `127.0.0.1:38411` — lượt F5 | 12/09 | `S-19`: đi tới ĐÚNG url đang đứng. Lượt gọi đầu tiên trong ngày đi qua **TÊN ghế** (`Udin_Scout`) thay vì dãy số | **ĐẠT.** Trước: 15.000ms rồi `NAVIGATE_TIMEOUT (url chưa đổi)` — câu sai nguyên nhân. Sau: **254ms**, `ok`, `reloaded: true`, `arrivedBy: "new_document"`. Đi tới url KHÁC vẫn chạy nguyên | ⑴ Tín hiệu "đã đi" không phải url mà là **danh tính tài liệu** (`backendNodeId` nút gốc) — `nodeId` vô dụng vì nó được cấp lại mỗi lượt hỏi. ⑵ Phải nhận CẢ HAI dấu hiệu: F5 đổi tài liệu chứ không đổi url, `#muc-2` đổi url chứ không đổi tài liệu. ⑶ **Tên ghế chạy trọn vòng** — Đức gõ ở bảng bên, máy chủ định tuyến đúng ghế; tính năng Profile ID nghiệm thu được ngoài đời, không chỉ trên phép ghim |
| Bridge thật — bảng hạn chờ | 12/09 | `S-16`: có method nào còn khai hạn chờ dài hơn ngưỡng 35 giây của máy chủ không | **ĐẠT.** `system.capabilities` khai ra ngoài dây: không method nào vượt 34.000. Xin `timeout_ms: 60000` bị cửa Bridge từ chối `INVALID_PARAMS` kèm khoảng hợp lệ | Một hạn chờ khai dài hơn ngưỡng máy chủ **không mua thêm thời gian — nó mua một lời nói dối**: máy chủ đã báo hết giờ trong khi extension vẫn đang làm. Hạ số ở một đầu mà quên trần tham số ở đầu kia thì bug chỉ đổi chỗ |
| Trang tự dựng `127.0.0.1:8642` — pilot `trang-thu-cham` | 12/09 | **T7: khép cả vòng tự cải tiến một lần** — Scouter dò trang → AI viết adapter → nạp lại → adapter chạy | **CHƯA KHÉP, và chặng dừng lại có tên.** Chặng ①②③ chạy trơn, không sửa tay: dò xong ghi báo cáo 18.354 byte xuống đĩa qua `file.write`; adapter dựng **chỉ từ báo cáo** và chạy được. Chặng ④ dừng: `scout.type` trả `typed: 8` mà ô nhập vẫn rỗng, `scout.click` trả `hit: descendant` mà tay nghe lượt bấm **không hề chạy** — xem `S-22`. Cùng lượt gọi đó **có chạy lúc 16:41** rồi thôi từ ~16:44 | ⑴ **Ô kết quả là `div` ẩn → `scout.page` KHÔNG thấy nó (không tương tác) và `scout.a11y` cũng KHÔNG (chưa hiện thì không có trong cây trợ năng).** Chỉ `scout.tree` thấy. Dò thiếu `tree` là dựng ra adapter biết bấm mà không biết câu trả lời rơi vào đâu — suýt nữa đã thế. ⑵ Seed **không có phép đọc chữ của MỘT phần tử**; đường duy nhất là đọc cây trợ năng rồi lọc. ⑶ Một trang thử mà độ trễ do `setTimeout` thì **không tách được** "chưa tới lượt" với "không tới nơi" — phải có một dấu đặt NGAY trong tay nghe. Dấu đó là thứ biến 8 giây chờ vô nghĩa thành một câu lỗi đúng |
| `vinfast.udinbv.com/optic` — màn "User Limit Reached" | 13/09 | Scouter tự vượt màn chờ bằng nút Try Again | **ĐẠT** — nút `usable` → click `self` → màn chắn tắt → ô prompt `usable` | Trang bắt bấm một nút mới vào được là use case chung: **chờ `usable` → bấm → kiểm bằng kết quả trên trang**, không tin lời báo của lệnh bấm |
| `vinfast.udinbv.com/optic` — gửi prompt | 13/09 | Scouter gõ prompt, bấm Send, chờ Udin làm xong | **ĐẠT** 3/3 — "a red car": 68s, 4 ảnh; `gui-prompt.mjs` sau vá audit: 51s, đúng 4 ảnh mới (đếm theo `src`) | Kiểm từng chặng bằng trang: nút Send mở khoá (chữ tới React) → nút thành `stop-button` (đã nhận) → `stop-button` tắt + đếm ảnh tăng |
| `vinfast.udinbv.com/optic` — lượt `scout.grab` đầu tiên | 14/09 | `T13`: `lay-anh.mjs` đi bằng `scout.grab` thay `scout.fetch`, lấy ảnh kết quả về đĩa | **HỎNG CÓ ÍCH, và W3 vẫn CHƯA ĐẠT.** Chọn selector ĐẠT: `alt` trùng 4 lần nên bị loại đúng, rơi xuống `[src^=]`, khớp **1**. Grab tới được mạng và trả **403** — vì **mọi ảnh trên trang là của 13/09** mà URL ký sẵn hết hạn sau **900 giây**. Gửi prompt mới để có ảnh tươi: `scout.type` + `scout.click` ĐẠT (Udin bắt đầu chạy), nhưng **quá 17 phút vẫn chưa xong** nên chưa có ảnh nào để grab | ⑴ **`X-Amz-Expires=900`** — bất kỳ workflow nào "thu kết quả sau" đều chết trên trang này; W3 phải chạy **ngay sau** W2. ⑵ **Lượt chạy thật bắt lỗi mà 23 khối ghim không bắt được** (`G-53`): `src` của `scout.query` mang thêm dấu `…` báo đã cắt query, nên nó KHÔNG phải tiền tố của `src` trên trang — máy giả của tôi trả `src` sạch nên xanh hết. Đã vá và thêm khối mang đúng hình dạng thật. ⑶ **`alt` không chỉ được một ảnh** (`G-54`, khớp 4): kế hoạch ban đầu định ghim `:nth-of-type` — nó sẽ tải nhầm ảnh mà không ai biết. Phép kiểm "hỏi lại trang từng ứng viên" là thứ cứu chỗ này. ⑷ **Trần 300s của adapter vứt mất một lượt đã tiêu tiền** (`G-55`): *quá giờ* và *hỏng* là hai câu khác nhau. ⑸ Không đọc được chữ trên trang nên **không biết Udin đang báo gì** — một lý do cụ thể cho `O8` |
| `vinfast.udinbv.com/optic` — **E2E ba chặng, và ảnh đầu tiên xuống đĩa** | 14/09 | `T13`+`T15`: mở trang → vượt màn chắn → gửi prompt mới → lấy ảnh kết quả về vùng ghi, một mạch không sửa tay | **ĐẠT** ba chặng; W3 lấy **2/4 ảnh** rồi dừng vì **hết trần 200 lượt ghi**. Hai tệp thật: `01-batch-….webp` **330.270 byte**, `02-….webp` **290.214 byte**, kiểm bằng kích thước thật trên đĩa | ⑴ **Tầng vận chuyển khai dối**: Bridge khai phong bì 1 MiB, thực tế rớt **chập chờn** quanh 65 KB — nguyên nhân chung của `scout.shot` chết và mọi lượt grab trả thân thật (`S-25`, `G-63`). ⑵ Header `Range` **giết service worker** (`G-62`) — đường chia khúc phải đi bằng *tải cả tệp, mã hoá một khúc*. ⑶ Một ảnh 746 KB = **16 khúc = 16 đơn vị trần ghi**: trần 200 chỉ đủ ~12 ảnh, nên trần ghi nay là một **ràng buộc thiết kế**, không phải một con số nền. ⑷ *Mỗi cỡ thử một lần thì một lỗi chập chờn trông y như một ngưỡng* |
| `vinfast.udinbv.com/optic` — **E2E trọn vẹn, và bốn ảnh xuống đĩa** | 14/09 | `T33`: chạy lại đúng lượt 14/09 trước đó, **không viết thêm một dòng nào**, chỉ khác ở chỗ `T24`+`T31` đã xong và Đức đã nạp lại extension | **ĐẠT 4/4.** Prompt *"a bronze desk lamp on a walnut table"*: 177s, 4 ảnh mới, cả bốn xuống đĩa — 764.000 · 686.422 · 787.096 · 603.660 byte, kích thước thật trên đĩa **khớp từng byte**, đầu tệp `RIFF…WEBP` hợp lệ. Tốn **9 đơn vị** trần ghi cho cả bốn ảnh | ⑴ **Con số nói `T24` đáng giá bao nhiêu**: 16 đơn vị cho MỘT ảnh → 9 cho BỐN. Trần ghi thôi là ràng buộc thiết kế của workflow này. ⑵ Không đổi một dòng adapter nào mà lượt chạy đi từ 2/4 lên 4/4 — chỗ hỏng ngày trước **chưa bao giờ nằm ở adapter**, nó nằm ở tầng vận chuyển (`S-25`). ⑶ Ranh giới khúc là **768 KB**, không phải 1 MB (`G-78`) |
| `vinfast.udinbv.com/optic` — **W4 đọc chữ · W7 prompt lần hai** | 14/09 | `T34`+`T36`: đọc câu trả lời của agent, rồi cố ý để chữ lạ trong ô prompt và gửi lượt hai đè lên | **ĐẠT cả hai.** W4 đọc đúng câu trả lời cho prompt vừa gửi (251 ký tự) bằng selector khớp **đúng một** — trong khi hai selector "cái cuối cùng" nghe hợp lý hơn khớp **20**. W7: không xin cờ thì **vẫn từ chối**; xin rồi thì xoá → chờ nút Send khoá lại → gõ → 4 ảnh mới, và agent trả lời về **đúng prompt mới** | ⑴ **Lời từ chối cũ là một lớp bảo vệ**, không phải thiếu sót — mở nó bằng một cờ người gọi phải XIN, chứ không hạ nó xuống cho tiện. ⑵ Bằng chứng W7 mạnh nhất không phải lời báo của lệnh xoá mà là **câu chữ agent viết ra**: nó không dính một mẩu nào của chữ cũ. ⑶ W4 là lệnh ĐỌC nên xếp **sau** lượt tiêu tiền — một W4 đỏ không bao giờ làm mất ảnh đã nằm trên đĩa |
| `vinfast.udinbv.com/optic` — **chặng ② của `T7` trên một trang khác** | 14/09 | Chạy bộ dò của trang thử lên Udin **không sửa một dòng nào dành riêng cho Udin**, ghi báo cáo xuống đĩa | **ĐẠT phần cơ học, ĐỎ phần quan trọng.** Báo cáo **219.730 byte** đi qua `file.write` trong MỘT phong bì; cả ba phép dò khai `hasMore: false`. Nhưng tìm sáu thứ `udin-optic` thật sự dùng thì chỉ thấy **hai**: `agent-send-button`, `agent-message-item`. Vắng `agent-prompt`, `batch-grid-image`, `markdown-content` | ⑴ **`hasMore: false` là một XANH GIẢ**: nó nói *tôi trả hết thứ tôi tìm*, không nói *tôi tìm đủ thứ anh cần*. ⑵ Nguyên nhân không phải trần — `scout.page` chỉ kiểm kê phần tử **tương tác**, `scout.a11y` chỉ thấy thứ có vai trò trợ năng, `scout.tree` sâu tối đa **10**. Nâng trần không chữa được. ⑶ Selector thật của `udin-optic` ngày xưa tìm bằng `scout.query` **dò tay** — nên vòng tự cải tiến chưa bao giờ thật sự tự đi qua chặng này. Đây đúng là thứ `T7` sinh ra để lộ |
| `vinfast.udinbv.com/optic` — **E2E trọn vẹn TỪ MỘT EXTENSION KHÁC** | 15/09 | `T21` chặng ④: chạy bốn chặng W1..W4 từ gói `udin-optic` mới tách — extension riêng, giao thức riêng, tệp ghép cặp riêng, **12 method thay vì 24**, quyền hẹp về một trang thay vì `<all_urls>` | **ĐẠT 4/4, HAI LƯỢT.** Lượt hai (*"a copper watering can among five lavender stems"*): 4 ảnh xuống đĩa — 786.870 · 959.068 · 964.148 · 943.476 byte, mỗi ảnh 3 khúc, đầu tệp `RIFF…WEBP` hợp lệ; W4 đọc đúng câu trả lời cho prompt vừa gửi (192 ký tự). **Và `git status workers/duc-scouter` SẠCH** | ⑴ **Vế thứ hai mới là vế chứng minh**: Scouter không phải sửa một dòng nào để một extension khác chạy trọn việc trên mã của nó — câu *"bộ đồ nghề chung"* thôi là lời khai. ⑵ **Lượt MỘT hỏng, và không phí** (`G-94`): ảnh Udin nằm trên một bucket S3 chứ không trên trang làm việc, nên `host_permissions` vừa thu hẹp làm `scout.grab` chết với `Failed to fetch`. Bảy phép ghim + suite gốc xanh + máy chủ chạy thật **đều không thấy** — không cái nào gọi ra ngoài internet. ⑶ **Thu hẹp quyền là một thay đổi HÀNH VI**, không phải một dòng khai báo; thứ duy nhất đo được nó là một lượt chạy thật |
| `vinfast.udinbv.com/optic` — **đường ghi TỰ KIỂM, `S1`+`S2`** | 16/09 | Gõ xong thì **đọc lại ô nhập**, và một cú bấm có **mốc để đối chiếu** — cả hai trên trang thật, sau khi đã đo trước trên Chrome sạch | **ĐẠT.** `scout.type` trả `da_kiem: true` qua đường trợ năng; **~390 ms** mỗi lệnh gõ (đối chứng: `scout.query` trần **15 ms**). Ba nhánh của `scout.click` đo trên dây thật: mốc có thật → `da_kiem: true` sau **2 ms** · mốc không tới → `CLICK_NOT_OBSERVED` sau **1018 ms** · bấm trần → tự khai chưa kiểm. E2E trọn W1→W2→W3→JPG→W4, 4 tệp JPEG thật, cỡ khớp **từng byte** | ⑴ **Phép đo lật ngược kế hoạch:** `<input>` và `<textarea>` giữ chữ ở **thuộc tính đối tượng** nên cây DOM đọc ra **rỗng**; ô `contenteditable` thì đọc được — ngược hẳn điều đã viết. Chọn đường đọc theo **tên thẻ**, đừng mò. ⑵ **Ô che nội dung là trạng thái THỨ BA:** `type="password"` trả về dấu che, nên báo *“lệch”* ở đó là một lời buộc tội sai. ⑶ **Đếm, đừng hỏi “có chứa”:** `scout.type` không xoá chữ cũ, nên một ô đã sẵn chuỗi ấy làm phép *“có chứa”* ĐẠT cho một lượt gõ chưa bao giờ tới trang. ⑷ **390 ms retire một câu hỏi tôi đã định đưa Đức chốt** — mở thêm một method CDP hẹp hơn: không đáng. |
| `vinfast.udinbv.com/optic` — **`W6`, và một tiền đề bị lật** | 16/09 | Tìm thao tác *“đưa một ảnh kết quả vào canvas”* mà bảng `W` khai từ 13/09 | **Thao tác đó KHÔNG TỒN TẠI.** Ảnh kết quả **đã ở trên canvas** dưới dạng lưới bốn ô; nút *Add to canvas* là nút **mở menu** bốn mục (`Frame` · `Image` · `Video` · `3D Model`); đếm trọn **54 nút** của trang thì không có nút nào trên từng ảnh. Thay bằng **thêm một khung**: đo thật **8 → 9**, menu tắt sau **1 ms** | ⑴ **Một dòng bảng năng lực là một LỜI KHAI, không phải một sự thật** — nó ra đời từ một cái tên nút đọc được trong DOM, và cái tên ấy nói dối. ⑵ **Lý do hoãn cũng sai:** `CHUOI-VIEC` đoán `W6` sẽ hỏng vì `S-22`; hôm nay cú bấm tới trang **bốn lần liên tiếp** trên đúng ghế ấy. Thứ chặn nó là một **thao tác không có thật**. ⑶ **Menu tắt KHÔNG chứng minh gì** — bấm lại chính nút mở menu cũng làm nó tắt; chỉ **phép đếm** phân biệt được. Đúng bài học `S1`. ⑷ **Có phép đo không được phép chạy:** bấm thử `Image` có thể dựng một hộp thoại hệ điều hành và **treo Chrome của Đức** — từ chối theo rủi ro, và khai đúng là chưa đo |
| `vinfast.udinbv.com/optic` — **`W8` CHẠY THẬT, bảng `W` đủ 8/8** | 16/09 | Đưa NGƯỢC một tấm ảnh mà chính Udin sinh ra hôm ấy vào lại Udin, **không một cú bấm nào của người** | **ĐẠT.** `mo_bang` trỏ mục `Image` tìm theo **NHÃN** (không theo số thứ tự) · **hộp thoại hệ điều hành KHÔNG hiện lên màn hình Đức** · pill đính kèm **0 → 2**, nhãn đọc ra `"1 Image"` · ô chọn tệp tạm bị trang thu lại về **0**, đúng như khi người dùng tự chọn · `hit: descendant` · 5 đơn vị trần ghi | ⑴ **Một method CDP mở ra để ĐÓNG một cửa khác:** `Page.setInterceptFileChooserDialog` làm cú bấm `Image` — thứ đã hai lần bị từ chối vì *"nó treo Chrome"* — trở nên an toàn cho máy tự bấm. Hiếm khi một lượt nới bề mặt lại **giảm** rủi ro. ⑵ **Hiểm thật của nó là ĐỂ QUÊN BẬT**, không phải quyền đọc file: lúc ấy hộp thoại chính Đức mở cũng im lặng không hiện. Lượt tắt nằm trong `finally`, con `U11` canh. ⑶ **Đo trước tiết kiệm ba method:** nó chạy không cần `Page.enable`, không cần kênh sự kiện, và ô nhận file nằm lại trong DOM chờ nên hỏi lại là thấy. ⑷ **Bảng `W` đủ 8/8 → Udin `MASTERED`**, và ba hàng cuối hết chặn vì **ba lý do khác nhau, không cái nào là lý do đã ghi**. |
| `vinfast.udinbv.com/optic` — **`W8`: ô nhận file sống bao lâu** | 16/09 | Bấm `Image` trong menu *Add to canvas* thì Udin mở cái gì? Đây là cú bấm tôi đã **hai lần từ chối tự bấm** vì nó có thể dựng hộp thoại hệ điều hành và treo Chrome. Giương máy quan sát DOM rồi **nhờ Đức bấm** — cú bấm và cú huỷ đều nằm trong tay người ngồi trước máy | **Ô `<input type="file">` được DỰNG TẠM rồi XOÁ ĐI.** Đo từng 2 giây: menu mở (+178s) → ô chọn tệp hiện ra **0 → 1** và menu tắt (+180s) → Đức chọn ảnh → **nay đếm lại: 0**. Ảnh đã vào: `agent-context-pill-thumb is-image` cạnh ô prompt (đúng chỗ Đức mô tả) **và** một `.canvas-image-container` trên canvas. Ô ấy chỉ sống trong lúc hộp thoại đang mở | ⑴ **`scout.upload` không với tới được nó**, và đó không phải khuyết tật của lệnh: ô biến mất trước khi một lượt gọi kịp hỏi tên nó. Cửa sổ sống của nó ≈ khoảng thời gian hộp thoại mở, tức là một cuộc đua, không phải một thiết kế. ⑵ **Đường đúng nay ĐO ĐƯỢC chứ không còn đoán:** `Page.setInterceptFileChooserDialog` — bảo Chrome **đừng dựng hộp thoại**, bắn một sự kiện thay thế. Chính nó làm cú bấm `Image` trở nên AN TOÀN. ⑶ **Hiểm thật của method ấy KHÔNG phải quyền đọc file** (file vẫn qua `DOM.setFileInputFiles`, vẫn nhốt trong vùng ghi) mà là **để quên nó BẬT**: lúc ấy hộp thoại Đức tự mở sẽ im lặng không hiện. Nên bật–bấm–tắt trong đúng một lượt, tắt trong `finally`. ⑷ **Tiền đề thứ năm trong hai ngày** bị lật khi đem đi đo. |
| `vinfast.udinbv.com/optic` — **`W5`, và một cột "Cần" khai sai** | 16/09 | Chọn chế độ *Agent* / *Manual Gen*, hàng bảng `W` khai cần `O3` (`scout.a11y`) | **ĐẠT, mà không dùng `O3` một lần nào.** `button.create-mode-btn` khớp **2** · `:nth-of-type(1)` = `"Agent"` (đang `active`) · `:nth-of-type(2)` = `"Manual Gen"`. Chạy thật ba nhánh: Agent→Manual Gen (`daBam: true`) · xin lại chính chế độ đang bật (**không bấm lần nào**) · nhãn lạ `"Manual"` bị từ chối kèm hai nhãn đọc được. Ghế trả về đúng chế độ ban đầu | ⑴ **Hàng ấy chỉ tới một lệnh gói này KHÔNG CÓ trên dây** — Udin khai 12 method, không có `scout.a11y`. Một dòng năng lực chỉ tới lệnh không gọi được là một dòng không ai chạy được, và nó nằm đó từ 13/09. ⑵ **Lời khai 12/09 đúng hẹp hơn nó nghe:** class không nói được *cái nào là Agent* (đúng), nhưng `.active` nói được *cái nào đang chọn* — và `W5` cần cái thứ hai. ⑶ **Con số hiển nhiên nhất là con số vô dụng:** số nút mang `.active` bằng **1** ở cả nhánh chạy đúng lẫn nhánh cú bấm không tới trang; chỉ **VỊ TRÍ** nút đang bật phân biệt được hai nhánh. ⑷ **`S-22` lại bị đổ oan lần thứ hai trong một ngày** — cú bấm tới trang cả ba lượt. Hai hàng liên tiếp (`W6`, `W5`) hoãn vì một lý do sai mà không ai đi kiểm lại. |
| `vinfast.udinbv.com/optic` — **`R1`: ba số lật ba giả định của chính tôi** | 16/09 tối | Nối các chặng đã ĐẠT thành MỘT lệnh chạy trọn vòng việc thật của Đức, rồi đem chạy sống | **Chưa ĐẠT — và lý do nằm ngoài repo.** Lượt chạy sống dừng ở chặng đính kèm; ba phép đo liên tiếp cho ba số: ① `[class*=agent-context-pill]` khớp **2 cho MỘT ảnh** — một là cái hàng chứa (`agent-context-pills-row`), một là cái thumb; ② gỡ sạch (`.pill-thumb-clear`, thumb **1 → 0**) rồi tải lên lại → **0 → 0**, trong khi `scout.upload` vẫn báo `files: 1`; ③ nạp lại trang thì **màn chắn `.concurrency-overlay` hiện ra và không tắt** — **Udin đang ĐẦY CHỖ** — màn chắn đọc nguyên văn *“User Limit Reached · Please try again in a few minutes when other users finish their sessions.”*; đo tiếp **~25 phút** và **6 lượt bấm `Try Again`** cách nhau một phút: không đổi. Trang vẽ xong mất **18 giây** sau nạp lại. Mã: 16 khối ghim · 16 đột biến, 0 sống sót · lượt chọn ảnh chạy thật trên vùng ghi | ➀ **Phép kiểm bắt đúng thứ nó sinh ra để bắt, ngay lượt đầu ra đời:** nó từ chối tiêu credit cho một lượt mà ảnh tham chiếu chưa bao giờ đính kèm. Nếu tin `ok` của `scout.upload` thì lượt đó đã gửi, đã mất tiền, và ảnh trả về vẫn *trông hợp lý*. ➁ **`W8` xanh là xanh của MỘT lần, không phải của MọI lần** — nó chạy trên trang vừa nạp lại, chưa có ảnh nào đính kèm, Udin còn rỗng chỗ. Một hàng ĐẠT nói *“làm được”*, không nói *“lần nào cũng được”*. ➂ **Một selector đếm cả cái hộp lẫn thứ trong hộp thì câu báo lỗi đọc ra sai:** *“2 → 2”* trông như đang có hai ảnh, thật ra là một ảnh và một lượt đính kèm hụt. ➃ **Sau lượt gỡ, mốc luôn bằng 0** — nên hiệu số `sau > truoc` là một biến **thừa** và hai con đột biến của nó là **tương đương**; đổi sang một con số tuyệt đối. |
| `vinfast.udinbv.com/optic` — **`R1` KHÉP VÒNG, và một lời khai của tôi bị Đức lật** | 16/09 tối | Chạy sống trọn vòng việc thật: ảnh lượt trước → đính kèm → prompt style mới → ảnh mới về đĩa | **ĐẠT, nhưng còn một vết.** Lấy `01-batch-…sn3w1x3b.jpg` của lượt sáng, xin *“1950s enamel travel poster”* → **4 ảnh mới**, đã đổi JPG, canvas **2 → 7**, ảnh đính kèm `0 → 1`. **VẾT:** chặng đính kèm **vẫn bật hộp thoại `Open` của Windows** và Đức phải bấm Cancel — lượt tải lên vẫn thành công dù có hộp thoại. **Đường DÁN đã đo và ĐÓNG:** Udin bỏ qua cả tệp lẫn ảnh bitmap trên clipboard hệ điều hành; nhưng copy–paste **trong nội bộ canvas Udin thì được** (Đức đo) | ⑴ **Câu “hộp thoại không hiện lên màn hình Đức” ở hàng `W8` CHƯA BAO GIỜ ĐƯỢC ĐO** — tôi suy ra từ việc Đức không kêu, rồi viết nó vào bảng năng lực như một phép đo. Đức gửi **ảnh chụp** hộp thoại, đúng thư mục ảnh của lượt chạy. Đã gạch tại chỗ. ⑵ **Nó hỏng theo kiểu IM LẶNG:** ô chọn tệp sống trong lúc hộp thoại treo, nên `DOM.setFileInputFiles` vẫn ăn và lượt chạy vẫn xanh — cái giá không nằm trong kết quả, nó nằm trên màn hình Đức. ⑶ **Ba giả thuyết đã đo, ĐỀU TRƯỢT** trên Chrome hồ sơ trống: tắt chặn sớm · trang bấm trễ · thiếu `Page.enable`. Cả ba: trang nhận file, 0 hộp thoại. Khác biệt nằm ở đường `chrome.debugger`, chưa đo được. ⑷ **Hai lần trong một tối tôi dùng SAI DỤNG CỤ ĐO:** `MainWindowTitle` không thấy cửa sổ con, và bộ lấy mẫu 400 ms bằng PowerShell thực ra chỉ lấy được 7 mẫu vì mỗi lượt gọi tốn hơn một giây. Cả hai lần đều cho ra *“không có hộp thoại”* — một câu **sai** mà đọc như một phép đo. |
| `vinfast.udinbv.com/optic` — **`@1` / `@2`: chọn ảnh tham chiếu theo thứ tự** | 16/09 khuya | Đức nêu: *“nếu bạn apply 1 cho 2 mà không xác định được đâu là 1, đâu là 2 thì sẽ không còn chính xác nữa”* | **ĐẠT, chạy thật.** Bỏ 2 ảnh đang chọn → Shift+click A rồi B → **đọc ngược từ trang: A→`"1"`, B→`"2"`**, đúng hai ảnh đã xin. `scout.chon` (method thứ 25) lên dây sau một lượt nạp lại; **không thêm method CDP nào** | ⑴ **Hai phép đo lật thiết kế ngay lượt chạy đầu.** ⒜ Canvas là mặt phẳng kéo được nên *có trong DOM* còn xa mới là *bấm được*: **6/12** ảnh bấm được, số còn lại `no_hit_test`, và phép kiểm điểm bấm chặn đúng một cú bấm mù ở toạ độ `y = -100`. ⒝ **Udin chỉ vẽ số khi có từ HAI ảnh trở lên** — một ảnh thì `selected` mà không huy hiệu; bản đầu đòi huy hiệu ngay sau cú bấm ĐẦU nên nó ném oan. ⑵ **Một con đột biến sống sót chỉ ra MÃ THỪA, không phải ghim hở:** phép đếm huy hiệu trong từng hộp chỉ nói lại điều `scout.text` đã bảo đảm (từ chối mọi selector khớp ≠1). Xoá, không đi ghim một thứ không làm gì. ⑶ **Đường dán đã đóng bằng chính phép đo của Đức:** copy–paste *trong nội bộ canvas* thì được, còn clipboard hệ điều hành (tệp lẫn bitmap) thì Udin bỏ qua. |
| `vinfast.udinbv.com/optic` — **kéo-thả: ảnh NGOÀI vào canvas, KHÔNG hộp thoại** | 17/09 | Đức hỏi *“có cách nào tránh được không hay phải sống chung?”* — thay `scout.upload` bằng `scout.tha` cho mọi lượt đưa ảnh từ ngoài | **ĐẠT, chạy thật.** Thả ảnh của Đức vào `#root` tại `(719, 455)`, `hit: descendant` → **canvas 17 → 18**, và **0 cửa sổ hộp thoại** ở cả ba lần đếm (trước · ngay sau lệnh · sau khi ảnh vào). Trước đó đo trên Chrome hồ sơ trống: `files.length = 1`, **525119 byte khai VÀ đọc thật ra cũng 525119** | ⑴ **Bỏ hẳn một thứ rẻ hơn đi chặn nó.** Ba giả thuyết sửa `Page.setInterceptFileChooserDialog` đều trượt; đường kéo-thả không đi qua hộp thoại nào nên không có gì để chặn. ⑵ **Udin KHÔNG có lớp canvas riêng nào đọc được** — `.canvas-viewport`, `.canvas-container`, `main` đều khớp 0; thứ duy nhất khớp đúng một là **`#root`**. ⑶ **Một lượt sửa mã máy chủ là một lượt KHỞI ĐỘNG LẠI TIẾN TRÌNH, không phải một lượt nạp lại extension.** Tôi bắt Đức nạp lại extension rồi mới phát hiện máy chủ vẫn chạy mã cũ (bật lúc 16:43) — hai việc đáng lẽ gộp một. ⑷ **Máy chủ từ chối đúng lúc phải từ chối:** extension đã có `scout.tha` nhưng máy chủ chưa có móc ghép đường dẫn, và nó trả `INVALID_PARAMS` thay vì đoán lấy một đường — đúng thiết kế `T29`. |
| `vinfast.udinbv.com/optic` — **VÒNG THAM CHIẾU trọn vẹn: `@1` / `@2` chạy thật** | 17/09 | Một lệnh: chọn hai ảnh làm tham chiếu theo đúng thứ tự → prompt gọi `@1` / `@2` → ảnh mới về đĩa | **ĐẠT.** `vong-tham-chieu.mjs` chạy sáu chặng `W1 → NGUỒN → CHỌN → W2 → W3 → JPG`. Thứ tự **đọc ngược từ huy hiệu trên trang**: `@1 = …2u4vdq`, `@2 = …jvw125`, đúng hai ảnh đã xin. Prompt *“apply the style of @1 to @2, ultramarine-and-gold Art Nouveau, peacock feather borders”* → **4 ảnh mới**, đã đổi JPG (1,0–1,6 MB mỗi tấm), thư mục `tham-chieu-17-09/2026-09-16T17-18-02-909Z` | ⑴ **Vòng việc thật của Đức nay chạy bằng MỘT lệnh**, và không còn cú Cancel nào — `scout.tha` thay `scout.upload` ở mọi lượt đưa ảnh ngoài. ⑵ **Đức bỏ ràng buộc credit 17/09** (*“tạo ảnh không tốn credit, đừng hesitate”*) và đổi lý do của luật *đổi prompt mỗi lượt*: nay nó để **nhận diện ảnh vừa tạo** trên một canvas đầy ảnh na ná nhau. Prompt đặc trưng là một **dụng cụ đo**, không phải trang trí. ⑶ **Một lượt thả mà canvas mọc thêm ≠ 1 ảnh thì TỪ CHỐI** — “lấy ảnh mới nhất” ở đó là đoán bừa, và nó trỏ nhầm tham chiếu mà không báo gì. |
| `tldraw.com` — **`R3`: TRANG THỨ HAI, KHÁC LOẠI** | 17/09 | Câu hỏi của cả lộ trình: *Scouter là bộ đồ nghề chung, hay một cái adapter cho Udin?* Điều kiện đóng do `CHUOI-VIEC` đặt ra: **một `W` ĐẠT trên trang mới mà KHÔNG sửa một dòng nào của seed** | **ĐẠT, và đạt bằng HAI `W` chứ không phải một.** ⓐ *Đổi màu vẽ*: `Color — Black (selected)` 1 → 0, `Color — Red (selected)` 0 → 1 — trang tự khai trạng thái trong nhãn trợ năng nên chặng này **tự kiểm được**, y hệt `W5` của Udin. ⓑ *Vẽ một hình*: bấm `Rectangle — R` rồi bấm lên `.tl-canvas` → `.tl-shape` **0 → 1**. Vốn từ dùng: `dom.query` · `page.snapshot` · `input.click`. **Không một dòng seed nào bị sửa** — hai file đo chỉ `import` seed. | ⑴ **Câu *“bộ đồ nghề chung”* hết là lời khai.** Udin và tldraw khác loại thật: một bên là chat sinh ảnh + canvas ảnh, bên kia là bảng công cụ + canvas vector, không chat, không sinh gì. Cùng ba lệnh chạy được cả hai. ⑵ **Một giả thuyết của tôi bị chính phép đo bác:** tôi đoán vẽ hình cần *nhấn-di-thả*, thao tác seed không có, và đã định báo đó là khoảng trống. Đo lại: **một cú bấm là đủ**. Suýt nữa ghi một giới hạn không tồn tại vào sổ. ⑶ **Nhãn trợ năng của tldraw chở cả phím tắt** (`Select — V`, `Draw — D`), nên selector phải khớp TIỀN TỐ; khớp đủ chuỗi thì trượt sạch. |

## Chưa thử, và biết là chưa

- **Trang thật của một nhà cung cấp AI** (ChatGPT · Gemini). Ba worker cũ chạy trên đó, Scouter
  thì chưa lần nào.
- **Trang có khung lồng (iframe)** — in ngay trong báo cáo của phép đo ② là chưa đo.
- **Trang đổi tỉ lệ hiển thị** — cùng chỗ.
- ~~**Một lượt GHI trong việc thật.**~~ **ĐÃ THỬ 12/09** — xem dòng cuối bảng trên. Kết quả: gõ chạy, bấm **chưa** làm ra được kết quả, và bốn khuyết tật của seed lộ ra.
- ~~**Kênh nghe mạng trên trang có lưu lượng thật**~~ **ĐÃ THỬ 12/09** — 30 lượt gọi thu được trên một lượt tải trang. Cái tai chạy.
- ~~**Trang đổi tỉ lệ hiển thị**~~ — máy Đức đang ở **125%** và mọi phép đo 12/09 chạy trên đó; toạ độ khớp, không lệch. Chưa thử các mức khác.
- **Cả VÒNG tự cải tiến** (dò → AI viết adapter → nạp lại → adapter chạy): thử 12/09, **ba
  chặng đầu chạy trơn, chặng bốn dừng ở `S-22`**. Vòng vẫn CHƯA khép một lần nào — và ghi đúng
  ở mức đó, vì một dòng xanh không đúng đắt hơn một ô trống.

## Cái file này KHÔNG làm

Không thay `BACKLOG.md` (nợ gì) · không thay `HANDOFF.md` (hôm đó làm gì) · không thay ADR (Đức
chốt gì). Nó chỉ giữ **một danh sách trang**, thứ ba quyển kia không có chỗ nào để.
