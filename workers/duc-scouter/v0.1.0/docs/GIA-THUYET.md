# SỔ GIẢ THUYẾT — Scouter

> **TRA SỔ NÀY TRƯỚC KHI THỬ BẤT CỨ GÌ.** Đức chốt 13/09, sau khi một phiên đi vòng lại đúng
> những giả thuyết đã loại trừ: *"cần một bản ghi các giả thuyết và kết quả, để trong quá trình
> trial có thể tra cứu và không lặp lại việc đã làm."*
>
> Cách tra: `grep -n "<từ khoá>" workers/duc-scouter/v0.1.0/docs/GIA-THUYET.md` — ví dụ `ẩn`,
> `cuộn`, `hit-test`, `setTimeout`. Thấy dòng **SAI** thì đừng thử lại; thấy **CHƯA** thì đọc
> cột *cách thử* để khỏi làm lại phần đã làm.

## Kết luận hiện tại (14/09, sửa cuối ngày)

- ✅ **`S-25` ĐÃ TÌM RA GỐC, và nó là MỘT DÒNG** (`G-67`): `_shared/bridge-host/websocket-core.mjs`
  ném lỗi ở **mọi tin WebSocket bị cắt mảnh**, mà Chrome tự cắt mảnh khi tin vượt ~64 KiB. Nên
  một câu trả lời hơi lớn làm **đứt kết nối** thay vì trả một lỗi có tên. Đây giải thích cả
  `G-56` (`scout.shot` chết), mọi lượt `grab` trả thân thật, và ghi chép 08/09. **Đó là một ca
  CHƯA VIẾT, không phải một lớp bảo vệ** — bản sửa thuần thêm vào. Việc `T24`.
- ⚠️ **Một kết luận của tôi đã SAI và đã sửa trong ngày** (`G-63`): bảng đo đầu tiên trông như một
  NGƯỠNG sạch, nên tôi viết ra rằng có ngưỡng. Đo lại **cùng một cỡ nhiều lượt** thì nó **chập
  chờn**. *Mỗi cỡ thử một lần thì một lỗi chập chờn luôn trông y như một ngưỡng.*

- 🛑 **`S-22` NGỪNG ĐIỀU TRA. Đức chốt 14/09.** Lý do không phải "đã hiểu" mà là **chi phí**:
  cùng một câu hỏi *"tab ẩn có làm mất cú bấm không"* đã được mở **năm lần** (`G-07` `G-38`
  `G-40` `G-42`, rồi `G-47` `G-49`), bốn lần trả lời **SAI**, và chính khối này đã ghi *"đừng
  thử lại"* trước khi tôi mở hai lần cuối. **Mở lại một giả thuyết đã đóng phải có DỮ KIỆN MỚI**
  — một tương quan cũ đếm lại không phải dữ kiện mới.
- **`S-22` đóng bằng LỜI KHAI, không bằng bản vá** (đường ⒝ của `T11`): `README.md` nói thẳng
  `scout.click` hứa *"đã bắn chuột vào đúng điểm của đúng phần tử"* và **không** hứa *"trang đã
  nhận"*. Adapter tự đặt dấu kiểm trên trang. `W1` và `W2` trên Udin đã làm đúng thế và **ĐẠT**
  — tức là giới hạn này không chặn việc thật, nó chỉ chặn việc tin lời báo của lệnh bấm.
- ⚠️ **`G-45` ("chập chờn 1/7") ĐO BẰNG MỘT DỤNG CỤ HỎNG** — xem `G-48`. Đừng trích con số 1/7.
- **Thứ đo được và còn đúng:** trên tab đang hỏng, lệnh **DOM chạy** mà lệnh **Input biến mất**,
  hỏng **theo TAB chứ không theo ghế** (`G-43`). Ai mở lại thì đọc `G-50` trước — đường chưa
  thử duy nhất nằm ở đó, và nó **không** cần một chiến dịch đo.
- **Chín giả thuyết đã loại, đừng thử lại:** tab ẩn (`G-07` `G-38` `G-40`) · cửa sổ thu nhỏ
  (`G-41`) · tab chưa từng hiện (`G-42`) · lớp che / vẽ sai / hộp thoại (`G-44`) · bộ đếm giờ
  (`G-02`) · tab đông cứng kiểu `G-03` · toạ độ lệch tỉ lệ (`G-05`) · gắn lại gỡ lỗi (`G-45`) ·
  điều hướng sau khi gắn (`G-46`).
- **Phép đo A/B trên Chrome SẠCH đã có sẵn** — bản chép của `scouter-action-reality-probe.mjs`
  đo được ba trạng thái tab và hai trạng thái cửa sổ. Dựng lại trong ~5 phút; xem `G-40..G-42`.

## Kết luận cũ (13/09)

- **`S-23` ĐÃ VÁ:** `DOM.getNodeForLocation` nói theo hệ TRANG, hộp/chuột theo hệ KHUNG NHÌN.
  Cộng độ cuộn (hộp `:root`) ở cả hai lõi. Probe 11/11, đột biến 128/128.
- **Udin:** Scouter vượt màn "User Limit Reached" bằng Try Again — adapter `pilots/udin-optic/`.
- **`S-22` vẫn CHƯA rõ:** trên ghế Đức, lượt bấm KHÔNG cuộn trả `ok` mà trang không nhận. Đã loại
  G-02..G-07. Không phải cùng lỗi với S-23 (S-23 trả lỗi, S-22 trả `ok`).

## Cách làm — đọc trước khi thử

1. Grep sổ này. Dòng SAI thì bỏ qua.
2. **Đọc mã trả về trước khi đoán.** G-15..G-18 mất bốn lượt vì chỉ nhìn trang, không nhìn lệnh trả gì.
3. **Chia đôi bằng phép đo có sẵn** (bảng cuối) thay vì dựng giả thuyết mới. Một biến một lượt.
4. Hai thứ **trùng lúc** chưa phải nguyên nhân — ghi CHƯA, đừng dựng code trên nó (G-07).

## Luật ghi

1. **Ghi TRƯỚC khi thử**, dòng trạng thái `CHƯA`. Thử xong thì sửa đúng ô *kết quả* — không
   xoá dòng, không viết lại câu hỏi. Giả thuyết sai **quý như** giả thuyết đúng: nó là thứ phiên
   sau không phải trả tiền lần hai.
2. **Kết quả chỉ có ba chữ:** `ĐÚNG` · `SAI` · `CHƯA`. `ĐÚNG`/`SAI` bắt buộc có *bằng chứng* đo
   được (lệnh, số, tên phép đo). Không đo mà ghi `SAI` vì người khác nói thì ghi rõ nguồn.
3. **Một dòng một câu hỏi.** Hai biến trong một phép thử thì tách hai dòng.
4. Mã `G-<số>`, tăng dần, không dùng lại. Mục nợ ở `BACKLOG.md` trỏ về mã `G-`, không chép lại.

## Sổ

| mã | ngày | mục | giả thuyết | cách thử | kết quả | bằng chứng |
|---|---|---|---|---|---|---|
| G-01 | 12/09 | S-17 | Chrome trả **nút văn bản** cho `DOM.getNodeForLocation` nên `<button>Gửi</button>` bị từ chối oan | trang tự dựng, bấm nút chữ thuần qua Bridge | **SAI** | Chrome tự đi lên phần tử cha → `relation: "self"` (TRIALS 12/09, `127.0.0.1:38411`) |
| G-02 | 13/09 | S-22 | Kết quả không hiện vì `setTimeout` của tab nền bị Chrome bóp | chờ 120s sau lượt bấm | **SAI** | 120s không hiện; dấu `data-bam` đặt NGAY trong tay nghe cũng không có → tay nghe không chạy |
| G-03 | 13/09 | S-22 | Tab bị Chrome **đông cứng** | `?chan=1` — mã lúc tải trang dựng tấm chắn | **SAI** | tấm chắn dựng được → mã trang có chạy |
| G-04 | 13/09 | S-22 | Tab **không được vẽ** | `scout.shot` | **SAI** | 28.488 byte, ảnh đúng trang |
| G-05 | 13/09 | S-22 | Toạ độ lệch vì màn hình 125% | so `clickedAt` với ảnh chụp | **SAI** | `x:87 y:275` CSS px ↔ `108,344` trên ảnh = ×1,25 đúng |
| G-06 | 13/09 | S-22 | Có lớp che | `hit` + ảnh chụp | **SAI** | `relation: "descendant"`, ảnh trống trơn |
| G-07 | 13/09 | S-22 | ✅ **ĐÃ KIỂM LẠI 14/09 (G-40) — kết luận SAI giữ nguyên, nay có phép đo hai chiều chứ không chỉ lời nói.** Tab ẩn (`visibilityState: hidden`) làm mất sự kiện nhập | trang tự ghi `visibilityState` vào `body[data-hien]` | **SAI** | Đo được tab `hidden` và không `mousedown` nào tới cửa sổ — nhưng hai thứ chỉ **trùng lúc**. Đức xác nhận 13/09 đã debug trước: tab ẩn **không** ảnh hưởng bấm. Và G-08 tái hiện lỗi trên Chrome riêng |
| G-08 | 13/09 | S-22 | Lỗi nằm ở **lõi ghi**, không riêng ghế `Udin_Scout` | `npm run scouter:action-probe` — Chrome riêng, hồ sơ trống, chạy chính lõi hiện tại | **ĐÚNG, một phần** | `CLICK_REACHES_BELOW_THE_FOLD` ĐỎ, tái hiện 3/3; bấm/gõ/Enter trên màn hình vẫn XANH. Ca không cuộn ở ghế Đức thì chưa tái hiện được ở đây |
| G-09 | 13/09 | S-22 | Lỗi có từ TRƯỚC bản sửa T1 | chạy cùng phép đo với lõi ở commit `be4f16b5` | **SAI** | lõi trước T1: 11/11 XANH → **T1 gây ra** |
| G-10 | 13/09 | S-22 | Thủ phạm là **làm tròn toạ độ** (`Math.round`, T1) | lõi hiện tại, bỏ riêng làm tròn | **SAI** | vẫn ĐỎ |
| G-11 | 13/09 | S-22 | Thủ phạm là **bước hỏi-điểm** (`kiemDiemBam`, T1) | lõi hiện tại, bỏ riêng lượt gọi `kiemDiemBam` | **ĐÚNG** | XANH lại, trang báo đúng nút `duoi` |
| G-12 | 13/09 | S-22 | Chạy đua: chờ 100ms giữa hỏi-điểm và bấm là đủ | chèn `setTimeout 100` | **SAI** | vẫn ĐỎ |
| G-13 | 13/09 | S-22 | Phải `mouseMoved` trước khi hỏi-điểm | gửi `mouseMoved` trước `kiemDiemBam` | **SAI** | vẫn ĐỎ |
| G-14 | 13/09 | S-21 | Target thỉnh thoảng trả `-32000 No node found` vì tab nằm sau / cửa sổ thu nhỏ | — | **CHƯA** | giả thuyết ⒜ của `S-21`; lưu ý G-07 đã SAI cho một triệu chứng khác |
| G-15 | 13/09 | S-22 | Thủ phạm là `DOM.querySelectorAll` (bước tìm con cháu), không phải `DOM.getNodeForLocation` | lõi hiện tại, gọi `getNodeForLocation` rồi bỏ qua kết quả, không gọi `querySelectorAll` | **ĐÚNG** | XANH, trang báo đúng `duoi`. Nên nút `duoi` — **đọc sai**, xem G-17 |
| G-16 | 13/09 | S-22 | Hỏi-điểm làm trang **dịch đi** sau lượt cuộn, nên toạ độ đã tính bị cũ | đo lại hộp ngay sau hỏi-điểm, so với hộp trước | **CHƯA** | Lần thử 13/09 KHÔNG ra số: `console.error` chạy trong extension thử, không ra stdout của phép đo. Muốn đo thì phải trả số qua kết quả của lệnh, không qua log |
| G-17 | 13/09 | S-22 | Chỉ riêng `DOM.querySelectorAll` (không đổi gì khác) là đủ gây lỗi | lõi hiện tại, giữ nguyên `getNodeForLocation`, thay nhánh con cháu bằng `relation: "descendant"` không hỏi | **SAI** | vẫn ĐỎ. G-15 xanh vì nó NUỐT lỗi của hỏi-điểm, không vì bỏ `querySelectorAll` |
| G-18 | 13/09 | S-22 | Bản vá: hỏi danh sách con cháu **TRƯỚC** lượt cuộn, rồi mới cuộn–đo–hỏi-điểm–bấm | lõi hiện tại, dời `querySelectorAll` lên trước `centreOf` | **SAI** | vẫn ĐỎ |
| G-19 | 13/09 | S-22 | Sau lượt cuộn, hỏi-điểm NÉM lỗi → lượt bấm bị TỪ CHỐI (fail-closed), phép đo chỉ đọc trang nên tưởng là mất | đọc mã trả về của lượt bấm dưới màn hình (`--json`) | **ĐÚNG** | `click_duoi: {ok:false, code:"CLICK_HIT_TEST_FAILED", scrollY:1288}` |
| G-20 | 13/09 | S-23 | Chrome ném lỗi gì ở hỏi-điểm sau lượt cuộn | phép đo ghi thêm `detail` của lượt bấm `#duoi` | **ĐÚNG** | `-32000 No node found at given location` tại (38, 772) — **cùng câu với S-21** |
| G-21 | 13/09 | S-23 | Điểm (38, 772) nằm NGOÀI khung nhìn, hoặc hộp đo được là hộp cũ trước khi cuộn xong | phép đo ghi `innerHeight` + `getBoundingClientRect()` của `#duoi` ngay sau lượt bấm | **SAI** | khung nhìn 1036×799, nút `y 754..789` → tâm 772 đúng và NẰM TRONG khung. Máy ở tỉ lệ **1,25** |
| G-22 | 13/09 | S-23 | `DOM.getNodeForLocation` hiểu toạ độ khác hệ CSS px của `getBoxModel` (ví dụ nhân tỉ lệ 1,25) | sau lượt cuộn, hỏi-điểm ở nhiều điểm, so với `elementFromPoint` (hệ CSS) | **SAI** | sau cuộn, hỏng ở MỌI điểm thử (5/5), kể cả chỗ `elementFromPoint` thấy `div`/`button#duoi` → không phải lệch tỉ lệ |
| G-23 | 13/09 | S-23 | Sau cuộn, `DOM.getNodeForLocation` nhận toạ độ theo **trang** (cộng `scrollY`), không theo khung nhìn | hỏi-điểm ở (38, 772+1288=2060) và vài điểm trang | **ĐÚNG** | (38, 2060) → `button#duoi`. Chưa cuộn thì hai hệ trùng nhau — vì thế T1 qua mọi phép thử lúc làm. Điểm ngoài vùng đang hiện vẫn `No node found` |
| G-24 | 13/09 | S-23 | Độ cuộn đọc được bằng method SẴN CÓ: hộp `margin` của `<html>` (`DOM.getBoxModel`) lệch âm đúng bằng `scrollX/scrollY` | so với `window.scrollY` sau lượt cuộn | **ĐÚNG** | margin quad `[0,-1288,…]` ↔ `scrollY: 1288` |
| G-25 | 13/09 | S-23 | Bản vá: cộng độ cuộn (hộp `:root`) vào điểm hỏi, ở CẢ HAI lõi | `scouter:action-probe` + suite + đột biến | **ĐÚNG** | probe 11/11; suite xanh; đột biến 128/128 (thêm CU1..CU4) |
| G-26 | 13/09 | S-22 | Bản vá S-23 cũng chữa S-22 trên ghế Đức | `vong.mjs` trên `trang-thu-cham`, ghế `Dummy_Scout` (tên mới của ghế cũ `Udin_Scout`) | **SAI** | 14/09: nạp lại 255ms ĐẠT, rồi `LUOT_BAM_KHONG_TOI_NOI` — `scout.click` báo `relation:"descendant"` mà `body[data-chuot]` = 0. S-23 là lỗi KHÁC, đã vá; S-22 còn nguyên |
| G-27 | 13/09 | S-21 | `No node found` của S-21 (12/09) chính là S-23: trang lúc đó đã cuộn | — chưa có cách tái hiện lại lượt 12/09 | **CHƯA** | cùng câu lỗi; chưa đo độ cuộn lúc đó |
| G-28 | 13/09 | Udin | Scouter vượt được màn "User Limit Reached" bằng nút Try Again | `pilots/udin-optic/scripts/qua-man-cho.mjs`: wait usable → click → wait overlay absent → wait prompt usable | **ĐÚNG** | click `relation: self` (768,455); màn chắn tắt; `textarea.agent-textarea` usable |
| G-29 | 13/09 | Udin | `scout.type` vào `textarea.agent-textarea` tới được React: nút `button.agent-send-button` hết `disabled` | type → query nút Send, xem còn thuộc tính `disabled` không | **ĐÚNG** | gõ "a red car" (typed 9) → 800ms sau nút Send mất `disabled`. Chưa bấm Send |
| G-36 | 14/09 | W3 | `scout.fetch` trả kết quả **phẳng**, không bọc trong `.data` như `scout.query` | lượt gọi thật đầu tiên | **ĐÚNG** | bản đầu đọc `.data` → `Cannot read properties of undefined`. 15 khối ghim vẫn xanh vì máy giả chép đúng cái hiểu sai của tôi — **phép ghim không kiểm được hình dạng dây** |
| G-30 | 13/09 | Udin | `scout.click` nút Send tới trang: Udin nhận prompt (nút Send khoá lại / DOM đổi) | đếm phần tử trước-sau click | **ĐÚNG** | click `descendant` (352,652) → 1,5s: nút thành `stop-button`, `agent-message-item user` hiện → 68s: `stop-button` tắt, 4 `img.batch-grid-image` "Variation 1..4" |

| G-31 | 14/09 | W3 | Ảnh kết quả Udin có `src` là URL **http(s) tuyệt đối** (không phải `blob:`), nên máy phục vụ nền với tới được bằng `scout.fetch` | `scout.query img.batch-grid-image` rồi đọc `src` | **ĐÚNG** | 16 nút ảnh, `src` là `https://optic-canvas-cache-vinfast.s3.us-east-1.amazonaws.com/ephemeral/…/generated/batch-….webp` |
| G-32 | 14/09 | W3 | Một ảnh nằm dưới trần `FETCH_MAX_BODY_BYTES` (512 KiB **sau** base64 ≈ 384 KiB thô) | `lay-anh.mjs` — quá trần thì trả `FETCH_BODY_TOO_LARGE` kèm số byte thật | **CHƯA** | chưa tới bước này: G-35 chặn trước |
| G-33 | 14/09 | W3 | CDN ảnh của Udin **không đòi cookie**: `credentials: omit` (mặc định) vẫn trả 200 | `lay-anh.mjs` | **SAI, nhưng không phải vì cookie** | 403. Nguyên nhân thật ở G-35 — S3 là URL **ký sẵn**, chữ ký nằm trong query. Đừng đi bật `with_credentials`: cookie không cứu một URL thiếu chữ ký |
| G-34 | 14/09 | E2E | Ba chặng W1→W2→W3 chạy liền một mạch trên ghế thật, không cần tay người xen giữa | `e2e.mjs "<chữ MỚI>"` — mỗi lượt một prompt khác | **CHƯA** | không chạy: W3 đang CHẶN (G-35), chạy chỉ tốn credit rồi ngã ở chặng ba |
| G-35 | 14/09 | W3 | 403 là vì **chính sách che của lõi ĐỌC cắt query string** khỏi mọi `href`/`src`, mà ảnh S3 là URL ký sẵn — chữ ký nằm đúng trong phần bị cắt | đọc `stripQuery` trong `observer-probes.mjs`; đếm `src` kết thúc bằng `…` trên trang thật | **ĐÚNG** | 17/17 `src` khác nhau đều bị cắt; `scout.query` tự khai `policy: de-xuat-chat-v1` — *"href/src bị cắt query và fragment"*. Đây là **bảo vệ**, không phải bug: đừng nới |

| G-37 | 14/09 | S-22 | Trên ghế `Udin_Scout` HÔM NAY, một lượt bấm **không phải cuộn** có tới được trang không | chạy `qua-man-cho.mjs` thật: bấm Try Again rồi kiểm **màn chắn có tắt không** — kiểm bằng trang, không bằng lời báo của `scout.click` | **ĐÚNG** | `{"daChan":true,"bam":1}` — màn chắn có thật, bấm 1 lần, màn chắn tắt, ô prompt `usable`. Đây **chưa phải** G-26: G-26 hỏi ca `trang-thu-cham` (cần một tab trống trên ghế đó), còn đây là một trang khác. Nhưng nó đúng HÌNH DẠNG của S-22 (bấm không cuộn) và **không tái hiện** |

| G-38 | 14/09 | S-22 | ~~Lượt bấm chỉ tới được trang khi tab ĐANG HIỆN~~ **SAI, xem G-40** — giữ dòng để đừng ai thử lại | cùng một lệnh `vong.mjs`, chạy hai lần: một lần tab ẩn (đã có), một lần sau khi tay người bấm cho tab hiện lên | **SAI** | Vế kia đo được ở `G-40`: tab ẩn trên Chrome sạch vẫn nhận đủ cú bấm. Mối tương quan ở ghế Đức là TRÙNG LÚC, đúng như `G-07` đã kết luận 13/09 |
| G-39 | 14/09 | S-22 | `document.hasFocus()` là dấu đáng tin để biết vì sao bấm hỏng | đọc `body[data-focus]` cùng lúc với `data-hien` | **SAI** | Đo được `data-focus="co"` TRONG KHI `data-hien="hidden"` — giá trị cũ đọng lại vì trang chỉ ghi lại lúc có sự kiện. Dùng `data-hien`, đừng dùng `data-focus` |

| G-40 | 14/09 | S-22 | (vế còn thiếu của G-38) Tab **ẩn** thì lượt bấm không tới — đo trên Chrome SẠCH do phép đo tự mở | bản chép `scouter-action-reality-probe.mjs` + mở tab thứ hai cho tab trang thử thành nền | **SAI** | tab `hidden` vẫn nhận đủ: `themCuBam: 1` cả ba lượt (ẩn · ẩn · hiện lại). **G-38 SAI theo** — Đức đã đúng từ 13/09, và `G-07` giữ nguyên kết luận SAI |
| G-41 | 14/09 | S-22 | Cửa sổ Chrome **thu nhỏ** thì lượt bấm không tới | cùng phép đo, `Browser.setWindowBounds` `minimized` rồi `normal` | **SAI** | thu nhỏ vẫn `themCuBam: 1`. Đây cũng là giả thuyết ⒜ của `S-21` (G-14) — nay loại được |
| G-42 | 14/09 | S-22 | Tab **mở nền và CHƯA TỪNG được hiện lần nào** (chưa vẽ) thì lượt bấm không tới | cùng phép đo, `chrome.tabs.create({active:false})` rồi bấm ngay, sau đó cho hiện lên bấm lại | **SAI** | chưa từng hiện vẫn `themCuBam: 1` |
| G-43 | 14/09 | S-22 | Hỏng theo GHẾ (cả ghế mất đường ghi) hay theo TAB | cùng ghế `Dummy_Scout`, cùng phút: bấm ô nhập trên Udin, rồi bấm nút trên trang thử | **Theo TAB** | Udin: `:focus` 0 → 1, `relation:"self"`. Trang thử: `relation:"descendant"` mà `data-chuot` vẫn 0. Ghế vẫn ghi được; riêng tab đó thì Input biến mất |
| G-44 | 14/09 | S-22 | Trang thử đang bị che / có hộp thoại / vẽ sai nên lượt bấm rơi chỗ khác | `scout.shot` + `scout.page` chính tab đang hỏng | **SAI** | ảnh đúng trang, không lớp che, 2 phần tử tương tác. Và tay nghe đặt ở **tầng cửa sổ, pha bắt** — sai toạ độ thì vẫn phải thấy `mousedown`, mà `data-chuot` = 0: **không một sự kiện chuột nào tới trang** |

| G-45 | 14/09 | S-22 | Phiên gỡ lỗi gắn vào tab hỏng NỬA (DOM còn, Input mất); `scout.reload` gắn lại là chữa được | trên ghế Đức: reload → bấm → đo `data-chuot` | **SAI** | **Lần đầu ĐÚNG** (0→1, `data-bam` 0→1) nên tôi tưởng đã tìm ra. Chạy lại 4 lượt hai chiều: **KHÔNG TỚI cả 4**, kể cả hai lượt ngay sau khi gắn lại. Một lần chạy được **không lặp lại** thì không phải bằng chứng — nó chỉ nói rằng lỗi **CHẬP CHỜN**, đúng như mô tả gốc 12/09 (*"có chạy lúc 16:41 rồi thôi chạy từ ~16:44"*) |
| G-46 | 14/09 | S-22 | Điều hướng sau khi đã gắn gỡ lỗi làm hỏng đường Input | Chrome sạch: `chrome.tabs.update` đổi URL rồi bấm, không gắn lại | **SAI** | vẫn `themCuBam: 1`. Trên ghế Đức thì lượt sau điều hướng hay hỏng, nhưng G-45 cho thấy lượt KHÔNG điều hướng cũng hỏng → điều hướng không phải biến quyết định |
| G-47 | 14/09 | S-22 | ~~Biến còn lại: tab **đang hoạt động** (active) của cửa sổ thì bấm tới, tab nền thì không~~ | — | **RÚT 14/09** | **Đây là tab ẩn lần thứ tư.** `G-07` (Đức xác nhận đã tự debug), `G-40` (Chrome sạch, tab nền vẫn nhận đủ), `G-42` (chưa từng hiện, vẫn nhận đủ) đã giết nó, và khối *Kết luận* của chính file này đã ghi **"đừng thử lại"** trước khi tôi mở dòng này. Mở lại một giả thuyết đã đóng thì phải có **dữ kiện mới**, không phải một tương quan cũ đếm lại |

| G-48 | 14/09 | S-22 | ⚠️ **PHÉP ĐO HỎNG, không phải giả thuyết** — mọi lượt đọc `body[data-chuot]` / `#ket-qua[data-bam]` bằng **sự tồn tại** của thuộc tính chỉ thấy được lượt bấm ĐẦU TIÊN | dò `[data-bam="n"]` để lấy **giá trị**, không hỏi "có thuộc tính không" | **ĐÚNG (là lỗi của tôi)** | Đo lại bằng giá trị: `data-chuot` **6 → 7** trên chính tab mà mười phút trước tôi khai "không tới". Bộ đếm đã chạy tới 6 trong khi phép đo của tôi vẫn in "1 → 1". **Mọi dòng ĐỎ đo bằng cách cũ đều phải đọc lại** — gồm cả bốn lượt của `G-45` |
| G-49 | 14/09 | S-22 | ~~Tab **đang hiện** thì lượt bấm tới, tab **ẩn** thì không — đo GHÉP CẶP~~ | ~~`scratchpad/do-cap-doi.mjs`, 18 lượt~~ | **RÚT 14/09** | Lần thứ năm của cùng một câu hỏi, xem `G-47`. Phép đo đã chạy 18 lượt và **không kết luận được** (thiếu vế "tab hiện" vì nó cần tay Đức) — tức là nó tốn thời gian của Đức để trả lời một câu đã có đáp án từ `G-40`. Đức chốt 14/09: **thôi đo S-22** |
| G-50 | 14/09 | S-22 | Nếu còn ai mở lại S-22: thứ CHƯA thử là **Chrome tự đông cứng renderer của tab nền** (Memory Saver / tab freezing) — khác hẳn "tab ẩn", vì nó phụ thuộc **thời gian + số tab + RAM**, nên Chrome sạch chạy 30 giây không bao giờ tái hiện | `chrome://discards` xem cột *Frozen*, hoặc tắt Memory Saver rồi chạy lại — **không cần một chiến dịch đo** | **CHƯA, và cố ý để nguyên** | Khớp với ba thứ đã đo mà chưa giải thích được: hỏng **theo TAB** (`G-43`), DOM chạy mà Input mất, và mô tả gốc 12/09 *"chạy lúc 16:41 rồi thôi từ ~16:44"* — đúng hình dạng một bộ đếm giờ đông cứng. Ghi ra để **không ai phải nghĩ lại từ đầu**; đừng lấy nó làm cớ mở chiến dịch thứ sáu |
| G-51 | 14/09 | W3 | URL ký sẵn của ảnh Udin **hết hạn**, nên ảnh cũ trên trang không tải lại được | đọc `X-Amz-Expires` khai trong `src`, so mốc `batch-<ms>` với giờ hiện tại, rồi `scout.grab` thật | **ĐÚNG** | `X-Amz-Expires=900`. Mọi ảnh đang hiện là của **13/09**, cách lượt đo **~100.800 giây**. Grab trả **403** — đúng, không phải lỗi. Trình duyệt vẫn hiện chúng vì **cache**, nên nhìn màn hình thì tưởng còn sống |
| G-52 | 14/09 | W3 | `scout.grab` đọc được `src` ĐẦY ĐỦ bên trong extension (không phải bản đã che) | grab một ảnh thật; nếu nó dùng bản che (mất query) thì S3 trả **400/404**, dùng bản đầy đủ đã hết hạn thì trả **403 chữ ký hết hạn** | **ĐÚNG** | Trả đúng **403**. Đây là bằng chứng gián tiếp nhưng phân biệt được: hai đường vào cho hai mã lỗi khác nhau |
| G-53 | 14/09 | W3 | `src` mà `scout.query` trả về **không phải tiền tố** của `src` trên trang | dựng `[src^="<src đã che>"]` rồi đếm khớp | **ĐÚNG** | Khớp **0**. Lõi đọc gắn thêm dấu `…` để BÁO đã cắt query (`stripQuery`), và dấu đó không có thật trong `src`. **23 khối ghim vẫn xanh** trên bản mã hỏng vì máy giả của tôi trả `src` sạch. Đã vá + thêm khối ⓥ mang đúng hình dạng thật |
| G-54 | 14/09 | W3 | `alt` đủ để chỉ đúng một ảnh kết quả | đếm `img.batch-grid-image[alt="Variation 1"]` trên trang thật | **SAI** | Khớp **4** — mỗi lượt sinh ảnh lại đẻ ra một bộ `Variation 1..4`. 16 nút · 16 `src` khác nhau · `alt` trùng 4 lần. Phép chọn selector **tự bắt được** và rơi xuống `[src^=]`; nếu tôi đã ghim `:nth-of-type` như dự tính ban đầu thì nó tải nhầm ảnh mà không ai biết |
| G-55 | 14/09 | W2 | Trần 300s của `gui-prompt` là "quá thì hỏng" | gửi prompt mới; W2 hết giờ ở 300s, rồi ĐO TIẾP trạng thái trang | **SAI — và đây là khuyết tật của adapter** | Hết 300s, `gui-prompt` bỏ cuộc, nhưng `stop-button` **vẫn bật sau 17 phút**: Udin **vẫn đang chạy**, credit **đã tiêu**, còn lượt chạy thì vứt đi. "Quá giờ" và "hỏng" là hai câu khác nhau, và gộp chúng làm mất trắng một lượt tốn tiền |
| G-56 | 14/09 | seed | `scout.shot` chạy được trên trang Udin hôm nay | gọi hai lần liền | **SAI** | Cả hai lần `TRANSPORT_DISCONNECTED`, trong khi `scout.query` · `scout.page` · `system.capabilities` cùng lúc đó vẫn chạy. Nghi phong bì ảnh quá lớn cho tầng vận chuyển. **Chưa đào** — ghi để lượt sau khỏi tưởng là tab chết |
| G-57 | 14/09 | O8 | `scout.text` đọc được chữ của một phần tử trên trang THẬT, và từ chối đúng hai ca xấu | nạp lại extension bằng `scout.reload`, rồi gọi bốn lượt trên trang Udin | **ĐÚNG** | 19 method sau nạp lại. `.concurrency-overlay` → 100 ký tự: *"User Limit Reached… Try Again"*. `div` (khớp **146**) → `SELECTOR_AMBIGUOUS`. Selector không khớp gì → `SELECTOR_NO_MATCH`. Và nó trả lời luôn câu mà suốt buổi tôi mù: ~~**màn chắn là thật, Udin đang hết chỗ**~~ — **vế sau SAI, Đức bác 17/09**: màn chắn CÓ thật, nhưng nó là **bug giao diện của Udin** chứ không phải hết chỗ. Phép dò đọc đúng CHỮ trên màn chắn; cái tôi thêm vào là một NGUYÊN NHÂN không đo được |
| G-58 | 14/09 | O8 | Trần 5.000 ký tự có thật sự giữ được "khối lượng" như `ADR-0006` hứa không | `scout.text` trên `body` của trang Udin | **SAI — trần KHÔNG giữ được điều đó** | `body` là **một** phần tử nên hợp lệ, và cả trang chỉ **1.390 ký tự** → ra hết trong một lượt. Giá này đã được khai trước trong `ADR-0006` (*"ai chỉ được `body` thì vẫn lấy được nhiều chữ"*), nên nó là **giới hạn đã biết**, không phải bất ngờ. Không siết thêm, và đây là lý do: `scout.shot` vốn đã trả **cả trang dưới dạng ảnh** — chữ của trang không phải một kênh mới. Siết `body` thì `body > div` lách được ngay, tức là một cái khoá chỉ để nhìn cho yên tâm |
| G-59 | 14/09 | O11 | Ảnh Udin **lọt được** một phong bì Bridge | `scout.grab` một ảnh tươi (sinh 71 giây trước) | **SAI** | Ảnh thật **746.722 byte** → base64 **995.632 byte**, vượt trần thân 524.288. Và trần đó **không tuỳ tiện**: phong bì Bridge chặn ở **1 MiB**, mà `MAX_ENVELOPE_BYTES` nằm ở `_shared/bridge-host` — **lõi dùng chung với ba gói đóng băng**, nên không nới. Đây là chỗ `W3` đang tắc, KHÔNG phải một lỗi của grab |
| G-60 | 14/09 | O11 | Phần còn lại của đường grab **ĐÃ CHẠY ĐÚNG** — chỉ tắc ở kích thước | cùng lượt gọi trên | **ĐÚNG** | Lời từ chối in ra **746.722 byte**, tức là extension đã: đọc `src` đầy đủ bên trong · gọi mạng với URL ký sẵn **còn hạn** · nhận về một ảnh thật. Bốn bước đầu của `O11` xanh; chỉ bước trả về là đỏ |
| G-61 | 14/09 | O11 | Máy chủ ảnh của Udin (S3) **nhận `Range`**, nên lấy được theo từng khúc mà không cần nới trần nào | `scout.grab` có tham số `part`, mỗi khúc một lượt `Range`; kiểm `status 206` + `Content-Range` | **CHƯA** | Đường này KHÔNG thêm trạng thái nào trong extension (mỗi khúc một lượt gọi riêng) và KHÔNG nới một lớp bảo vệ nào — đó là lý do chọn nó thay vì nâng trần phong bì. Máy chủ lờ `Range` thì phải ĐỎ với câu nói rõ, đừng lặng lẽ trả cả file |
| G-62 | 14/09 | O11 | Header `Range` dùng được trong service worker của extension | chia đôi: bỏ ĐÚNG header `Range`, giữ nguyên mọi thứ khác, rồi gọi lại | **SAI** | Có `Range` → `TRANSPORT_DISCONNECTED` mọi lượt. Bỏ đúng header đó → grab chạy và trả lỗi sạch. `G-61` **SAI theo**: đường lấy theo `Range` chết. Thay bằng: tải cả tệp rồi **chỉ mã hoá khúc này** — không cần `Range`, không giữ trạng thái |
| G-63 | 14/09 | — | ~~Tầng vận chuyển có một NGƯỠNG cỡ, dưới ngưỡng thì chạy~~ | máy chủ tại chỗ trả đúng N byte, tăng dần, **mỗi cỡ thử MỘT lần** | **SAI — và sai vì phép đo, không vì tầng vận chuyển** | Bảng đầu tiên trông như một ngưỡng sạch (74.668 base64 chạy · 85.336 đứt) và tôi đã viết kết luận đó ra. Đo lại bằng **cùng một cỡ, nhiều lượt**: khúc 0 (65.536) **chạy** · khúc 14 (65.536) **chạy** · khúc 8 (65.536) **ĐỨT** · khúc 15 (12.592) chạy. Cùng cỡ, hai kết quả → **CHẬP CHỜN**, không phải ngưỡng. Mỗi cỡ thử một lần thì một lỗi chập chờn luôn trông như một ngưỡng |
| G-64 | 14/09 | S-25 | ✅ Tầng vận chuyển **rớt chập chờn** khi phong bì lớn, và đó là nguyên nhân chung của `G-56` (`scout.shot` chết hôm nay) lẫn mọi lượt `scout.grab` trả thân thật | gọi lặp cùng một khúc; đếm tỉ lệ đứt theo cỡ | **CHƯA** | Có bằng chứng đủ để **không đoán tiếp**: cùng cỡ hai kết quả khác nhau, và `TRANSPORT_DISCONNECTED` tự khai là *retryable*. Chốt chặn cỡ ở đường gửi ra đo theo `MAX_ENVELOPE_BYTES` (1 MiB) nên nó không bao giờ bắt được ca này. Cách đi tiếp **không phải** đo thêm: adapter thử lại có trần, và chữa gốc ghi thành `S-25`. — **ĐÓNG 14/09**: gốc là `G-67`, đã chữa, và `G-71` đo lại trên dây thật 15/15 ở đúng cỡ từng chết. Một chi tiết trong ô này **SAI**: `_shared/bridge-host` KHÔNG dùng chung với ba gói đóng băng — mỗi gói giữ một bản sao riêng |
| G-65 | 14/09 | O12 | ❌ ~~**Zoom out làm ứng dụng dạng artboard vẽ ra THÊM phần tử**, nên Scouter nhìn được nhiều hơn~~ | Đức zoom ra rồi zoom lại **mà không đụng gì khác**, đếm `div`/`button` hai lần | **SAI — đóng 14/09** | Đo sau khi Đức zoom: `div` **146 → 224**, `button` **10 → 35**. **KHÔNG kết luận được**: giữa hai lượt đo có một lượt nạp lại trang và một loạt ảnh mới, tức là **ba biến đổi cùng lúc**. Đây đúng cái bẫy `G-48` để lại. — **SỐ NỀN ĐO SẴN 14/09 để phép thử chỉ còn MỘT lượt gọi**: trên tab Udin `EBC59A03…`, ghế `Dummy_Scout`, `zoom 1` · khung `1090×783` · **`div` 274 · `button` 43 · `img.batch-grid-image` 36**. Đức chỉ cần **zoom RA rồi DỪNG** — đừng zoom lại, đừng nạp lại, đừng gửi prompt — rồi bất kỳ phiên nào đếm lại đúng ba selector ấy là xong. Nạp lại trang hay sinh thêm ảnh giữa hai lượt đếm là hỏng phép đo (đó là điều đã xảy ra lần trước: ba biến đổi cùng lúc). — **ĐO ĐƯỢC MỘT NỬA 14/09, và nó TÁCH dòng này làm hai câu**: Đức zoom VÀO, không nạp lại không gửi prompt, tức là **đúng một biến đổi** — phép đo sạch mà `G-48` chưa bao giờ lấy được. Kết quả: `div` **274 → 274**, `button` **43 → 43**, `img.batch-grid-image` **36 → 36**. KHÔNG đổi một phần tử nào. Và `scout.view` vẫn khai `zoom: 1`, khung nhìn vẫn `1090×783` — vì `zoom` đọc `cssVisualViewport.zoom`, tức là thu phóng của TRÌNH DUYỆT. Nên thứ Đức làm là **zoom của chính ứng dụng trên artboard**, một phép biến hình thuần hiển thị: ảnh phóng to tràn khung, bảng chat y nguyên (ảnh chụp 14/09). **Hệ quả**: dòng này trộn hai cơ chế khác hẳn nhau. ⑴ *zoom canvas của app* — **KHÔNG vẽ thêm phần tử nào**, đo xong; nếu app có ảo hoá theo khung nhìn thì zoom VÀO phải làm số đếm GIẢM, mà nó không giảm. ⑵ *zoom trình duyệt (Ctrl +/−)* — **CHƯA đo**, và đó mới là cái `O12`/`G-69` nói tới. **SỐ NỀN CHO NỬA ⑵, đo ở trạng thái hiện tại** (canvas đang zoom vào, trình duyệt 100%): `*` **581** · `div` 274 · `button` 43 · `img` 44 · `svg` 45 · `canvas` **0** (artboard này là DOM+SVG, không phải thẻ canvas). — **ĐÓNG 14/09, SAI.** Đức bấm Ctrl-trừ và dừng: `zoom` **1 → 0,667**, khung nhìn **1090×783 → 1635×1174** (đúng 1/0,667, và **2,25 lần diện tích**). Số đếm: `*` **581 → 581**, `div` 274 → 274, `button` 43 → 43, `img` 44 → 44, `svg` 45 → 45, `canvas` 0 → 0. **Không một phần tử nào được vẽ thêm.** Cơ chế dòng này nêu ra là sai, ở CẢ HAI cơ chế zoom và ở cả hai chiều: zoom canvas vào (không giảm), zoom trình duyệt ra (không tăng). ⟶ **Nhưng ảnh chụp cho thấy khung artboard có đổi**: chỗ trước đó là một ảnh phóng to tràn khung, nay thấy ba ảnh nhỏ nằm gọn. Cùng 581 nút. Nghĩa là app **xếp lại canvas theo khung nhìn mới**, chứ không vẽ thêm gì — và đó đúng là vế *"chỉ là tiện cho ảnh chụp"* mà dòng này đặt ra làm đối chứng. `O12` vì thế là **tiện ích hiển thị, không phải năng lực nhìn**. Xem `G-79` cho hệ quả với `G-69` |
| G-66 | 14/09 | O12 | Ảnh chụp cả trang (`captureBeyondViewport`) thay được zoom cho việc "nhìn toàn cảnh" | — | **SAI, do `G-63`** | Nó là một **tham số** của method đã có trong danh sách đọc, nên trông rẻ hơn hẳn. Nhưng ảnh cả trang của một artboard lớn thì to hơn ảnh khung nhìn nhiều lần, mà tầng vận chuyển đã đo được là **rớt chập chờn quanh 65 KB**. Zoom ra rồi chụp khung nhìn cho **cùng phạm vi với ít byte hơn**. Đường này chỉ mở lại được sau khi `S-25` xong. — **ĐIỀU KIỆN ĐÃ ĐỦ 14/09**: `T24` ghép được mảnh nối, nên cái chặn đã mất. Và `clip.scale` làm nốt phần còn lại: chụp cả tài liệu RỒI THU NHỎ ngay trong lượt chụp, tức là đúng cái *"cùng phạm vi với ít byte hơn"* mà dòng này nói zoom mới làm được. Nên câu kết luận **SAI** ở trên vẫn đúng với ngày nó được viết, còn hôm nay đường ấy đang chạy — xem `G-69` cho nửa còn lại |
| G-67 | 14/09 | S-25 | ✅ **GỐC CỦA `S-25`**: bộ giải khung WebSocket của máy chủ **ném lỗi ở mọi tin bị cắt mảnh**, mà Chrome tự cắt mảnh khi tin vượt ~64 KiB | đưa thẳng một tin cắt hai mảnh đúng chuẩn (`fin=0` + `opcode=0`) vào `createFrameDecoder` | **ĐÚNG** | Ném ngay ở mảnh ĐẦU: *"Fragmented WebSocket messages are not supported."* (`_shared/bridge-host/websocket-core.mjs:76`). Khớp cả bốn triệu chứng: ngưỡng ~65 KB · **chập chờn** (Chrome cắt hay không tuỳ nhịp đệm, nên cùng một cỡ lượt chạy lượt đứt) · `scout.shot` chết · mọi lượt grab trả thân thật chết. Đây là một ca **CHƯA VIẾT**, không phải một lớp bảo vệ — nên sửa nó không phạm luật *"không nới bảo vệ"*; bản sửa thuần THÊM VÀO |
| G-68 | 14/09 | S-25 | Sau khi ghép được mảnh nối, trần khúc của `scout.grab` nâng lại được và một ảnh Udin về **một hoặc hai** khúc | sửa `T24` xong rồi nâng `FETCH_MAX_BODY_BYTES`, đo lại bằng `scratchpad/may-do-co.mjs` | **ĐÚNG** | 64 KiB đặt ra **để né đúng `G-67`**, không phải vì một giới hạn thật. `T24` xong 14/09, trần nâng lên **512 KiB** (`T31`), và Đức nạp lại extension cùng ngày — **đo được trên dây thật ở `T33`**: bốn ảnh 764.000 · 686.422 · 787.096 · 603.660 byte về trong **2 · 2 · 3 · 2** khúc, tổng **9 đơn vị** trần ghi cho cả bốn. Lượt trước đó: **16 đơn vị cho MỘT ảnh**. Xem `G-78` cho lý do ảnh thứ ba tốn ba khúc |
| G-69 | 14/09 | O12 | **Một lượt thu phóng SỐNG QUA NHIỀU LƯỢT GỌI là bất khả ở kiến trúc hiện nay**: `observer-engine.js` gắn rồi THÁO debugger quanh từng lượt gọi (dòng 123 và 160), mà `Emulation.setDeviceMetricsOverride` sống theo phiên debugger | mở `input.zoom`, gọi nó, rồi gọi `scout.view` ở một lượt sau và xem `zoom` có đổi không | **CHƯA** | Chưa đo, và **cố ý chưa làm cái để đo**: nếu đúng thì một `scout.zoom` đứng riêng sẽ trả về *"đã thu phóng"* rồi không còn gì thu phóng nữa — một lệnh nói dối theo đúng nghĩa đen. Đổi vòng đời gắn debugger là quyết định KIẾN TRÚC (nó giữ debugger cắm vào tab của Đức giữa các lượt gọi, và dải băng *đang gỡ lỗi* ở lại), nên nó là câu của Đức chứ không phải một tham số. **Nửa ⑴ của `O12` không cần câu trả lời này** và đã làm xong bằng `scout.shot full_page + scale` |
| G-71 | 14/09 | S-25 | ✅ **Sửa xong `T24` thì tin lớn đi được trên DÂY THẬT, và hết chập chờn** | `scout.shot format:"png"` lên bảng bên — phong bì **87.298 byte (85,3 KiB)** — lặp **15 lượt liền** trên máy chủ đã sửa, extension **KHÔNG nạp lại** | **ĐÚNG** | **15 chạy / 0 đứt**, cỡ giống nhau từng byte cả 15 lượt. Đúng cỡ `85.336` mà lần chia đôi đầu tiên ghi là **ĐỨT**, và đúng vùng `G-63` đo được là chập chờn. Đây là phép đo sạch nhất có thể lấy lúc này: `S-25` nằm ở **máy chủ**, nên nó kiểm được mà không cần đụng tới extension — mã extension y nguyên bản đang chạy từ trước. `G-64` đóng theo: nguyên nhân chung đã tìm ra và đã chữa |
| G-72 | 14/09 | I5 | ✅ **`Input.dispatchMouseEvent` kiểu `mouseWheel` KHÔNG BAO GIỜ trả lời**, và lượt treo đó **giữ `chrome.debugger` cắm vào tab** nên khoá mọi lệnh sau | gọi `scout.scroll` (bản bánh xe) trên tab Udin và trên bảng bên; rồi gọi `scout.hover` — **cùng method, cùng tab** — làm đối chứng | **ĐÚNG** | Bánh xe: **treo 35.007ms** rồi máy chủ bỏ cuộc; sau đó mọi lệnh trên tab ấy trả `TARGET_ALREADY_ATTACHED` — thử lại **12 lượt trong 120 giây vẫn kẹt**, phải `scout.reload` mới gỡ. Đối chứng `scout.hover`: **OK 5.171ms** trên đúng tab ấy. Nên chỗ hỏng là **riêng `mouseWheel`** — không phải tab ẩn, không phải `Input.*`, không phải đường ống. Hai hệ quả: ⑴ `scout.scroll` bỏ đường bánh xe, dùng `DOM.scrollIntoViewIfNeeded` (cơ chế `input.click` đã chạy từ 06/09); ⑵ gốc sâu hơn — **một lệnh CDP treo thì khoá cả tab** — chữa bằng `CDP_HAN_MS = 20000` ở **cả hai** lõi, để `finally { detach }` chạy được. Con `V10` `V11` canh hai chỗ đó |
| G-73 | 14/09 | I5 O13 | ✅ **`scout.scroll` bản `scrollIntoViewIfNeeded` CUỘN ĐƯỢC THẬT, và `scout.view` đo được lượt cuộn đó** | trang thử cao 2.762px: `scout.view` → `scout.scroll` tới `#cuoi-trang` → `scout.view` | **ĐÚNG** | cuộn `y` **0 → 1.972**, `conLai y` **1.971 → 0**; cuộn ngược tới `#o-nhap` thì về `y=0`. Trang **không đếm được lượt bánh xe nào** (`data-banhxe` vắng) — đúng thiết kế, cơ chế mới không bắn sự kiện chuột. Và đó là lý do nó **miễn nhiễm `S-22`**: lệnh DOM, không đi đường sự kiện chuột |
| G-74 | 14/09 | I6 I7 | ❌ ~~Rê chuột / bấm phải / bấm đúp TỚI ĐƯỢC trang trên ghế Đức~~ | trang thử đếm `mousedown` · `contextmenu` · `dblclick` ở tầng cửa sổ, pha bắt | **SAI — và là `S-22`, KHÔNG mở lại** | Cả ba lệnh trả `ok`; trang **không đếm được lấy một lượt nào**, kể cả của một lượt bấm THƯỜNG. Đó đúng là `S-22`, đã đo kỹ ngày 13–14/09 (`G-02`…`G-07` loại hết các nguyên nhân dễ) và **Đức chốt NGỪNG ĐIỀU TRA 14/09 vì chi phí**. Ghi ở đây để lượt sau không đo lại lần thứ sáu. Hệ quả cho bảng năng lực: `I6` `I7` dừng ở `CÓ` — *lệnh hoàn tất* không phải *trang đã nhận* |
| G-75 | 14/09 | T16 | ✅ **Dấu chẩn đoán của trang thử KHÔNG đọc được bằng giá trị thuộc tính** — phải đọc bằng SỐ KHỚP selector | đọc `attributes["data-chuot"]` từ `scout.query`, rồi đọc lại bằng `body[data-chuot="1"]` và đếm khớp | **ĐÚNG** | Chính sách che cắt mọi `data-*` khỏi phần thuộc tính trả về, nên đường thứ nhất **luôn ra `null`** — kể cả khi trang có ghi thật (chứng minh: `body[data-cuon="0"]` khớp 1). Tôi đã trượt đúng cái bẫy này một lần trong lượt đo hôm nay và suýt kết luận *"lượt bấm không tới nơi"* từ một phép đo hỏng. Đây là món nợ `T16`, nay có giá cụ thể |
| G-70 | 14/09 | O13 | ✅ `Page.getLayoutMetrics` trả về số đo **dùng được** trên trang thật, không chỉ trên trang giả | gọi `scout.view` trên tab Udin và trên bảng bên của chính Scouter | **ĐÚNG một phần** | Udin: khung `1090×783`, tài liệu `1090×783`, `conLai (0,0)` — **artboard KHÔNG cuộn tài liệu**, nó vẽ trong một khung cố định. Bảng bên: khung `404×743`, tài liệu `404×3002`, `conLai y=2259` — đọc đúng một trang cuộn được thật. Cả hai trả `donVi: "css"`. **Phần CHƯA**: so `scroll` trước/sau một lượt `scout.scroll` — bản bánh xe treo (`G-72`) nên phép so đó chưa chạy được, phải đo lại với bản `scrollIntoViewIfNeeded` |
| G-76 | 14/09 | W4 T34 | ✅ **Câu trả lời chữ của agent đọc được bằng MỘT selector khớp đúng một** — và mọi selector "cái cuối cùng" nghe hợp lý đều KHÔNG khớp một | hỏi lại trang từng ứng viên bằng `scout.query`, rồi `scout.text` cái khớp 1 | **ĐÚNG** | `.agent-message-item:last-child .markdown-content` → khớp **1**, đọc ra đúng câu trả lời cho prompt vừa gửi (**251 ký tự**, không cắt). Nhưng `.markdown-content:last-of-type` → **20**, `.chat-message.agent:last-of-type` → **20**: `:last-of-type` xét theo TÊN THẺ trong TỪNG cha, nên trên một danh sách tin nhắn nó ra mỗi cha một cái. Một adapter tin vào tên selector sẽ đọc tin nhắn của **lượt khác** và không ai biết — cùng cái bẫy `G-54` (`alt` khớp 4 ảnh), và cùng phép kiểm cứu được nó |
| G-77 | 14/09 | W7 T36 | ✅ **Xoá ô prompt rồi KIỂM BẰNG TRANG thì gửi được lượt hai trên cùng ô**, chữ mới không dính chữ cũ | cố ý `scout.type` chữ lạ vào ô để dựng lại đúng thế chặn, rồi chạy E2E có cờ `--xoa-o-cu` | **ĐÚNG** | Gõ sẵn `"chu con sot lai cua luot truoc"` → nút Send mở (đúng thế từng chặn `W7`). Không cờ: **vẫn từ chối**, không gõ không bấm — lớp bảo vệ còn nguyên. Có cờ: `scout.clear` → chờ nút Send **khoá lại** → gõ → gửi → 4 ảnh mới. Bằng chứng mạnh nhất không phải lời báo của lệnh xoá mà là **câu trả lời của agent**: *"options for the slate coaster and brass compass"* — không một mẩu nào của chữ sót lại. Chỗ dễ làm sai: hạ lời từ chối xuống thay vì thêm cờ, tức là nới một lớp bảo vệ cho tiện |
| G-78 | 14/09 | T31 T33 | ✅ **Trần khúc 512 KiB là trần SAU base64**, nên số khúc tính trên **393.216** byte thô — không trên 524.288 | đối chiếu `bytes` với `khuc` của bốn ảnh `T33`, rồi đọc lại `FETCH_MAX_RAW_BYTES` | **ĐÚNG** | `FETCH_MAX_RAW_BYTES = floor(512·1024 / 4) · 3 = 393.216`, **suy ra từ trần chứ không gõ riêng**, nên hai số không lệch nhau được. Khớp cả bốn ảnh; ảnh 787.096 byte sang khúc **thứ ba** vì vượt 2 × 393.216 đúng **664 byte**. Hệ quả để ước lượng: một ảnh Udin ~700 KB tốn **2 đơn vị**, và ranh giới nằm ở **768 KB** chứ không phải 1 MB — ai tính bằng 512 KiB sẽ ước thiếu một phần ba |
| G-79 | 14/09 | O12 G-69 | ✅ **Không cần đổi vòng đời gắn debugger để có thu phóng: thu phóng của TRÌNH DUYỆT là một thiết lập của Đức, và `scout.view` đọc được nó** | đọc `scout.view` ngay sau khi Đức bấm Ctrl-trừ | **ĐÚNG** | `zoom: 0,667` · khung nhìn nở từ `1090×783` lên `1635×1174`. Đây là chỗ `G-69` tưởng phải trả bằng một quyết định KIẾN TRÚC: giữ `chrome.debugger` cắm vào tab giữa các lượt gọi để `Emulation.setDeviceMetricsOverride` sống sót — cái giá là dải băng *đang gỡ lỗi* ở lại vĩnh viễn trên tab của Đức. **Đường rẻ hơn nhiều**: Chrome nhớ mức thu phóng **theo từng site**, nên Đức đặt MỘT lần là xong, và adapter chỉ cần `scout.view` để **kiểm** rằng nó đang ở mức mình cần (từ chối nếu không, thay vì tự ý đặt). Không lệnh mới, không quyền mới, không đổi kiến trúc. **Đề xuất đóng `O12` ở nửa ⑴ + dòng này; `G-69` hết là câu chặn** — Đức vẫn là người chốt, nhưng nó không còn chặn gì nữa. **Phép kiểm còn nợ đã trả cùng ngày**: Đức nạp lại tiện ích và F5 trang, đọc lại `scout.view` → `zoom` vẫn **0,667**, khung nhìn vẫn `1635×1174`. Mức thu phóng **sống qua lượt nạp**, đúng như Chrome nhớ theo site. Nên đường này dùng được thật: Đức đặt một lần, adapter `scout.view` để kiểm |
| G-80 | 14/09 | T16 | ✅ **Dấu chẩn đoán đọc được bằng GIÁ TRỊ nếu chép sang CHỮ** — `scout.text` không bị chính sách che cắt | thêm `#dau-chan` vào trang thử, đọc một lượt | **ĐÚNG** | Một lượt `scout.text` ra cả bảng: `chuot=0 phim=0 chuotphai=0 bamdup=0 banhxe=0 cuon=0 bam=0 hien=hidden focus=khong`. Đây là `T16`, nợ do `G-48`/`G-75` để lại — trước đó mọi dấu là `data-*`, mà chính sách che cắt sạch `data-*` khỏi phần thuộc tính, nên đường duy nhất là **đoán giá trị rồi đếm khớp** (`[data-chuot="1"]`, `="2"`, …) — không dùng được cho một bộ đếm. Chỗ đặt cũng là một phép đo: `position:fixed` để không nới vùng cuộn (`G-73` đo độ cuộn trên chính trang này), `pointer-events:none` vì nó nằm ở góc 0,0 |
| G-81 | 14/09 | T16 S-22 | ✅ **Dụng cụ đo phải sống được dưới đúng điều kiện đang đo** — bản `#dau-chan` đầu tiên vẽ lại qua `setTimeout`, và tab thử chạy ẩn | so số của bảng với `scout.view` | **ĐÚNG, và là lỗi của tôi** | Bản đầu gói lượt vẽ trong `setTimeout(…, 0)` cho chắc thứ tự. Tab này chạy **ẩn** (`hien=hidden`, đúng quyết định `D3`), mà Chrome **bóp nghẹt bộ đếm giờ của tab nền** — nên bảng đứng im và tôi suýt kết luận *"lệnh cuộn không cuộn"*, **ngược hẳn `G-73`** đã đo cuộn thật 0 → 1.972. Thứ cứu chỗ này là **nghi dụng cụ trước khi nghi phép đo cũ**. Chữa: vẽ thẳng ở pha sủi — pha sủi vốn đã chạy sau pha bắt của cùng một sự kiện, nên `setTimeout` chẳng mua được gì mà bán mất phép đo |
| G-82 | 14/09 | S-22 T7 | ❌ ~~**`S-22` là của TAB chứ không của ghế, và khác biệt là tab đang hiện / tab nền**~~ | đối chiếu tab Udin với tab trang thử trong cùng một phút | **SAI — và là LỖI CỦA TÔI, đừng mở lại lần thứ bảy** | ⑴ Vế đầu **không mới**: `README.md` mục `scout.click` đã khai sẵn *"hỏng theo từng tab, không theo hồ sơ Chrome"* từ trước. Tôi tưởng mình phát hiện, thật ra chỉ đọc sót lời khai có thẩm quyền. ⑵ Vế sau **chính là `G-07`** — *tab ẩn làm mất sự kiện nhập* — đã **SAI**, đã kiểm lại 14/09 (`G-40`) bằng phép đo hai chiều, và Đức tự debug 13/09 xác nhận tab ẩn không ảnh hưởng; `G-08` còn tái hiện lỗi trên một Chrome riêng. ⑶ Thứ tôi gọi là *số liệu mới* (Udin chạy được, trang thử không, cùng ghế cùng phút) là một **tương quan đếm lại**, không phải dữ liệu mới — mà mở lại một dòng SAI thì phải có dữ liệu mới. Tôi đã suýt tiêu 5 giây của Đức cho lần mở lại **thứ sáu** của cùng một giả thuyết. Giữ dòng này lại làm cái chặn cho lượt sau: **ba số đo `chuot=0 phim=0 bam=0` trên trang thử KHÔNG phải bằng chứng mới về `S-22`** — nó là đúng cái `S-22` đã khai. `S-22` do Đức chốt NGỪNG ĐIỀU TRA 14/09 vì chi phí, và nó vẫn đóng |
| G-83 | 14/09 | T7 | ✅ **Chặng ② của `T7` sinh ra một báo cáo TỰ KHAI LÀ ĐỦ mà adapter Udin KHÔNG thể dựng từ nó** — *dò xong* và *dò đủ* là hai chuyện khác nhau, và chưa có gì đo chuyện thứ hai | chạy chặng ② trên Udin (không sửa dòng nào riêng cho Udin), rồi tìm trong báo cáo sáu thứ mà `udin-optic` thật sự dùng | **ĐÚNG** | Báo cáo **219.730 byte**, cả ba phép dò khai `hasMore: false` — **không bị cắt**. Vậy mà tìm trong đó: `agent-send-button` **CÓ**, `agent-message-item` **CÓ** — còn `batch-grid-image` (kết quả ở đâu) và `markdown-content` (agent nói gì) **VẮNG**. ⚠️ **SỬA TẠI CHỖ 14/09 — vế thứ ba của câu trên là LỖI CỦA TÔI:** tôi khai `agent-prompt` vắng, nhưng `agent-prompt` **không phải tên class nào cả** — ô prompt là `agent-textarea`, và nó **CÓ** trong báo cáo ngay từ lượt dò đầu. Tôi grep một chuỗi tự nghĩ ra rồi đọc *không thấy* thành *trang không có*. Bài học cùng họ với [[G-82]]: **kiểm tên trước khi kết luận về sự vắng mặt** — một phép tìm chỉ trả lời đúng câu mình gõ ra. Số thật: thiếu **hai** trong sáu, không phải ba. Lý do không phải trần: `scout.page` chỉ kiểm kê phần tử **tương tác**, `scout.a11y` chỉ thấy thứ có vai trò trợ năng, `scout.tree` sâu tối đa **10** mà ảnh kết quả nằm sâu hơn. Selector thật của `udin-optic` ngày xưa tìm bằng `scout.query` dò tay, **không phải** từ ba phép này. **Hệ quả**: `hasMore: false` là một **xanh giả** — nó nói *tôi trả hết thứ tôi tìm*, không nói *tôi tìm đủ thứ anh cần*. Đây đúng loại lỗi đã cắn gói này nhiều lần, và `T7` sinh ra chính là để lộ nó. Chặng ② phải đóng bằng **ba câu trả lời được** ⟨gõ ở đâu · bấm ở đâu · kết quả hiện ở đâu⟩, hôm nay nó trả lời được **một** |
| G-84 | 14/09 | T7 G-83 | ✅ **`truncated` của `scout.tree` KHÔNG canh nhát cắt độ sâu — nó chỉ canh ngân sách nút** | đếm nút có `childNodeCount > 0` mà không có `children` trong chính báo cáo đã khai là đủ | **ĐÚNG, và đây là nguyên nhân gốc của `G-83`** | Ở mép `depth`, CDP không gửi `children` nhưng vẫn khai `childNodeCount` **thật** — hai con số đá nhau, và chưa ai đếm. Báo cáo Udin: `truncated:false` · `nodeCount:202` trên trần **500** (ngân sách còn thừa hơn nửa) mà **42 nhánh cụt, 79 nút con rơi ra ngoài**, gồm cả 36 ảnh kết quả. Sửa: `dom.tree` trả thêm `cutByDepth` + `childrenDropped`, và đánh dấu **từng nhánh cụt tại chỗ** để người đọc biết đi tiếp từ đâu. **Nâng `max_nodes` không chữa được** — đó là lý do câu *đừng nâng trần* ở `G-83` đúng. Trang giả của phép ghim cũ trả `childNodeCount: 0` ở mép, tức là vẽ một cái cây lúc nào cũng vừa khít lượt hỏi: **phép ghim mù vì trang giả mã hoá niềm tin của người viết nó** |
| G-85 | 14/09 | T7 chặng ④ | ✅ **Đếm một tập vừa THÊM vừa RỤNG thì không bao giờ thấy nó lớn lên** | chạy adapter thật trên Udin với luật *chờ số kết quả tăng* | **ĐÚNG** | Udin sinh 4 ảnh mới trong lúc 8 ảnh cũ hết hạn và rụng khỏi DOM: **36 → 32**. Lượt chạy **thành công**, ảnh có thật, mà adapter quay vòng chờ **590 giây** rồi bị giết — credit đã tiêu, kết quả không ai nhận. Sửa: hỏi *có **THÀNH VIÊN MỚI** không*, nhận dạng theo `src`→`href`→`id` (cả ba đều là chỗ HTML đặt danh tính, không phải hiểu biết về trang nào). Lượt chạy ngay sau: `ketQuaTruoc 32 · ketQuaSau 32 · **ketQuaMoi 4**` — đúng ca mà phép đếm mù hoàn toàn. Họ hàng với [[G-55]]: **con số dễ đếm gần như không bao giờ là con số cần đếm** |
| G-86 | 14/09 | T7 chặng ② | ✅ **Chữ ký không class là TẬP CHA của mọi chữ ký có class cùng thẻ** | đo `khop` thật của từng chữ ký bằng `scout.query` | **ĐÚNG** | Gom theo `nodeName` + bộ class **y hệt** thì `IMG` (không class) là một nhóm **3**. Đọc nó như một selector CSS thì `img` khớp **39** — hơn `img.batch-grid-image` (32) và **luôn** thắng mọi phép xếp theo số khớp. Một chiến thắng không mang tin gì: nó chỉ nói *có nhiều ảnh*, không nói *ảnh kết quả là nhóm nào*. Sửa: đích thao tác xếp theo `khop` đo được (khớp đúng một lên đầu, luật gói số 7), vùng kết quả xếp theo `gom` — cỡ nhóm cùng bộ class |
| G-87 | 14/09 | T7 chặng ②④ | ✅ **`khop` suy ra từ chữ ký là một xanh giả nữa** | so con số gom được với con số `scout.query` trả về | **ĐÚNG** | `BUTTON.create-mode-btn` gom đúng **1** phần tử (cái còn lại có thêm class `active`) nhưng đọc như selector thì khớp **2**: chữ ký gom theo bộ class *y hệt*, còn CSS khớp theo *tập con*. Cả báo cáo đứng trên ba chữ **khớp đúng một**, nên một con số suy ra là một cái xanh giả — và chặng ④ đã bỏ qua đúng một ứng viên nút vì nó. Sửa: hỏi lại trang cho **từng** chữ ký. **Đọc không tốn gì; đoán tốn một lượt chạy tiêu tiền** |
| G-88 | 14/09 | T21 | ✅ **Hỏi HTML/ARIA trước, hỏi hình dạng class sau** — luật dựng chữ ký cũ bỏ qua lời khai của trang để đi đoán vân tay | chạy chặng ② trên ChatGPT, so ứng viên *gõ ở đâu* với thuộc tính thật | **ĐÚNG** | Bản đầu dựng chữ ký thuần `thẻ.class`. Trên Udin (class ít, có nghĩa) ra đúng; trên ChatGPT ra `a.interactive-bg-secondary.…print\:hidden` — mười class tiện ích, dài, giòn, **và sai**. Trang đã tự khai hết: ô thật là `#prompt-textarea` mang `role=textbox`, ô giả `#upload-files` mang `type=file` + `tabindex=-1`, nút gửi mang `type=submit`. **Luật che KHÔNG cắt những thứ đó** — nó là danh sách CHO PHÉP 24 tên, và `id` · `data-testid` · `aria-label` · `role` · `type` đều ở trong. Báo cáo không thiếu dữ liệu, nó thiếu một luật biết dùng dữ liệu. Sửa: móc theo bậc `#id` → `[data-testid]` → `thẻ.class`, cộng điểm theo `role=textbox` / `type=submit`, trừ điểm `tabindex=-1`, và loại `INPUT` có `type` không gõ được. Sau khi sửa: **cả Udin lẫn ChatGPT đều ra đúng ô nhập ở vị trí đầu**. Kèm theo: class tiện ích không phải định danh CSS hợp lệ (`text-[13px]`, `md:w-1/2`) nên phải thoát, và một chữ ký hỏng **không được giết cả lượt dò** |
| G-89 | 14/09 | T21 | ✅ **Câu *bấm ở đâu* đôi khi KHÔNG CÓ LỜI ĐÁP lúc dò — cái nút chưa tồn tại** | hỏi `#composer-submit-button` trên ChatGPT lúc ô prompt trống | **ĐÚNG, và nó đổi kiến trúc adapter** | `scout.query` trả **0**. ChatGPT không vẽ nút gửi khi ô trống; nó chỉ hiện ra sau lượt gõ. Udin thì ngược lại — nút nằm sẵn đó với `disabled`. Nên **một báo cáo chụp lúc chưa gõ không thể chứa cái nút**, và mọi luật lọc *"chữ ký khớp đúng một trong `bam_o_dau`"* đều mù ca này. Hệ quả: adapter thôi lấy ứng viên từ báo cáo, thay bằng **so chính trang trước và sau lượt gõ** — nút cần tìm là nút đổi từ *chưa bấm được* sang *bấm được*, mà **chưa có mặt cũng là một cách chưa bấm được**. Chia lại nguồn cho ba câu: báo cáo trả lời *gõ ở đâu* và *kết quả ở đâu*; **chính trang** trả lời *bấm ở đâu*. Đo lại trên Udin sau khi đổi: vẫn khép, 4 ảnh mới trong 59 giây |
| G-90 | 14/09 | T21 | ⚠️ **Câu *kết quả hiện ở đâu* KHÔNG tổng quát** — *kết quả* là khái niệm ngữ nghĩa mà HTML không đánh dấu | so câu trả lời trên Udin với trên ChatGPT | **ĐÚNG, và CHƯA CÓ LỐI RA** | Trên Udin kết quả là một bầy `img` cùng class → `img.batch-grid-image` (32) đúng ngay. Trên ChatGPT kết quả là **chữ**, và thứ duy nhất đánh dấu nó là `data-message-author-role` — thuộc tính này **không** nằm trong danh sách cho phép 24 tên, nên báo cáo không thấy. Câu trả lời báo cáo đưa ra là `li.list-none` (31): sai. Ba lối còn để ngỏ, chưa chọn: ⑴ nới danh sách cho phép — **đổi luật an toàn, phải hỏi Đức, và nới một lớp bảo vệ cho tiện là thứ repo cấm**; ⑵ nhận vùng kết quả bằng THÍ NGHIỆM y như cái nút (chụp trước/sau, cái gì mọc thêm sau lượt bấm là kết quả) — không cần quyền mới, nhưng chỉ biết được SAU khi đã tiêu tiền một lượt; ⑶ chấp nhận rằng câu này cần một dòng người viết. Đây là thứ `T21` còn nợ |
| G-91 | 15/09 | T9 | ✅ **`system.ping` KHÔNG kết thúc ở máy chủ — nó đi tới extension** | chạy `kiem-cai-dat.mjs` bản đầu trên máy đang có hai ghế | **ĐÚNG, và nó làm hỏng đúng bước sinh ra để chẩn đoán** | Bản đầu của bộ kiểm cài đặt dùng `system.ping` làm bước ① với lý do *"nó kết thúc ở máy chủ nên tách được Bridge-chưa-chạy khỏi extension-chưa-nối"*. Lượt chạy thật đầu tiên đỏ ngay bước một: `TARGET_AMBIGUOUS`. Tức là bước chẩn đoán **chết vì đúng cái nó định chẩn đoán**. Method thật sự do máy chủ tự trả lời là `bridge.sessions` — và nó còn trả luôn danh sách ghế, nên đổi sang nó thì bước ① làm được nhiều việc hơn với ít giả định hơn. Bài học chung: **một công cụ chẩn đoán phải sống được dưới đúng điều kiện hỏng mà nó đi tìm** — cùng một câu với `G-81`, lần này ở tầng dây thay vì tầng trình duyệt |
| G-92 | 15/09 | T21 | ❌ **Gộp `transport.mjs` về `_shared` được, y như đã gộp lõi host** | đo hai bản, rồi đọc lại xem AI NÀO nạp file | **SAI — chặn cứng, và tôi đã viết nó thành chặng ① của lộ trình T21 trước khi đo** | Lõi host gộp được vì nó là tiến trình **Node** đọc cây repo: `../../../_shared/` là một đường dẫn đĩa bình thường. `transport.mjs` thì **chạy bên trong extension** (`background.js` service worker + `sidepanel.js`). Chrome nạp thư mục `v0.1.0/` làm GỐC gói; mọi `import` phân giải thành URL `chrome-extension://<id>/…` và `..` bị kẹp lại ở gốc — nên `../../../_shared/x.mjs` thành `chrome-extension://<id>/_shared/x.mjs`, không tồn tại, service worker không đăng ký được. **Ranh giới Node/Chrome mới là thứ quyết định gộp được hay không, không phải số dòng giống nhau.** Hai đo phụ: ⑴ hai bản hôm nay **giống nhau TỪNG BYTE** (cksum 3518328594, 30113 byte) — con số *"khác 18 dòng"* tôi khai 15/09 là **đo sai**; ⑵ `ADR-0021 ⑵` đã chốt sẵn đường chép-nguyên-văn + phép ghim so từng byte, và tôi viết chặng ① mà **không đọc ADR đang cai quản đúng file đó**. Bài học: lộ trình cũng là một lời khai — phải đo và phải tra sổ trước khi cam kết |
| G-93 | 15/09 | T21 | ❌ **Tách Udin thành extension riêng tốn cỡ như `hnx-fetch` đã tốn: transport + bridge-core** | đếm method Udin THẬT SỰ gọi, rồi hỏi method nào cần lõi nào | **SAI, thiếu ~2.100 dòng — và chỗ thiếu đúng là chỗ nguy hiểm nhất** | Tôi lấy `hnx-fetch` làm thước mà **không hỏi vì sao thước đó ngắn**: `hnx-fetch/AGENTS.md` ⑴ nói thẳng *"không có quyền `debugger`, và đó là lời hứa lớn nhất của gói"* — cả gói chạy trên **một** lệnh `scout.fetch`, nên nó chưa bao giờ phải mang theo bộ máy bấm/gõ. Udin gọi **5 lệnh GHI** (`click` · `type` · `clear` · `grab` · `navigate`), nên gói mới buộc phải mang `scouter-probes.mjs` (1.181) + `scouter-actions-core.mjs` (938) + `scouter-seed-core.mjs` (715, **chứa cái phanh**). Bài học: **một tiền lệ chỉ đo được việc mới nếu hai việc cùng bề mặt quyền** — chép con số của tiền lệ mà bỏ qua điều kiện sinh ra con số đó là đo sai, y như `G-92` đo `transport.mjs` mà bỏ qua ranh giới Node/Chrome. Hai lần trong một ngày, cùng một hình dạng lỗi. Đức được đưa con số trước khi làm và **chốt vẫn tách**, vì lý do khác phép đo: *"Scouter sẽ còn thay đổi nhiều, UI của Udin cũng sẽ đổi cho phù hợp usecase"* — tách rời NHỊP THAY ĐỔI. Nên bảy tệp chép bị ghim so **từng byte** (`udin-optic/v0.1.0/tests/be-mat-hep-smoke.mjs` khối ⑷): bản chép được phép trôi, không được phép trôi mà không ai biết |
| G-94 | 15/09 | T21 | ❌ **Thu hẹp `host_permissions` về đúng trang làm việc là đủ cho gói Udin** | chạy live thật từ extension mới, W1→W4 | **SAI — ảnh KHÔNG nằm trên trang làm việc** | W1 vượt màn chờ ĐẠT, W2 gửi prompt ĐẠT, W3 chết: `scout.grab` trả `Failed to fetch` trên `optic-canvas-cache-vinfast.s3.us-east-1.amazonaws.com`. Gốc: `scout.grab` gọi `fetch` **trong service worker** (phải thế, vì header `Range` giết service worker — `G-62`), nên một tên miền không khai trong `host_permissions` bị CORS chặn. Scouter không bao giờ gặp vì nó mở `<all_urls>`. **Bảy phép ghim + suite gốc xanh + máy chủ chạy thật đều không thấy — vì không cái nào gọi ra ngoài internet.** Bài học chung, và nó rộng hơn cái bucket này: **thu hẹp quyền là một thay đổi HÀNH VI, không phải một dòng khai báo**, và thứ duy nhất đo được nó là một lượt chạy thật. Chữa bằng đúng một bucket có tên, không phải `s3.amazonaws.com/*`; khối ⑵ so `deepEqual` nên thêm một tên miền cũng đỏ |
| G-95 | 16/09 | U0 | ❓ **`chrome.tabs.setZoom` đòi quyền `tabs`, nên `U2` sẽ phải đụng manifest** | `npm run udin:zoom-probe` — nạp CHÍNH thư mục `udin-optic/v0.1.0` (manifest nguyên vẹn, KHÔNG khai `tabs`) vào một Chrome sạch qua `Extensions.loadUnpacked`, rồi gọi `chrome.tabs.*` từ trong `sidepanel.html` | **SAI — không cần `tabs`. Và phép đo lật ngược chỗ đặt lớp an toàn** | Chrome 153: `chrome.tabs` **có mặt** dù manifest không khai; `setZoom(1.2)`/`setZoom(0.8)` chạy và `getZoom` đọc lại **đúng y** 1.2 / 0.8, không trôi một phần nghìn nào. Nên `U2` không đụng `manifest.json`, không đụng hợp đồng `deepEqual` của khối ② — quyết định ⓪ c ⑴ của `CHUOI-VIEC` thành vô dụng, theo chiều tốt. **Nhưng chỗ đáng đọc nằm ở nhánh còn lại:** `setZoom` **KHÔNG** bị `host_permissions` chặn — nó phóng to được cả một tab `file://` ngoài mọi quyền. Thứ duy nhất chặn Udin phóng nhầm tab lạ là **`tab.url` bị GIẤU** ở tab ngoài quyền (đo: `url` đọc được ở tab `127.0.0.1`, `null` ở tab ngoài). Tức lớp an toàn nằm trên đường **ĐỌC**, không trên đường **GHI** — ngược hẳn trực giác, và ngược cả với `G-94` nơi quyền chặn thẳng đường ghi. Hệ quả cho người sửa sau: ai *"chữa lỗi nút zoom xám"* bằng cách cứ zoom tab đang xem khi không đọc được `url` là **vừa gỡ đúng lớp chặn duy nhất**. Ba gói `duc-auto-*` không trả lời được câu này vì cả ba đều khai `tabs` cho việc khác — đúng bẫy `G-93`. Một đo phụ: bản đầu của phép đo tìm tab ngoài quyền bằng tiền tố `file:`, mà tab đó **không có `url`**, nên nó trượt sạch nhánh này và suýt khai *"đo rồi"* cho một nhánh chưa hề chạy |

| G-96 | 16/09 | U3 | ❓ **Bảng bên chạy được sáu bước của `kiem-cai-dat.mjs`, bước ① hỏi `bridge.sessions`** | đọc `scouter-transport-loopback.mjs` đếm xem extension GỬI được những khung nào ra dây | **SAI — chặn cứng, và tôi đã viết nó thành tiêu chí đóng của `U3` trước khi đo** | Extension **chỉ trả lời**: ba loại khung duy nhất nó gửi ra dây là `auth`, `keepalive`, `rpc_response`. Không có đường phát đi một YÊU CẦU, nên bảng bên không gọi được `bridge.sessions` — cũng không gọi được method nào khác của máy chủ. Mở đường đó là đổi hình dạng dây = đổi luật an toàn → hỏi Đức → chuỗi dừng. **Chữa không phải bằng cách cắt bớt, mà bằng cách đứng sang phía bên kia:** năm bước hỏi cùng những câu ấy từ phía extension (① trạng thái dây trong kho lưu · ② `capabilities()` · ③ quét target · ④ `dom.query` thật · ⑤ đọc công tắc). Bước ② của bản dòng lệnh (*"ghế kia còn sống không"*) **biến mất chứ không bị bỏ**: ở phía này người hỏi chính là cái ghế đó, nên câu ấy hằng đúng. Cùng hình dạng lỗi với `G-92`: một chặng lộ trình là một lời khai, và lời khai này nằm ngay trong ô *"cái bẫy đã biết, đừng vấp lại"* — tôi chép cảnh báo `G-91` sang một phía dây mà cảnh báo đó không với tới. Đo phụ, cùng ngày cùng chặng: bản đầu của bước ④ gọi `engine.runProbe("query", {…})` — sai cả thứ tự tham số (target đứng TRƯỚC) lẫn tên phép dò (`dom.query`). `ScouterEngine` **không ném** cho lỗi này, nó trả `{ ok:false, code:"PROBE_UNKNOWN" }`, nên bước ④ sẽ luôn đỏ **kèm một nguyên nhân sai**. Bắt được bằng cách đọc chữ ký thật trước khi viết phép ghim; nay ghim bằng một `engine` giả **áp đúng chữ ký** và đối chiếu `PROBE_NAMES` thật |

| G-97 | 16/09 | S-26 | ❓ **`dom.text` và `dom.wait` đã có chốt, vì con đột biến `M5` giết được cả ba đường** | tách `M5` làm ba con, mỗi con neo vào đúng một chỗ, rồi chạy lại `npm run scouter:mutation` | **SAI — hai trong ba đường không có chốt nào, và con đột biến cũ đang che chúng** | `M5` cũ khai `soLan: 3` và bộ đo thay **tất cả** các chỗ khớp (`split().join()`). Nên chỉ cần **một** phép ghim — của `dom.query` — đỏ là nó được khai *"giết được"*, trong khi `dom.text` (08/09) và `dom.wait` (12/09) ra đời bằng cách dùng lại cùng đường mà **chưa ai dựng chốt cho chúng**. Tách xong, lượt chạy đầu tiên: `M5a` (`dom.text`) và `M5c` (`dom.wait`) **SỐNG SÓT**, bộ đo tự in *"chốt này chỉ là bình luận"*. Tức **14/14 xanh của mọi lượt trước đó là màu xanh giả cho hai trong ba đường**. **Bài học, rộng hơn ba phép dò này: một con đột biến thay NHIỀU chỗ cùng lúc là một con đột biến không phân biệt được hai nhánh** — cùng họ với `assertion-must-distinguish-branches`: nó báo đạt cho cả những chỗ trống không. **Đo phụ, và nó độc lập với phần trên:** mở lượt quét *"không method nào ngoài danh sách tới được trang"* từ **4 lên 9** phép dò thì lộ ra `Page.getLayoutMetrics` **không nằm trong danh sách cho phép**, dù `page.shot` và `page.view` đã gửi nó từ lâu — lớp bảo vệ ấy **vốn không phủ hai phép dò đó**. Đã khai nó vào kèm lý do viết ra (getter thuần, cùng họ `Page.captureScreenshot`), và trang giả nay hiểu đủ ba method còn thiếu — một trang giả thiếu method **đọc y hệt một phép dò đã được kiểm** |

| G-98 | 17/09 | N1 | ❌ **Máy chủ Bridge chết giữa phiên vì `Start-Process` chạy từ PowerShell của phiên AI bị dọn theo tiến trình cha khi phiên ấy đóng** | `Get-CimInstance Win32_Process` đọc `ParentProcessId` của MọI máy chủ đang sống, rồi hỏi từng tiến trình cha ấy còn sống không | **SAI — ba phản ví dụ đang sống ngay lúc đo** | Đo 17/09 ~15:25. Máy chủ Udin `PID 17760` (bật 15:10) có cha `31212` **đã chết**, nó vẫn nghe và vẫn trả `bridge.sessions` (1 ghế). Scouter `PID 16152` cha `29188` **đã chết**, vẫn sống. Máy chủ `duc-auto-chatgpt` của chính Đức `PID 1484` cha `24052` **đã chết**, vẫn sống từ 09:07. Tức Windows **không dọn con theo cha**, và giả thuyết này còn kèm một hệ quả đã viết sẵn vào lộ trình — *"nếu đúng thì nó chỉ hỏng khi AI bật"* — mà phép đo lật luôn cả hệ quả: ghế của Đức cũng mồ côi cha và cũng không chết. **Nguyên nhân thật vẫn CHƯA BIẾT** — đừng chép dòng này thành một nguyên nhân khác. Ứng viên chưa đo, xếp theo giá: ⓡ chính bộ khởi động `-KhoiDongLai` gọi `Stop-Process -Force`, giết node **không để lại một byte `stderr`** — đúng chữ ký đã đo; ⓢ một tiến trình khác giữ cùng cổng. **Bài học, và nó lớn hơn mục này:** *"bị giết, không tự lỗi"* là một **phép đo đúng**; câu *"bị cha dọn"* là một **câu chuyện đắp lên phép đo đó**, và tôi đã viết nó thẳng vào lộ trình — có dán nhãn *CHƯA ĐO*, nhưng một dòng nằm trong ô "việc kế tiếp" thì phiên sau đọc như một điểm khởi hành, không đọc nhãn. Cùng hình dạng với *"máy chủ Udin đầy chỗ"* sống ba ngày — và phép đo lật nó tốn **một lệnh, ba dòng** |
| G-99 | 17/09 | N4 | ❓ **`scout.type` không trả `da_kiem`, nên bản extension đang nạp phải cũ hơn repo** | bốn lượt đo nối nhau, mỗi lượt bác một mảnh: ① đếm `METHOD_NOT_FOUND` · ② so `system.capabilities` với registry · ③ tìm bản chép thứ hai trên đĩa và đối chiếu dấu DOM riêng của repo · ④ đọc **nguyên phong bì thô** thay vì bản đã lọc | **SAI TỪ ĐẦU — và triệu chứng là do TÔI ĐỌC, không do máy** | `da_kiem` **luôn có mặt**. Nó nằm **cạnh** `data` chứ không **trong** `data`: kết quả có sáu khoá `action · data · cdp · write_budget · da_kiem · kiem_bang · kiem_noi`, còn `data` chỉ giữ `selector · matchCount · typed · method`. Suốt bốn lượt tôi in `r.data` rồi đọc **tập khoá của một mảnh** thành *lời khai của cả lệnh*. Đo lại đúng chỗ: `da_kiem: true`, `kiem_bang: "dom.text"`, kèm câu *“chuỗi 9 ký tự xuất hiện 0 lần trước khi gõ và 1 lần sau khi gõ — trang đã nhận”*. `scout.clear` cũng thế. **Bản đang chạy CHÍNH LÀ repo** — đo bằng bốn `id` chỉ có trong `sidepanel.html` của repo (`kiem-chay` `kiem-ket` `kiem-list` `zoom-web-nhom`), cả bốn đều khớp 1; cả hai ghế là cùng một extension. **GIÁ PHẢI TRẢ:** tôi bắt Đức nạp lại extension cho một lỗi không nằm trong extension, và đã viết kết luận sai vào sổ, `TRIALS`, lộ trình, hai commit và một memory trước khi kiểm. **Bài học, và nó lặp lại ba lần trong một ngày:** *cắt dữ liệu cho dễ nhìn rồi đọc bản cắt ấy như sự thật* — sáng nay là `targetId.slice(0,10)`, sau đó là regex `[a-z]+` nuốt mất `scout.a11y`, và ở đây là `r.data`. **In cả vật trước, thu hẹp sau** — và khi một trường *“biến mất”*, nghi cách đọc trước khi nghi cái máy |
| G-100 | 17/09 | US1 | ❓ **Vizcom không đọc được vì nó nằm ở profile không có Scouter** | mở app Vizcom rồi đếm `scout.targets` khớp `vizcom` — bốn lượt, đều 0 | **SAI — và cái sai là ở TÔI ĐỌC, không ở máy** | Vizcom của `anhducds@gmail.com` nằm ở profile `Default`, **đúng profile có ghế `Scouter_blank`**, và đọc được đầy đủ: `title` "Vizcom", 28 `button`, 9 `a`, cây trợ năng **715 node / 317 node dùng được**, trong đó có `role=button name="Vinfast VN … Enterprise plan"`. Nguyên nhân thật của bốn lượt trượt: Chrome **focus cửa sổ app đang mở ở profile khác** thay vì mở cửa sổ mới ở profile được chỉ định — `--profile-directory` **bị bỏ qua khi Chrome đã chạy** — nên thứ tôi đo là một cửa sổ `Profile 6`. Dữ liệu chỉ nói *"không thấy"*; tôi viết thành *"không tồn tại ở profile này"* và đưa kết luận ấy vào một báo cáo cho Đức. **Cùng họ `G-99`:** đọc một lát cắt dữ liệu như thể nó là cả sự thật. **Luật rơi ra, rẻ và chặn được cả họ này:** mọi câu *"không tìm thấy"* phải kèm **đã hỏi ghế nào, mỗi ghế thấy bao nhiêu target** — một câu "không thấy" không nêu người quan sát là một câu không có giá trị |
| G-101 | 17/09 | US2 | ❓ **Target sống thì trang đọc được** | mở cửa sổ app Vizcom rồi đọc NGAY, `scout.view` và `scout.query` trên cùng một target, cách nhau vài ms | **SAI — có một trạng thái THỨ BA** | Ở **74ms** sau khi cửa sổ mở: target đã tồn tại (`id=A9966ABE… tab=2096832504`), `scout.view` trả số đo thật (`viewport 2048×1078`) — tức renderer **đang trả lời** — nhưng `title`, `h1`, `button` đều `matchCount: 0` vì **SPA chưa dựng xong DOM**. Đọc lại sau khi trang dựng: `title` khớp 1, `button` 28. Nên ba trạng thái phải tách: ⓐ renderer **không trả lời** · ⓑ trả lời nhưng **chưa dựng** · ⓒ **dùng được**. Gộp ⓑ vào ⓒ thì một trang đang load **đọc y hệt một trang trống**, và đó là một kết luận sai trông hệt một phép đo. Chữa: phép dò sống hạn ngắn tách ⓐ, rồi `scout.wait` trên chính selector danh tính tách ⓑ — **không cần method mới cho ⓑ** |
| G-102 | 17/09 | US3 | ❓ **`target_id` phải hỏi lại sau mỗi lần trang đổi URL** | giữ `target_id` của Vizcom rồi để Đức tự bấm sang route khác trong chính app, đọc lại sau 20 giây | **SAI với điều hướng CÙNG NGUỒN** | `url` đổi `/files/fc25a65d-…/recent` → `/settings/organization/…/members`, `target_id` **giữ nguyên** `A9966ABE…`, lệnh sau vẫn chạy. Nên trong MỘT lượt chạy, adapter **không phải** đi tìm lại target sau mỗi lần route đổi. Ranh giới còn đứng: đóng cửa sổ thì target **biến mất**, gọi bằng id cũ trả `PROBE_FAILED — No Chrome debug target with id`, mở lại cho **id MỚI** (`43723A5D…` → `4B9C391C…`) — hỏng đóng đúng, **không tái dùng id chết**. Kết: giữ id **trong** một lượt chạy thì được, giữ **qua** hai lượt thì không |
| G-103 | 17/09 | US4 | ❓ **Cửa sổ app / PWA nằm ngoài tầm `chrome.debugger.getTargets()`** | mở một trang tự dựng dạng `--app=` và mở app Vizcom thật, rồi đếm target trước/sau | **SAI — app window không khác gì tab thường** | Cả hai hiện ra với `type: "page"` **và có `tabId`**, đọc được `scout.query` · `scout.text` · `scout.view` · `scout.a11y` như tab thường, **bảng bên đóng**. Cái thật sự chặn là **PROFILE**: `getTargets()` chỉ thấy profile mà extension được cài — Đức có **11 profile**, Scouter ở **2** (`Default`, `Profile 4`); hai ghế thấy hai tập target **rời nhau hoàn toàn** (40 vs 19). Đo phụ, cùng lượt, và nó đóng một nhánh khác: `proc=ChatGPT`, `proc=claude`, `proc=Zalo` (Electron) và `proc=msedgewebview2` (WebView2) **đều dựng cửa sổ Chromium** nhưng **không** nằm trong runtime extension của Chrome — cùng engine **không** có nghĩa cùng runtime. Ngoài phạm vi, chốt cứng, đừng mở lại |
| G-104 | 18/09 | US5 | ❓ **Lọc origin/URL là đủ để chọn đúng một trong ba tài khoản Vizcom** | mở hai tài khoản cùng lúc ở hai profile, rồi so URL · title · DOM · cây trợ năng | **SAI — không một bit nào trong URL phân biệt được chúng** | Hai cửa sổ đo cùng lúc: cùng origin `app.vizcom.com`, cùng hình dạng `/files/<uuid>/recent`, cùng `title` là `"Vizcom"`. Chỉ có **hai chỗ trên trang** khác nhau: `[data-testid="organization-switcher-button"]` đọc ra `"ĐĐức Nguyễn's WorkspaceFree plan"` so với `"VinfastEnterprise plan"`, và cây trợ năng chứa `anhducds@gmail.com` so với `tuanvv`. Cả hai marker **ổn định 3/3 lượt** cách nhau 4 giây. Hệ quả đi thẳng vào hợp đồng: `danh_tinh` là trường **BẮT BUỘC** của mọi adapter, và nó phải đến từ TRANG chứ không từ URL. Một đo phụ đáng giá ngang: `scout.page` chỉ liệt kê phần tử **tương tác**, mà email nằm trong `StaticText` — nên đường đọc duy nhất không phải đoán selector là `scout.a11y`. Đó là lý do bộ giải có hai đường đọc danh tính, không phải vì tiện |
| G-105 | 18/09 | US6 | ❓ **Ghế nào cũng gọi tới được bằng nhãn** | Đức cài Scouter lên hai profile mới; chạy pilot gọi `scout.targets` theo nhãn | **SAI — hai ghế mới lên dây với nhãn RỖNG, và một ghế không tên là một ghế không gọi tới được** | Gọi bằng nhãn rỗng thì `target` thành chuỗi rỗng, máy chủ không thấy đích nào và trả `TARGET_AMBIGUOUS — More than one extension session is connected`. Cả pilot Vizcom chết ở chặng ②, và triệu chứng đọc y hệt *"chưa chọn ghế"* chứ không giống *"ghế chưa có tên"*. Chữa: **địa chỉ LUÔN là `instance_id`** — máy sinh, luôn có, không trùng; nhãn chỉ để người đọc. Đo phụ, cùng lượt: bản đầu của bộ giải trả `ghe` = nhãn và `dia_chi` = địa chỉ, nên người gọi cầm `r.ghe` ném vào lượt sau — việc hiển nhiên phải làm — và nhận `TARGET_NOT_CONNECTED`. Tôi tự vấp ngay lượt E2E đầu tiên. Đổi lại: `ghe` = **địa chỉ**, `ghe_nhan` = nhãn. **Trường nào tên tự nhiên nhất thì phải là trường dùng được** |
| G-106 | 18/09 | US7 | ❓ **Bộ soát hợp đồng adapter chặn được lời gọi CDP thô** | viết một adapter vi phạm cố ý kèm chuỗi `Page.navigate`, rồi chạy phép ghim | **SAI — bộ dò RỖNG, và nó đã xanh trọn vẹn** | Bản đầu viết `/\bPage\.[A-Z]/`, tức đòi chữ HOA sau dấu chấm. Method CDP là `Miền.tênMethod` — miền viết hoa, tên method viết **thường**: `Page.navigate`, `DOM.getDocument`, `Input.dispatchMouseEvent`. Nên bốn trong sáu mẫu cấm **không bao giờ kêu**, và bộ soát vẫn trả `dat: true` cho một adapter gõ thẳng CDP vào. Phép ghim ⓓ bắt được ngay lượt chạy đầu vì nó đi kèm một adapter **vi phạm cố ý** — một bộ ghim chỉ thử adapter HỢP LỆ sẽ xanh trọn vẹn dưới bộ dò rỗng này. Bài học, và nó lặp lại: *một lớp bảo vệ không có ca vi phạm đi kèm thì không ai biết nó có kêu không* — cùng họ `assertion-must-distinguish-branches` |
| G-107 | 18/09 | US8 | ❓ **Ngưỡng 1.500ms cho phép dò sống là con số phải đo lại** | `scout.view` trên **19 page target** của hai ghế, ghi ms từng cái | **ĐÚNG là phải đo, và số cũ SỐNG SÓT** | Target **sống**: min 8ms · p50 28ms · p90 87ms · **max 282ms** (11 cái). Target **chết**: 6 cái, tất cả ~**20.020ms** — đúng trần `CDP_HAN_MS`. Phân bố **hai cực, cách nhau ~70 lần**, không có vùng xám: ngưỡng đặt ở đâu trong 500–2.000ms cũng tách sạch. 1.500ms = **5,3× cái sống chậm nhất** và **1/13 trần chết**, nên giữ. Một lớp thứ ba lộ ra, khác hẳn hai lớp kia: `chrome://` và `chrome-extension://` **từ chối ở 6–20ms** với lỗi khác lỗi treo — lọc theo lược đồ trước khi dò là miễn phí. **CHƯA implement `scout.song`**: đo cho thấy nó chỉ cắn khi một ứng viên chết, mà cả hai ứng viên Vizcom đều sống, nên pilot chưa chứng minh nó cần. Trigger đã ghi sẵn, đợi pilot gặp thật |
| G-108 | 18/09 | US9 | ❓ **Chỉ 2 trong 3 tài khoản Vizcom với tới được** — lộ trình Phase 2 §6 viết chân thứ ba là `Profile 10` / `vfsct01@gmail.com` | `kiem-ke-tai-khoan.mjs`: quét 4 ghế, lọc target `app.vizcom.com`, rút chuỗi **dạng** email khỏi cây trợ năng (không gõ cứng địa chỉ nào) | **SAI cả hai vế** | **3/3**, không phải 2/3 — và chân thứ ba là `vf.styling01@vinfast.vn` ở ghế `c8568b34` (`Scouter_blank`), **không phải** `vfsct01@gmail.com`. Ba ghế khác nhau · ba org UUID khác nhau · ba email khác nhau. Sáng 18/09 ghế `c8568b34` trả **0** target Vizcom; chiều trả 1 — Đức mở nó trong lúc đó. Bài học: một dòng *"đang chờ Đức làm X"* là một lời khai có hạn dùng, và không ai đọc lại nó. Bộ lọc dùng **hình dạng** email chứ không phải danh sách ba địa chỉ tôi tin là có — gõ cứng thì tài khoản thứ tư sẽ vô hình |
| G-109 | 18/09 | US9 | ❓ `scout.song` cần một phép dò CDP riêng, tức nới `READ_ONLY_CDP_METHODS` | đọc lại `PROBE_NAMES` + `READ_ONLY_CDP_METHODS`: `page.view` đã gửi đúng một `Page.getLayoutMetrics` từ 14/09 | **SAI** | `scout.song` chạy lại `page.view`, **không method CDP mới, không phép dò mới**. Thứ duy nhất mới là hạn 1.500ms + lời hứa không ném, cả hai nằm ở seed. Hệ quả cho phép ghim byte của Udin: chỉ **1** tệp lệch (`scouter-seed-core.mjs`), không phải 2 như lộ trình §3 dự tính — và bản chép của Udin **không thiếu bản vá an toàn nào**, vì không có cửa nào được mở |
| G-110 | 18/09 | US9 | ❓ Khai `CO_Y_KHAC` là đủ để một lượt lệch có chữ ký | đọc lại khối ⑷ `be-mat-hep-smoke.mjs`: nó làm `continue` | **ĐÚNG là có chữ ký, SAI là đủ** | `continue` **tắt hẳn** phép ghim trên tệp đó: từ lượt khai trở đi, mọi lượt lệch khác trên 950 dòng của seed — kể cả một bản vá cổng ghi quên chép sang — đều xanh. Sửa: lượt lệch đã khai bị **neo bằng băm hai bên**; bên nào nhúc nhích thì ĐỎ và bắt người mở lý do ra đọc lại. Đo ngay trong phiên: thêm một ký tự vào seed bên Scouter → ĐỎ đúng dòng `(bản gốc Scouter) đã đổi SAU khi khai lệch`. Và nó bắt thật một lần nữa **trong chính phiên này** khi tôi đổi `setTimeout` → `timers.setTimeout` |
| G-111 | 18/09 | US9 | ❓ `scout.song` chạy đúng sau lượt nạp lại, và `song:false` nghĩa là trang chết | Đức nạp lại 2/4 ghế; quét `scout.song` trên **28 page target** của cả 4 ghế, rồi lặp 5 lượt trên hai target Vizcom | **ĐÚNG vế đầu, SAI vế sau** | Ghế đã nạp: chạy. Vizcom `anhducds` **20·84·85·10·4 ms**, `tuanvv4` **2·3·3·2·3 ms**, `song:true` cả 10 lượt — nằm gọn trong dải sống của `G-107` (max 282ms), cách hạn 1.500ms **≥17×**. Hai ghế chưa nạp: `METHOD_NOT_FOUND` ở 3–22ms, tức bản trong Chrome là bản lúc nạp chứ không phải bản trên đĩa. `target_id` bịa → **NÉM** `PROBE_FAILED`, không trả `song:false` — đúng: "tìm target nào" là câu hỏi khác. **Chỗ sai:** `chrome://extensions/` trả `song:false` ở **ms 0–1** — đó là TỪ CHỐI (không gắn được debugger vào trang nội bộ), không phải renderer chết, và bản đầu gộp hai thứ vào cùng một `false`. Sửa: thêm `ly_do` — có chữ = không được phép hỏi, rỗng + ms≈1.500 = hỏi rồi không đáp. **CHƯA ĐO ĐƯỢC đường TREO trên máy thật**: 6 target chết của `G-107` nằm ở ghế `Scouter_blank`, ghế đó chưa nạp lại. Đường treo mới chỉ có phép ghim với đồ giả |
| G-112 | 18/09 | US10 | ❓ Vào được `/workbench/…` bằng một lượt điều hướng đảo ngược được, rồi ghi ở đó | `scout.query` trên `a[href^="/workbench/"]` ở trang `/files/<org>/recent` | **SAI** | Khớp đúng **1** phần tử, và nó là nút **`Create new file`** (`href=/workbench/folder/<id>`). Tức đường DUY NHẤT từ trình duyệt tệp sang `/workbench/` là **tạo một tệp** — chỉ xoá mới đảo ngược, mà xoá bị cấm. Thẻ tệp trong danh sách là `<button>` không `href`, nên cũng không có URL nào để `scout.navigate` tới. Pilot DỪNG trước cửa này theo đúng luật STOP của đề bài, rồi làm lượt ghi ở trang hiện tại. Trường adapter đổi tên `tep_moi` → **`tao_tep_moi`**: cái tên cũ mời phiên sau bấm vào tưởng là điều hướng |
| G-113 | 18/09 | US10 | ❓ `scout.type` + `scout.clear` là một lượt ghi **đảo ngược hoàn toàn** | chụp tập tên cây trợ năng trước/sau, hai lượt chạy | **GẦN ĐÚNG — thiếu một bước** | Chuỗi đã gõ biến mất sạch sau `scout.clear` (`da_kiem:true`, ô 17 ký tự → rỗng). Nhưng trang **không về nguyên trạng**: dư 2 tên `"Recent searches"` — bảng gợi ý còn MỞ vì ô vẫn giữ con trỏ. Không dữ liệu nào đổi, nhưng "đảo ngược" mà còn một bảng mở là đảo ngược XẤP XỈ. Thêm `scout.key Escape` → **96 tên node, đúng bằng ảnh chụp trước lượt ghi đầu tiên**. Bài học: ảnh chụp trạng thái phải so với **gốc**, không so với đầu lượt chạy hiện tại — lượt chạy ②  vào với nền 98 vì lượt ① để lại bảng mở |
| G-114 | 18/09 | US10 | ❓ Lệnh ghi không tự kiểm: `scout.type` trả `da_kiem` rỗng | đọc lại tầng của phong bì ở `scouter-seed-core.mjs` | **SAI — tôi đọc nhầm tầng** | `da_kiem`/`kiem_bang`/`kiem_noi` nằm **cùng tầng với `data`** (`{...ra, da_kiem}`), không nằm trong `data`. Đọc đúng chỗ thì: `scout.type` → `da_kiem:true`, `kiem_bang:"a11y"`, *"chuỗi 17 ký tự xuất hiện 0 lần trước và 1 lần sau — trang đã nhận"*; `scout.clear` → *"ô có 17 ký tự trước khi xoá và rỗng sau"*. `scout.key` thì KHÔNG có `da_kiem` — đó là khoảng trống thật, phải tự kiểm bằng phép đọc riêng. Đúng bài `a-narrowed-view-is-not-the-fact`: trường trông như THIẾU thì nghi cách đọc của mình trước |
| G-115 | 18/09 | US11 | ❓ Bề mặt `/workbench/<uuid>` cũng đọc được dấu hiệu danh tính như trang `/files/` | `scout.a11y` trên canvas, 50 tên node; `khoaDanhTinh` 4 lượt cách nhau 1,5s | **SAI, nhưng câu SAI này viết quá tay — sửa 18/09 tối** | Canvas sạch: **không** email, **không** workspace, **không** gói cước (50 tên node). NHƯNG lượt đọc workbench ĐẦU TIÊN cùng ngày — lúc Đức bấm nhầm mở Settings — có **143** node và **có `anhducds@gmail.com` hai lần**, cùng route `/workbench/`. Nên kết luận đúng KHÔNG phải *"dấu hiệu không tồn tại trên bề mặt này"* mà là **không THƯỜNG TRÚ: nó chỉ vào cây khi có thứ gắn nó vào, và gắn nó lại cần một lệnh GHI**. Vòng luẩn quẩn giữ nguyên, lý do thì chính xác hơn. Bài học: *"đọc 2 lượt không thấy"* là một phép đo về HAI LƯỢT ĐÓ, không phải một lời khai về bề mặt. Thứ duy nhất dính tới người dùng là chữ **`"Đ"`** — một ký tự avatar, không do ai khai, trùng với mọi tài khoản bắt đầu bằng chữ đó (`a-truncated-string-is-also-a-name`). `khoaDanhTinh` trả `DANH_TINH_LECH` **4/4 lượt, đều ~100ms** — tức dấu hiệu KHÔNG TỒN TẠI, không phải trang chưa dựng xong. Hệ quả: `danh_tinh` hôm nay là MỘT dấu hiệu cho cả site, còn thực tế **mỗi route phơi một bộ khác nhau** → hợp đồng adapter cần danh tính THEO TỪNG BỀ MẶT. `srcRoute=…/files/<org-uuid>/…` trong URL **không dùng được**: nó là vết của lượt điều hướng vừa rồi |
| G-116 | 18/09 | US11 | ❓ Lượt ghi "gõ rồi xoá" chạy sạch ở `/files/` thì bê sang workbench được | đọc `value` của hai ô prompt bằng a11y, **không gõ gì** | **SAI, và sai về phía phá hoại** | Hai ô đang giữ chữ THẬT của Đức: `"Elegan nice coupe silver car"` · `"Racing morden car colorful"`. `scout.clear` ở đây **xoá mất chữ của người dùng**. Lý do thứ hai độc lập: `textarea[placeholder="What are you creating?"]` khớp **2**, mà luật gói số 7 đòi khớp ĐÚNG MỘT trước khi ghi. Bài học: một lượt ghi đảo ngược được trên ô TRỐNG là một lượt ghi phá hoại trên ô ĐẦY — tính đảo ngược là thuộc tính của *trạng thái*, không phải của *lệnh* |
| G-117 | 18/09 | US11 | ❓ Cây trợ năng và bản kiểm kê phần tử tương tác kể cùng một bề mặt | so `scout.a11y` (143 node) với `scout.page` (25 phần tử) trên cùng một target | **SAI** | a11y kể cả nhánh **ẩn**: nó phơi ra `"Delete account"`, `"Generate API Key"` của trang Settings chưa hề mở, còn `scout.page` chỉ thấy một thẻ `a[href="/settings/account/profile"]`. Tôi đọc a11y rồi báo với Đức là "đang có modal Settings mở" — **sai**, và đã sửa ngay trong phiên. Luật rút ra: *có mặt trong a11y* ≠ *bấm được*; muốn biết thứ gì thao tác được thì hỏi `scout.page`/`scout.query`, đừng suy từ a11y |
| G-118 | 18/09 | US11 | ❓ `target_id` dùng lại được sau một lượt nối lại Bridge | máy chủ Bridge tắt giữa buổi, bật lại bằng `START-BRIDGE_Scouter.cmd` | **SAI** | Ghế tự nối lại, nhưng **mọi `target_id` cũ chết**: `PROBE_FAILED — No Chrome debug target with id …`. Nguy hiểm không nằm ở lượt chết — nó nằm ở khả năng Chrome cấp lại đúng chuỗi đó cho một tab KHÁC. Đã biến thành phép ghim: khối ⓤ của `giai-target-smoke` quét mã thật của mọi pilot và ĐỎ nếu thấy một chuỗi 32 hex gõ cứng. Đột biến thử: gõ một id vào `ghi.mjs` → ĐỎ đúng dòng |
| G-119 | 18/09 | US11 | ❓ `scout.a11y` trả về **toàn bộ** cây, nên "đọc 2 lượt không thấy email" là kết luận về bề mặt | đọc mã `a11y.tree`: có `limit` (mặc định **400**, trần 1500), có cờ `truncated`, và **tôi chưa bao giờ đọc cờ đó** | **SAI — phép đọc có một nhát cắt tôi không nhìn** | Đo lại trên `/files`: `total_nodes=275 · returned=131 · truncated=false` — lần này không cắt, nên `G-115` vẫn đứng. Nhưng **`docDanhTinh` của resolver không truyền `limit` và không kiểm `truncated`**: một trang có hơn 400 node hữu ích sẽ đẩy dấu hiệu danh tính ra ngoài cửa sổ trả về, và bộ giải báo `DANH_TINH_LECH` cho ĐÚNG tài khoản. Fail-closed nên an toàn, nhưng đó là một lượt TỪ CHỐI SAI mà không ai biết lý do. Bản vá: `khoaDanhTinh`/`docDanhTinh` phải kiểm `truncated` và ném nếu bị cắt — *chưa làm, Đức chưa cho sửa code lượt này*. Đúng bài `a-self-declared-complete-is-not-complete` |
| G-120 | 18/09 | US12 | ❓ `scout.a11y` là thứ đốt nhiều context nhất | đo `bytes_tho` của 8 method trên CÙNG một target, cùng một trang | **SAI — có cái đắt hơn, và nó ở chặng TÌM** | `scout.targets` trên một ghế 44 tab = **16.577 B**, và một lượt giải target hỏi HẾT ghế = **23.080 B** — hơn cả `scout.a11y` (14.400 B). Con số đó **không tỉ lệ với việc phải làm, nó tỉ lệ với số tab Đức đang mở**, nên nó tệ dần theo ngày làm việc. Phổ đầy đủ: `song` 73 · `view` 244 · `text` 961 · `query` 1.568 · `page` 8.966 · `tree` 10.501 · `a11y` 14.400 · `targets` 475–16.577. Bậc rẻ nhất rẻ hơn bậc đắt **9–200×** |
| G-121 | 18/09 | US12 | ❓ Muốn giảm context phải thêm method/năng lực cho trình duyệt | benchmark 3 ca × 3 mode trên Vizcom thật | **SAI** | Rút gọn ở **Node** giảm **99,4–99,7%** chữ vào context mà **không đụng extension, không thêm một method nào**: cùng một `scout.a11y`, cùng 14.400 B về Node, đưa cho AI **89 chữ** thay vì **14.180**, **cùng một kết luận và cùng một chuỗi bằng chứng**. Nhưng nó **không giảm một byte thô nào** — nên `bytes_tho` và `chu_cho_ai` phải in TÁCH, gộp lại là bỏ mất đúng chỗ tiết kiệm. Tỉ lệ hữu ích của lối đọc rộng: **43/14.180 = 0,3%** |
| G-122 | 18/09 | US12 | ❓ Ảnh Đức gửi rẻ hơn đọc DOM, nên nên mặc định screenshot-first | so chi phí: ảnh theo công thức tài liệu `(rộng×cao)/750` vs chữ đo được | **CHƯA KẾT LUẬN ĐƯỢC — và đó là câu trả lời** | Ba ảnh ~2.000×1.050 ≈ **2.800 token/ảnh, ~8.400 token** — **ước lượng theo công thức, KHÔNG phải usage đo được**, và đơn vị (token) khác đơn vị các số kia (chữ) nên **không so trực tiếp**. Ảnh rẻ khi **dùng lại cho nhiều câu hỏi** hoặc khi Đức **vốn đã gửi**; số câu hỏi tối thiểu để có lãi: **CHƯA ĐO**. Hai giới hạn đo được: ảnh chỉ ra email ở góc dưới trái nhưng **không sinh ra selector ổn định** (đoán selector bị cấm); ảnh thấy rõ nút `Generate` nhưng nút đó **chỉ có hash styled-components** nên hợp đồng luật ⑸ chặn — **biết nó ở đâu mà dây vẫn không có đường gọi hợp lệ** |

## Phép đo dùng lại được — đừng dựng lại

| cần biết | dùng | tốn |
|---|---|---|
| bảng bên có **nạp được** không — lỗi console, `id` thiếu, mã có chạy thật không | `npm run scouter:bang-ben` · `npm run udin:bang-ben` | ~15s mỗi gói, Chrome riêng |
| `setZoom`/`getZoom` có chạy khi manifest KHÔNG khai `tabs` không (`G-95`) | `npm run udin:zoom-probe` | ~30s, Chrome riêng |
| lõi ghi có bấm/gõ **tới trang** không, ngoài trình duyệt của Đức | `npm run scouter:action-probe` | ~30s, Chrome riêng |
| một bản sửa của lõi có gây lỗi không | chép lõi cũ bằng `git show <commit>:…/scouter-actions-core.mjs` vào thư mục tạm cạnh bản chép của phép đo, chạy ở đó | ~30s |
| sự kiện có **tới trang** không, trên ghế thật | trang `pilots/trang-thu-cham/` — `body[data-chuot]` · `body[data-phim]` · `#ket-qua[data-bam]` đọc bằng `scout.query` | vài giây |
| tab đang hiện/ẩn, có focus không | cùng trang — `body[data-hien]` · `body[data-focus]` | vài giây |
