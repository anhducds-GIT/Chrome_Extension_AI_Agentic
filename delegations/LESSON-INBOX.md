# LESSON-INBOX — trần 20 dòng, về 0 sau mỗi kỳ review

Format: `<ngày> · <nguồn> · <quan sát> · <BRIEF|CONTRACT|ROUTING|CHANNEL|PLATFORM> · <gợi ý (tuỳ chọn)>`

- 2026-09-02 · A-01 (dispatch) · `claim.mjs` KHÔNG có đường cho Đức duyệt chuyển quyền: nó từ chối cả `--take` vùng có chủ lẫn `--release` hộ người khác, nên một quyền đã hết việc mà chủ quên trả thì cách duy nhất là sửa tay — đúng việc lệnh này sinh ra để dẹp. Gặp thật: `_root` do `claude-bridge-multiprofile` giữ sau khi commit của họ đã vào `origin/main`. · CHANNEL · thêm `--force-release <khoá> --as <phiên> --duyet-boi duc` in rõ chủ cũ + ghi chú của họ, và đòi một cờ nói Đức đã duyệt
